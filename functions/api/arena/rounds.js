// /api/arena/rounds — clean path alias for readiness probes + DID stranger-walk.
// Same source as rounds.jsonl.js (re-export from sov-arena). Keep both.
export { onRequestGet } from "../sov-arena/rounds.jsonl.js";
