# packages/eval-ci

GitHub Action: on harness / fixture change, run the frozen bank, diff, emit a signed delta card.

Tonight the workflow exists and **does not emit a card**. That is honest. Wiring `run.sh` to an existing harness (GovBench, GoldBank-Detector) is the next commit, not a Hub mill of 3,032,028 listings.
