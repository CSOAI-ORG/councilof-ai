{
  "name": "@csoai/gspc-cli",
  "version": "0.2.0",
  "description": "csoai check <model> — print the signed axis/staleness snapshot for any measured model, with every card verified. Measurement, never certification.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CSOAI-ORG/councilof-ai.git",
    "directory": "packages/gspc-cli"
  },
  "bin": {
    "csoai": "./bin/csoai.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "gspc-card-verifier": "file:../gspc-card-verifier"
  }
}
