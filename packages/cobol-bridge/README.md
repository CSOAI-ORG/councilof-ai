# packages/cobol-bridge

Donor: `CSOAI-ORG/cobol-bridge-mcp`. **In build.** Apex `cobolbridge.ai` is **HTTP 522** (Cloudflare origin timeout; custom domain is not on Pages project `cobolbridge-site`). Pages origin `cobolbridge-site.pages.dev` answers 200 — a marketing landing, not a measured mill. Do not demo the 522. SWIFT 17 is a DISCOVERED census at `GET /api/swift`. Subtree fold is a later PR, not a silent copy.
