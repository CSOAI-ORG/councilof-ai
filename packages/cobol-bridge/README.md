# packages/cobol-bridge

Donor: `CSOAI-ORG/cobol-bridge-mcp`. Live apex `cobolbridge.ai` is **HTTP 522**. SWIFT 17 is a DISCOVERED census at `GET /api/swift` (after #1009 deploy). Do not demo the 522. Subtree fold is a later PR, not a silent copy.
