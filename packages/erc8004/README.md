# packages/erc8004

Independent Validation Registry attestations for measured agents. No owner consent. No trust score.

Tonight: stub that refuses to write a score and prints `writes_board=false`. The 531k identity census stays DISCOVERED. ChainAware selling trust scores on that layer is not our mill.
