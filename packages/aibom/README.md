# packages/aibom

Wrap point for OWASP AIBOM Generator + CycloneDX / SPDX 3.0 AI Profile.

Tonight: stub emitter. A lineage with no components yields an honest empty BOM and a `bom_sha256`. That hash is what later folds into a GSPC card. Do not stamp MEASURED from a BOM.
