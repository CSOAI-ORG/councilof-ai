# E2E — honesty surfaces

**NEXT_300 #351–360** · smoke coverage for key honesty routes.

## Indices / products smoke

`e2e/tests/indices-products-smoke.spec.ts` covers:

- `/indices`, `/indices/ai-economy`, `/products`, `/powered-by` — UNMEASURED / HO.2 copy
- Mobile viewport (375×812) for `/indices` and `/products` (#220)

Related: `pre-deploy-smoke.spec.ts`, broken-link crawl `scripts/broken-link-crawl.mjs` (#221–230), catalog lint `npm run lint:evm-catalog` (#303–308).

Doctrine: labour/economy indices stay UNMEASURED; RWA packs unsigned until custody + counsel. DSH = OS.
