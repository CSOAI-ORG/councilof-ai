# LANES — live lane claims. Check before starting ANY work; add your row before touching files.
# One lane = one concern = one owner. Do not touch another lane's files. 2026-08-27.

| lane | owner | files/territory | state |
|---|---|---|---|
| home-films | grok (tui-2) | homepage films from NotebookLM Downloads + demo loop | RUNNING |
| epic-home | grok (tui-2) | restore mega-menu + OS + HeroSlides + 9-product ToolStack + LivingStages on home | RUNNING |
| weekend-must | grok (tui-2) | MUST 1–25 lean surface: OS FAB, products four lines, assess insurer, mcp four tools | RUNNING |
| eighty-twenty | grok (tui-2) | home composer+board, footer Product, legal certify/rank copy, brand-gate nearAllow | RUNNING |
| nine-products-e2e | claude (main) | product tiles, /products, /tools, product flows | RUNNING |
| os-inner-experiences | claude (agent) | lobby/play.ts, LobbyPlay, GameBar, city/sim/arena pages, quests | RUNNING |
| gspc-mcp | claude (agent) | functions/mcp/, new mcp server package, /llms.txt, mcp-registry | RUNNING |
| axis-language | claude (main) | DONE — display "axes"->"axis", 205 strings, 71 files, commit 07983d01 | DONE |
| final-build-deploy | claude (main) | dist/, deploy — SINGLE builder; nobody else builds into dist/ | PENDING |
| harness-measurement | grok (cursor bc-54c88537) | harness/arena, /api/arena/scoreboard HEAD, living-door aliases (rwa/xrpl/evm/arc-agi/overlay/ins) | RUNNING |
| dsh-sessions | DSH | its own session work; consume gspc-mcp when published | — |
| reach-funnel | claude (agent) | /get-listed page + PRIMARY_PATHS entry + docs/OUTREACH-PACK + docs/ANCHORS-TRUTH-TABLE; committed, NOT pushed (another lane mid-flight) | COMMITTED |

RULES (all agents): master only, no worktrees, no dev servers left running, stage-by-name,
six gates before any dist build claim, browser-verify (green build ≠ working page),
display language is "axis" never "axes", counts derive from /api/gspc//api/state,
card_index rule (OWNER RULING, 2026-08-27, supersedes the 150 freeze): the index lists EVERY verifying published GSPC card — today 313 — enriched with sig/pubkey/card_url; chain.json is published; the cross-border card is a SEPARATE schema counted separately, never folded into the GSPC count. No agent may clamp the index to any constant.
deploys via scripts/deploy-site.sh --via-actions (direct wrangler = owner only).

## lane/os-unify-home-board — Claude (2 Sep 2026) — PR #1093
Writer: Claude (one writer). Files: client/src/App.tsx (/os route → OsRoute), components/DashboardLayout.tsx, components/DashboardPane.tsx (new), components/Header.tsx, components/HeaderNav.tsx, components/HeroSlides.tsx, components/home/{HeroBoard,HomeCinematicWorlds,HomeWorlds,HomeGspcBoard(new)}.tsx, pages/{Dashboard,HomeVerify}.tsx, functions/api/gspc.ts (totals.public_leader_count/model_fleets/fact_runs, derived), public/llms.txt (lid line).
Ruling executed (owner, 2 Sep): Dashboard IS Council OS · tabs render in-shell, never navigate out · /os?lobby=X → /dashboard?tab=X (embed=1 keeps AG-UI pane) · home board = living HF Space csoai/gspc-board + derived lid + 22-axis strip. Other lanes: do not re-point CTAs back to /os; do not add a second board.

## lane/results-hungry — Claude (2 Sep 2026) — hub-queue mill: the loop had no consumer
Writer: Claude (one writer). Files: `.github/workflows/hub-queue-land.yml` (new), `.github/workflows/hub-queue-flip.yml` (new), `scripts/land_mill_cards.py` (new), `scripts/flip_hub_queue.py` (new), `harness/gspc-top100/mill_hub_queue.py` (OpenRouter same-id fallback + `route` on the card body), `harness/gspc-top100/test_mill_honesty.py` (+3 tests).

**Measured on 2 Sep 07:37Z (run 33604505548, axis=governance, grade=8):** providers_with_keys=1 (HF only — the owner's secrets are named `GEMINI_TOKEN` / `NVIDIA_NIM_TOKEN` / `OPENROUTER_TOKEN` / `GROK_TOKEN`, mapped by PR #1146), graded=8, staged_unsigned=0, skips: 92 × not-in-this-batch-pick, 8 × `UNCHECKABLE probe hf:<slug>:…:HTTP 403`. hf-inference-mill (run 33604507713): `INFERENCE_FAIL 403 "This authentication method does not have sufficient permissions to call Inference Providers"`. Both mills grade nothing until `HF_INFERENCE_TOKEN` carries the **Inference Providers** scope (issue #1102, owner-only, 2 min). No Cerebras / Together / Mistral / SambaNova secret exists.

**The gap (bytes):** `hub-queue-mill.yml` only ever uploaded `mill-out/` as a run artifact. The sign path (`scripts/sign_mill_cards.py` under `hf-fin-shells-measure.yml target=mill`, OIDC → `/api/board-sign`, allowlist by workflow filename) reads `public/interop/mill-cards-unsigned/` on a branch — nothing put the artifact there. The 82 existing VALID cells (`public/interop/mill-cards-signed/`, 12 models × 7 axes, all n=30) were landed by hand in PRs #1057–#1073, and the HF `csoai/hub-queue` flip was done off-repo.

**What now consumes staged cards:** `hub-queue-land.yml` (workflow_run after every mill run) downloads the artifact → `scripts/land_mill_cards.py` (rejects anything signed / tampered / n=0 / >3KB / brand-gated; one card per (model, axis)) → branch `mill/land-<axis>-<run_id>` → `gh workflow run hf-fin-shells-measure.yml --ref <branch> -f target=mill` → waits, verifies VALID under the live DID, comments the verdicts on the PR. Human merge is the only gate. After merge, `hub-queue-flip.yml` (push to master touching `mill-cards-signed/`) runs `scripts/flip_hub_queue.py`: cell MEASURED iff VALID and n≥30, top-level `status` untouched, then writes `csoai/hub-queue` (queue.jsonl/parquet/SUMMARY.json) and `csoai/gspc-hub-cards/mill-cards/` with the GHA HF token (main, PR fallback) — only when a cell actually changed. Dry-run on today's bytes: 82 VALID → 82 cells, `changed=false`, byte-equal to the public SUMMARY.

**Still owner-gated:** (1) `HF_INFERENCE_TOKEN` Inference Providers scope (#1102); (2) the mill grades the hub slug *as itself* — direct provider keys only help through OpenRouter under the same id (this lane) — Groq/Gemini/NIM keys do not serve arbitrary hub slugs and stay inert for this mill; (3) `pick_emptiest` walks rank order, so the 8 emptiest are the same unservable rank-top repos every hour (gpt2, opt-125m, trl tiny) until they are marked dead across runs — a cross-run skip file is the next fix; (4) `csoai/gspc-safety` bank is 401 (private) — the `safety` axis needs the public bank or a mapping in the workflow `case`.
## lane/xrpl-swift-eater — Claude (agent, 2 Sep 2026)
Writer: Claude (one writer). Files: harness/rwa-attest/xrpl_swift_eater.py (new eater), scripts/adapters/staged_leaves.py (new file-reader adapter, never raises), scripts/adapters/xrpl-swift-eater-cards.test.ts (vitest gate: 3KB + verdict words + sha/sig), public/interop/xrpl-swift-eater-2026-09/ (52 UNSIGNED card-v0 atoms + SKIPLOG + manifest + TOML mirrors), scripts/publish_public_root.py (+4 lines: staged_leaves wired as a leaf source). Signing = GHA public-root.yml only. Never touches *.signed.json, card_index, HF, or a key.

## lane/provider-diff-feed — Claude (agent, 2 Sep 2026)
Writer: Claude (one writer). Files: scripts/watch/{provider_watch.py,targets.json,test_provider_watch.py,fixtures/}, scripts/adapters/provider_diff.py (+5 lines in scripts/publish_public_root.py wiring it as a leaf source), public/feeds/provider-diff/{state.json,index.json,leaves/}, functions/api/feeds/provider-diff.ts (+test), functions/api/_skus.ts (+1 SKU provider_diff_feed), functions/api/x402.ts (+1 catalog tier), .github/workflows/provider-watch.yml, docs/PROVIDER-DIFF-FEED.md. Hash-only; robots honoured; UNCHECKABLE/UNKNOWN never reported as unchanged; signing = GHA public-root only; no backfill; no keys.
## lane/e2e-product-finish — Claude (2 Sep 2026)
Writer: Claude (one writer, worktree /private/tmp/coai-e2e). Files: components/DashboardPane.tsx (every tab id + legacy door id → a real pane; honest fallback), components/DashboardLayout.tsx (hook order, pane crumb, Overview link, free-rail padding), lib/lobbyLink.ts (+test: hrefs land on /dashboard?tab= directly), components/lobby/LobbyEvidencePane.tsx (axes ReferenceError), pages/{FinancialAxes,CouncilSpace,Dashboard,Benchmarks,WatchdogMap,PublicWatchdog}.tsx, App.tsx (/rankings direct), e2e/tests/dashboard-shell.spec.ts (new, CI in deploy.yml), e2e/playwright.shell.config.ts (new), e2e stale specs retired/re-pinned.
Territory: the Council OS shell end-to-end. Other lanes: do not add a second pane map; register new tabs in DashboardPane.PANES.
## lane/mill-picker — Claude (2 Sep 2026) — hub-queue mill picker: dead slugs, servable tags, probe-first
Writer: Claude (one writer). Files: `harness/gspc-top100/mill_hub_queue.py`, `harness/gspc-top100/dead_slugs.jsonl` (new, persistent), `harness/gspc-top100/test_mill_honesty.py` (+3), `public/interop/mill-cards-unsigned/` (+4 UNSIGNED governance cards from HF Job 6a98072d0718b0f6d891094c).
Three honest zeros before the fix: rank-order pick re-probed gpt2 / tiny-Qwen / opt-125m every run; `--only live_provider_slugs.txt` picked 0 (its 12 ids are not in the emptiest 100); and from the HF Jobs runner Python `urllib`'s default User-Agent gets a Cloudflare 403 HTML page from `router.huggingface.co` while `curl` gets 200 — so every router call from a job was UNCHECKABLE regardless of token.
Fix: (a) `dead_slugs.jsonl` honoured on pick, appended only for model-side outcomes (probe-first "no live inference provider", Hub 404, or every provider suffix 400/404 — a single suffix's 400 is that provider's miss, never a dead model; 401/403/429 are token/rate states and never persist); (b) pick = empty cell ∧ `pipeline_tag ∈ {text-generation, text2text-generation, conversational}` ∧ not dead, walking rank with no fallback to non-generative repos; (c) `--probe-first` asks the public `api/models/<id>?expand[]=inferenceProviderMapping` for a live conversational/text-generation provider before spending a grade; (d) `_chat` sends a real User-Agent, keeps the provider's error body in the skip reason, and sets `chat_template_kwargs.enable_thinking=false` so Qwen3-class models answer instead of spending max_tokens on reasoning. n≥30 unquotable rule and never-MEASURED unchanged.
Run (HF Jobs cpu-basic, mirror at 103ec792 + this patch, governance, `--pick 300 --grade 6 --probe-first --dead`): picked=300, graded=6, **staged_unsigned=4** (Qwen/Qwen3-0.6B 6/30, Qwen/Qwen3-8B 16/30, Qwen/Qwen2.5-7B-Instruct 10/30, meta-llama/Llama-3.2-1B-Instruct 7/30 — all `route: hf-router`, all UNMEASURED, signature null), skips 289 × not-in-this-batch-pick, 5 × no live inference provider (probe-first → dead file), 2 × `:groq HTTP 400` (Qwen2.5-1.5B/3B, provider miss, not dead). Accuracy numbers here are card bytes, not a leaderboard: nothing is MEASURED until OIDC sign + VALID.
Still owner-gated: signing (GHA OIDC `hf-fin-shells-measure target=mill` on this branch, or an HF Jobs runner holding BOARD_SIGN_KEY); the staged cards are the deliverable.
