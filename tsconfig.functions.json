{
  "compilerOptions": {
    "target": "es2022",
    "module": "esnext",
    "moduleResolution": "bundler",
    "lib": [
      "es2022",
      "dom",
      "dom.iterable"
    ],
    "strict": false,
    "noEmit": true,
    "skipLibCheck": true,
    "allowJs": false,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "types": [
      "@cloudflare/workers-types"
    ]
  },
  "include": [
    "functions/**/*.ts"
  ],
  "exclude": [
    "**/*.test.ts"
  ],
  "_note": "Mirrors tsconfig.json (strict:false, ES2022, bundler) so the count reflects the repo, not a strictness chosen here. types now names @cloudflare/workers-types, which is a real devDependency as of this change: it supplies PagesFunction, KVNamespace, Request/Response and the rest of the Workers globals, and removes 181 of the 233 errors recorded in docs/reports/functions-typecheck-2026-09-06.md. What is left is real."
}
