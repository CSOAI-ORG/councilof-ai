import { XRPL_STATUS_LABEL } from "./anchoringClaim";

/** Mega-nav copy leftover after #966. Honesty stays free; /assess is Coming — Paddle waitlist; /xrpl-attest is a public-root reader. */
export const ASSESS_NAV_NAME = "Get measured — Coming";
export const ASSESS_NAV_DESCRIPTION =
  "Honesty stays free. /assess is Coming — Paddle waitlist. Not a free signed run";
export const XRPL_NAV_DESCRIPTION =
  `${XRPL_STATUS_LABEL}. Living feed is GET /root.json; GET /api/xrpl writes_board false. Never a rating`;
