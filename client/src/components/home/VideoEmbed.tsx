/**
 * Moved. The implementation now lives in the shared scroll-world module so the
 * homepage and every deck page run ONE copy: client/src/components/scrollworld/VideoEmbed.tsx
 * This file stays as a re-export so existing import paths keep resolving.
 */
export { VideoEmbed } from "@/components/scrollworld/VideoEmbed";
