/**
 * Global Search Component - Command Palette Style (Cmd+K / Ctrl+K)
 * PLACEHOLDER_RESTORE_IN_PROGRESS — will be replaced with full carry-on content
 */
export function GlobalSearch(_props?: { open?: boolean; onOpenChange?: (open: boolean) => void }) {
  return null;
}

export function GlobalSearchTrigger(_props?: { onClick?: () => void }) {
  return null;
}

export default GlobalSearch;
