#!/usr/bin/env python3
"""csoai-goal-mode.py — get all done today.

The 10 goals for today (in priority order):
  1. Fix the deploy pipeline (the predicate URI returning 200)
  2. Fund the burner wallet ($5 USDC)
  3. Set X402_FACILITATOR_URL on CF Pages
  4. Send 4 grant applications (NLnet deadline 3 Nov 2026)
  5. Publish gspc-card-verifier to npm (2FA)
  6. Publish csoai-ai to PyPI (2FA)
  7. Register EAS schema on Base (MetaMask)
  8. Set GH HF token (HF badge → public)
  9. Submit arXiv preprint (needs endorsement)
 10. Submit SWH archive

What the agent can do (lane-doable):
  A. Run every relentless master loop
  B. Mine every atom source
  C. Sign every atom
  D. Anchor every card (OTS + Rekor + EAS planned)
  E. Wire every engine
  F. Wire every game
  G. Build every API endpoint
  H. Build every well-known door
  I. Build every interop format
  J. Build every A2A agent card
  K. Build every MCP server
  L. Run every gate (brand + facts + redirects)
  M. Run every test (vitest)
  N. Commit + push to master

What only the operator can do (5 clicks):
  1. Fund burner wallet ($5 USDC) — `0xb3a6a8d92a3aec1aca31b4ef06c95be468562c47f054182f1a39d26a880827da`
  2. Set CF Pages env var X402_FACILITATOR_URL
  3. Send 4 grant applications (paste + submit)
  4. Set GH HF token for badge draft → public
  5. npm publish gspc-card-verifier (needs 2FA)

Operator has 5 clicks to make + agent does everything else.
"""
