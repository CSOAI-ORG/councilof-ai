# Goal mode — TUI 2: N-site coverage and distribution

Paste everything below into TUI 2.

```text
GOAL MODE: N-SITE COVERAGE + DISTRIBUTION. Do not modify payment/evidence internals or the Council OS UI.

Objective: make Hugging Face the first reusable N-site adapter, using honest coverage states that can later extend to Kaggle, OpenML, GitHub, and other licensed/open surfaces.

1. Inspect the existing scripts/auto-eat engine and document both deliberate locks. Keep schedule and signing/OIDC disabled until TUI 1 proves the exact-byte atom chain and Master authorizes activation.
2. Define and enforce one adapter state ladder:
DISCOVERED → CATALOGUED → ACCESSIBLE → RUNNABLE → PROBED → REPRODUCED → MEASURED → ADMITTED → SIGNED → OTS_CONFIRMED → PUBLISHED.
Never say “100% coverage” without a frozen denominator, timestamp, exclusions, and revision identifiers.
3. Implement and test only the Hugging Face adapter first: models, datasets, Spaces, provenance, revision pinning, license/terms, rate limits, runnable eligibility, evidence lineage, retries, and removal/drift handling.
4. Keep discovery/probing on GitHub runners, Oracle, or CPU workers. The RunPod 3090 is inference-only and may emit unsigned UNMEASURED candidates. It must not sign, anchor, publish, claim Hugging Face identity, or claim BFT/compliance.
5. Preserve the three x402 census methodologies separately; never merge host-, resource-, and sample-level percentages.
6. Rank later N-sites by lawful API access, reproducibility, coverage value, compute availability, and adapter reuse. “Open” does not waive terms, licenses, attribution, privacy, or rate limits.
7. Build a bounded dry-run fixture proving retries, resumability, rate limiting, deduplication, revision drift, tombstones, and explicit failure states. Do not publish externally.
8. Inventory current Hugging Face assets before creating anything new. Consolidate duplicate datasets, Spaces, cards, or badges into a canonical subject/revision identity map.

Deliver:
- NSITE-ADAPTER-CONTRACT.md
- frozen Hugging Face coverage manifest
- dry-run report with exact denominators
- next-site ranking
- PR-ready owned branch and exact changed-file list

No external publication, autonomous schedule, signing, deployment, GPU crawling, or account changes.
```
