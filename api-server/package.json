{
  "name": "csoai-api-server",
  "version": "1.0.0",
  "description": "CSOAI governance API — GitHub OAuth + evidence collection, HMAC-signed webhooks, fairness harness, Sovereign Town export. Designed to run on the CSOAI GCP VM.",
  "type": "module",
  "main": "server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0"
  }
}
