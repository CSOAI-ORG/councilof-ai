{
  "schema": "csoai.card-store-verification/0.1",
  "as_of": "2026-08-26",
  "mandate": "BOARD-RULING.md (2026-08-26) 1: every card hash must resolve to a signed card whose bytes recompute; the number that verifies becomes the board.",
  "method": "For every index hash (150 from master; 335 from restored history): locate card bytes in every reachable store (pod signed-cards content-index, SOVOS/assets/cards, mergekit/3kb_cards, oowm cards, HF datasets csoai/gspc-*), recompute sha256(canonical body) == content_id, then Ed25519-verify the embedded signature.",
  "stores_searched": ["pod 3090 /workspace/sovos-repo/signed-cards (content-index: 16 entries)", "pod SOVOS/assets/cards + mergekit 3kb_cards (26 .3kb = float32 weight vectors, NOT cards)", "pod oowm_merge_v1/cards (chessboard clan cards — different format)", "HF csoai/gspc-board, gspc-boards (208 files: board_*.json + benchmark-register, no hash-named cards)", "HF gspc-signed-boards, signed-fleet-boards-v2 (board-type files)"],
  "result": {"verified_of_150": 0, "verified_of_335": 0, "canary_checks": "8/8 store candidates were inspected by bytes; zero content-hash->byte resolutions found.",
  "conclusion": "The frozen 150 floor STANDS per the ruling (a freeze needs no verification to stand). The 185 remain UNMEASURED. The real card store location is UNRESOLVED: candidates = Oracle EC2 micros (uk-london-1), Kaggle kernels, RunPod sink pod 213.173.105.83, A100 (endpoint drifted this round). Next: store hunt on those hosts.",
  "honesty": "0 verified is the honest count from the reachable record — not a claim that cards don't exist elsewhere, and never an invitation to relax the floor."
}
