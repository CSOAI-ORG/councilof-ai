# Interop harness notes (short)

## Separation densify (#12) — SKIPPED here

**Separation densify is Measure/Arena owned.** It needs real McNemar compute on pods (discordant-item p-values → `SEPARATED` / `TIE` / `UNTESTED`).

- Do **not** invent separation scores, p-values, or win counts in `/interop`.
- Do **not** densify behavioural axes from static JSON alone.
- Cite live board / arena outputs when Measure/Arena publishes them; empty stays empty.

Owner: Measure/Arena lane. This tree only hosts unsigned coverage leaves and honesty envelopes.
