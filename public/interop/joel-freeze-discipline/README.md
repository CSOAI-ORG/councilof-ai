# EAT: Joel / Certisyn freeze discipline → UNCHECKABLE reason vocab

Public **method alignment** only. Cite the freeze habit; do **not** treat this
leaf as affiliation, endorsement, partnership, or Certisyn/VRO implementation.

## Discipline (our framing)

| Rule | Meaning |
| --- | --- |
| `UNCHECKABLE` must carry a **reason** | A bare state without why is incomplete |
| nothing-to-check ≠ failed-to-check | Missing preimage / absent payload is not a failed evaluation |
| Freeze path mirrors atom path | `freeze → hash → confirm → deposit` ≈ `card → root → witness` |

We keep our own sentence (card / root / witness). Their freeze→hash→confirm→deposit
is recorded as a **mirror**, not adopted as our product claim or as a Certisyn seal.

## What this is / is not

- **Is:** honesty vocab for unsigned interop leaves — named UNCHECKABLE + reason.
- **Is not:** a VRO grade, VAO, GSPC axis fill, SCITT TS, or endorsement of
  Certisyn / Joel Hillier / peers named on adjacent banks.
- **Unsigned only.** No `/api/gspc` edits. No wrangler. Never certify.

## Adjacent banks that apply the vocab

- [`../emilia-ep/`](../emilia-ep/) — `EP-AUTHORIZATION-PAYLOAD-IDENTITY-VERIFIES` stays
  UNCHECKABLE when the pinned vector set has no EP receipt payload (reason required).
- [`../scrapi-ccf/`](../scrapi-ccf/) — data-hash binding is not authorization; entry
  digest cannot substitute for authorization.

## Cite (public method, not affiliation)

- draft-hillier-certisyn-ai-governance-verified (IETF individual draft) — map / cite only
- Certisyn public materials — no implementation claim here

Atom path (shared):

```
probe / pin / digest → unsigned card-v0 → (later n≥30 + 4way + keystone) → root → witness root only
```
