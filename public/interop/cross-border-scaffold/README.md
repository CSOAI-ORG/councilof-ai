# Cross-border SCAFFOLD leaves (unsigned)

Six **domain** leaf stubs for `domains[].cross-border` (`axes: 6`,
`status: SCAFFOLD` on the live board). **Not board slots.**

| Leaf | Control (east-west mapped) |
| --- | --- |
| `leaf-risk-management.json` | Risk management |
| `leaf-data-governance.json` | Data governance |
| `leaf-documentation-records.json` | Documentation & records |
| `leaf-transparency-disclosure.json` | Transparency & disclosure |
| `leaf-human-oversight.json` | Human oversight |
| `leaf-security-resilience.json` | Security & resilience |

Hard stops: `status=SCAFFOLD` only; `sig_ed25519=null`; never invent MEASURED;
never wrangler; never certify; never promote these into GSPC axis cells.
