# Lid pack — 1 Sep 2026

Assembled against the **live** board after #1077.
Quote both numbers from `totals.public_count`. Ready is not certified. Live is **22 axis · 22 measured**.

Machine copy: [`/interop/lid-2026-09-01.json`](/interop/lid-2026-09-01.json).

## Live GET (this publish)

- GSPC: **22 slots / 22 measured / 0 UNMEASURED**. Schema `csoai.gspc-axes/0.5`. 14/14 behavioural measured. 8/8 financial/domain measured as deterministic-facts runs (risk verdicts stay UNMEASURED). Living stamp **SIGNED**. DOI `10.5281/zenodo.21991104`.
- Public root: **200 on both hosts, UNSIGNED envelope**. `as_of` `2026-09-01T01:48:00Z`. merkle `4a9a5036b7e82b682e0908062e6b43043e3b16f02d1e4694b73607ad565ac69c`. The file itself says it is not Ed25519-signed until keystone.
- Free inclusion **LIVE** at `/api/proof?sha=` and matches that merkle. Bare `/api/proof` is **400** with the free path named.
- Hub census: **3,032,028 DISCOVERED / n_measured=0**.
- ERC-8004: **531,269 identities** (31 Aug). Cannot measure 531k today.
- `cobolbridge.ai`: **in build · HTTP 522**. Do not demo. Status: [`/cobolbridge-status.json`](/cobolbridge-status.json).
- `/api/swift`: honest **404**.
- card-v0 surfaces frozen at four. `genius.reserve` / `erc8004.callable` / `cobol.legacy` need a published schema bump.

## BOARD_SIGN_KEY (truth)

The PKCS8 does **not** live in GitHub Actions secrets. `gh secret list` on `CSOAI-ORG/councilof-ai` has no `BOARD_SIGN_KEY_PKCS8_B64`. It lives on **Cloudflare Pages**. GHA authenticates with OIDC to `POST /api/board-sign` (401 without a GitHub OIDC bearer). Do not copy the key to GHA, laptop, 3090, or MetaMask.

`public-root.yml` cron is already `7 * * * *`. Halt-on-split and halt-on-unsigned-leaf are armed. Envelope signing, when it lands, is a **compact preimage** under the 3KB board-sign cap — not the full root JSON.

## MCP

Remote URL: `https://councilof.ai/mcp` (seven tools live, including `get_root` / `get_card` / `verify_inclusion`). npm `csoai-gspc-mcp@0.1.0` still ships four. Source tracks 0.1.1.

## Hard stops

fill-7 · 22/22 · laptop sign · second root writer · paywall `/root` · COBOL 522 demo · Hub `CSOAI/` uppercase · mill 3,032,028 · trust-score 531k
