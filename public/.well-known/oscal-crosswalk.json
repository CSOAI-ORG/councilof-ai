{
  "schema": "csoai.well-known/0.1",
  "slug": "oscal-crosswalk",
  "name": "OSCAL crosswalk posture",
  "description": "What OSCAL material the estate actually holds, measured, with the path to each file.",
  "as_of": "20260905T133000Z",
  "state": "PARTIAL",
  "upstream": {
    "standard": "NIST OSCAL (Open Security Controls Assessment Language)",
    "reference": "https://github.com/usnistgov/OSCAL",
    "relationship": "none. A crosswalk to a public schema is not a relationship with the body that publishes it, and no NIST participation, membership or endorsement is implied."
  },
  "measured_component_definitions": [
    { "path": "defoneos-assurance-pack/oscal/MEOK_OSCAL_COMPONENT.json", "components": 15 },
    { "path": "csoai-os/maps/archive/sovereign_maps.oscal.json", "components": 14 },
    { "path": "MEOK_OSCAL_COMPONENT.json", "components": 10 }
  ],
  "largest_real_component_definition": 15,
  "probe": "python3 -c \"import json,sys; d=json.load(open(sys.argv[1])); print(len(d['component-definition']['components']))\" <path>",
  "retracted_claim": {
    "claim": "97-component Ed25519-signed OSCAL package",
    "asserted_in": "CSOAI_LAYER0_SCORECARD_2026-06-29.md, protocol P6",
    "state": "DOES NOT HOLD",
    "re_measured": "2026-09-05",
    "why": "The cited file layer0_protocol.oscal.json is not an OSCAL document. Top-level keys are constitution, vsm, layers, charters, archetypes, council, temples, guarantees, resonance, sigil. There is no component-definition key and no components array, so the scorecard's own verification one-liner raises KeyError on the file it names. The sigil value is a bare 32-hex string, not a signature.",
    "largest_actually_found": 15
  },
  "existing_crosswalk": {
    "slug": "nist-airmf-gspc-crosswalk",
    "note": "A real crosswalk the estate already holds. Recorded so the gap is scoped correctly: what is missing is an OSCAL component-definition over the GSPC axes, not a crosswalk capability."
  },
  "what_this_does_not_establish": [
    "No OSCAL component-definition over the GSPC axes exists yet.",
    "No NIST endorsement, membership or participation is claimed.",
    "The 97-component figure is not restated here as true. It is recorded as a retracted claim with the measurement that replaced it."
  ],
  "links": { "self": "https://councilof.ai/.well-known/oscal-crosswalk.json", "live_board": "https://councilof.ai/api/gspc" },
  "doctrine": "A number with no runnable check behind it is not a measurement. Every count on this door names the file it came from."
}
