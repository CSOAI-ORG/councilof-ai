# Apollo ACRED adapter (Ethereum / Securitize)

**Posture:** REPORTED / contact-only · **not MEASURED** · no signing in this stub.

## Notes

- **Public artifact only** — cite dated Etherscan + Securitize fund pages; never invent AUM as measured_score.
- `fetchFacts()` returns unsigned Stage-2 adjacency facts from `rwaAttestationTargets` / matrix.
- Reg D feeder into Apollo Diversified Credit — measurement attestation language only (not a rating).
- Wilson / signed cards require frozen bank + custody + counsel — not this adapter.

## Usage

```bash
npx tsx adapters/xrpl/apollo-acred/index.ts
```
