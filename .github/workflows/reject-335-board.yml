name: reject-335-board
# DISABLED — mine MANIFEST has 335 signed cards. Do not restore exact-150.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  reject:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "disabled — floors must not race verified 335"
          exit 0
