name: reject-335-board
# DISABLED for wave-11 restore (was exact-150 floor).
on:
  push:
    paths:
      - 'public/signed/card_index.json'
      - '.github/workflows/honest-board-floor.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  reject:
    if: false  # wave-11 restore
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Restore honest 150 and exact-150 floor if either drifted
        run: |
          set -euo pipefail
          echo "disabled for wave-11 restore"
