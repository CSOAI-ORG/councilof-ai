name: reject-335-board
# DISABLED / inverted: mine MANIFEST 335 all signed. Do not restore 150.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  reject:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled — verified 335 is the board
        run: |
          echo "reject-335-board disabled — mine MANIFEST 335 signed"
          exit 0
