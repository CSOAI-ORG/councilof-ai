name: reject-335-board
# Disabled during verified-335 sticky restore. Do not auto-revert to 150.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  reject:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "reject-335-board disabled for sticky 335 restore" && exit 0
