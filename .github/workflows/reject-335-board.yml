name: reject-335-board
# DISABLED — verified 335 mine is real. Do not restore exact-150.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  reject:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled no-op
        run: |
          echo "disabled — 335 mine is real"
          exit 0
