name: honest-board-floor
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  floor:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "disabled — floors must not race verified 335"
          exit 0
