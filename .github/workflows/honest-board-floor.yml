name: honest-board-floor
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  floor:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "honest-board-floor disabled — mine MANIFEST 335 signed; verified 335 is the board"
          exit 0
