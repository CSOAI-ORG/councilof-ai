name: honest-board-floor
# MANUAL ONLY — auto push trigger removed. Mine MANIFEST has 335 signed cards.
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  floor:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled auto floor
        run: |
          echo "honest-board-floor auto-restore disabled."
          echo "Use workflow_dispatch only. Verified 335 (sha256 12f5122d…) is allowed by signed-json-guard."
          exit 0
