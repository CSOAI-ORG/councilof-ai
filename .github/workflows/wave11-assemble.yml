name: Wave11 assemble card_index
# DISABLED — do not assemble or claim 335. Do not invent the missing 185.
on:
  workflow_dispatch:
jobs:
  assemble:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled — do not claim 335"
