name: atomic-publish-card-index-335
# workflow_dispatch only — do not auto-publish a fabricated 335 board.
# The honest published board is exactly 150 cards / 34171B. Do not invent the missing 185.
on:
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse fabricated 335 publish
        run: |
          echo "atomic-publish-card-index-335 is disabled. The honest board is 150 cards."
          echo "Do not claim 335. Do not invent the missing 185."
          exit 1
