name: sha256-gated-335-once
# Disabled. Do not publish a fabricated 335 board.
on:
  workflow_dispatch:
jobs:
  refuse:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse fabricated 335 publish
        run: |
          echo "sha256-gated-335-once is disabled. The honest board is 150 cards."
          exit 1
