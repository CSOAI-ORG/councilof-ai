name: restore-honest-150
# DISABLED — mine MANIFEST has 335 signed cards.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "disabled — floors must not race verified 335"
          exit 0
