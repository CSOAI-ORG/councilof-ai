# Absorption Intelligence — June 25, 2026

> V2 ↔ M4 coordination. Three research packages (SOCIAL_LEGACY, CROWN_JEWELS 308,
> BLACK_SWAN 121) processed into the OS. Live pages shipped now; deep fork/runtime
> work is the M4 + GCP-VM lane.

## Shipped to the V2 OS (live on csoai-v2-app.vercel.app)
| Surface | Route | Source | Notes |
|---|---|---|---|
| Legacy Bridge | `/legacy`, `/cobol` | SOCIAL_LEGACY | Layer 0 control H — COBOL/mainframe tunnels, Ed25519 attest, finance pre-checks |
| Social OS | `/social` | SOCIAL_LEGACY | AI character as social presence — 50+ platforms, decentralized protocols, 12 civilizations, DSA/GDPR/C2PA |
| Crown Jewels | `/jewels`, `/crown-jewels` | CROWN_JEWELS + BLACK_SWAN | Tiered Diamond/Gold/Silver marketplace + black-swan vault, mapped to OS components |

All three reachable from the OS launcher (`/os`) tiles and verified rendering live.

## Absorption backlog (M4 + VM lane — fork/integrate the real repos)
- **Governance engine** ← ACGS-Lite (constitutional, Ed25519 receipts) + AegisAI (EU AI Act + LLM Guard)
- **MCP ecosystem** ← Agent Fleet Q (675+ MCP tools, visual DAG)
- **Character memory** ← A-MEM (Zettelkasten) + Mnemosyne (7-type taxonomy) + G-Memory
- **Sov Town minds** ← agent-village + Generative Agents/Smallville + concordia
- **Voice pipeline** ← Kokoro TTS + openWakeWord (the planned Kokoro lane on the VM)
- **Agent sandboxing** ← forkd (microVMs, 100 children / 101ms)
- **BFT Council** ← OPUS (Borda) + ByzFL (Byzantine-resilient FL)
- **Vector / on-device** ← chromem-go + Vicinity
- **3D globe** ← NASA WorldWind (ties into csoai-dashboard globe work)
- **Data layer** ← Common Crawl + GDELT + SEC EDGAR (free, petabyte-scale)

## Legacy bridge toolchain (runtime, when the VM lights up)
GnuCOBOL · Apache Camel (350+ adapters) · Debezium CDC · Mirth Connect (HL7/FHIR) ·
QuickFIX (FIX) · Apache PLC4X (SCADA/OPC UA → OpenGridWorks) · X-Road (gov) · Node-RED.

## Distribution play (the business model)
Each jewel → an MCP server / plugin in the Crown Jewels Marketplace; builders paid via
x402. CSOAI becomes the distribution layer for ~238 under-distributed solo builders.

## Open item for owner
`councilof.ai` apex currently serves a separate "33-Agent BFT Council" landing site, NOT
the V2 OS. The V2 OS (incl. all of the above) is live at `csoai-v2-app.vercel.app`. If
councilof.ai should point at the V2 OS, that's a Vercel domain-mapping change — owner lever.

> Runtime absorption (forking repos, wiring inner-sov3 queens, x402 payouts) flips on with
> the Layer 0 gateway / GCP VM api-server deploy — the one owner lever.
