# Estate crosswalk — RWA attestation × GSPC × regulation × SOV Signal × labour/economy

> How the Aug 2026 RWA / Council OS work maps onto **everything we already ship**,
> live regulation legs, and honest gaps (human labour, humanoid labour, AI economy).
> Claude doctrine: measurement not certification; DSH = OS; unsolicited + permissionless;
> scores never sold; regulators free forever; no model judges another.
> CSOAI Ltd · UK **16939677**.

## One sentence

We are **both** a signed-data generation business **and** a compliance-measurement attestation body: the 3KB Ed25519 card is the atomic unit; RAS / A2A / MCP / index subscription monetize access to cards and runs — **never** the grade; regulators free forever.

## The composed product (already live)

```
Live regulation          Hive crosswalk (15)         GSPC board (13+jail)        Arena / reported
GET /api/regulation  +   /crosswalk              +   GET /api/gspc           +   REPORTED / PARTIAL
                              │
                              ▼
                    GET /api/signal  — SOV Signal Index
                    (per-axis legs; signal_index stays null until frozen formula)
                              │
                              ▼
              Council OS Lobby cards · DSH /dashboard · MCP tools · A2A agent-card
```

RWA / EAT attestation **does not invent a second board**. It attaches a **permissionless measurement card** about a public issuer/contract, then must land in the same OS+DSH verify path as every other card.

---

## GSPC 13 (+ jail) → RWA / attestation adjacency

| Axis | Live regulation / frameworks (SOV Signal) | How RWA / Option A / publishers touch it | Upgrade |
|------|-------------------------------------------|------------------------------------------|---------|
| **governance** | EU AI Act, ISO 42001, NIST AI RMF | Attestation methodology transparency; pure-opinion posture; NRSRO-adjacent hygiene | Publish methodology URL on every RWA card |
| **safety** | Art 5, NIST, ISO 42001 | Not primary for RWA funds; relevant if models grade inside engine | Keep **no model judges another** — GPU not the churn path |
| **provenance** | Art 50, C2PA | Card hash + Memo/EAS pointer = provenance of *our* opinion | Align card schema → **W3C VC 2.0** (EdDSA/ECDSA) |
| **continuity** | DORA, NIS2 | White-label engine as ICT third-party input; CRA SBOM for EU product | CRA ENISA 24/72/14 workflow before Sept 11 2026 |
| **conformance** | EU CRA, ISO 42001 | Option A licensed engine = “product with digital elements” | SBOM + vuln reporting for `/powered-by` SKU |
| **openness** | GPAI Arts 53–55 | GPAI vendor map for any model *in* the engine; enforcement Aug 2 2026 | Model portability; CoP status in procurement |
| **machinery-conformity** | Machinery Reg 2023/1230 | Humanoids POC adjacency — **not** RWA funds | Separate humanoid measurement track (MachBench) |
| **care** | Art 5 exploitation | JMWH demo: no disparagement framing | Schema: represented≠distributed flags |
| **cross-reality** | Synthetic disclosure | Future: attestation of synthetic / mirrored asset claims | Track only |
| **detector-interop** | Art 50 detection | Verify path for strangers | `/gspc-verify` + future VC status list |
| **art5-safeguard** | Prohibited practices | Not RWA primary | — |
| **swarm** | Art 14 oversight | Multi-agent Lobby / MCP; Channels later | Controlled cards, not open iframes |
| **affect** | Emotion recognition rules | Not RWA primary | — |
| **jail** (slot 14) | Containment | Custody isolation for signing keys | **HSM/KMS gate** before mainnet |

**In-lane only (not board-quotable):** `human-vs-ai`, `instrument-honesty`.

---

## SOV Signal × live regulation × RWA

| Leg | API / surface | Status | RWA cross-ref |
|-----|---------------|--------|---------------|
| Live regulation / East-West deadlines | `/api/regulation`, `/east-west/*`, MCP `east_west_board` | SHIPPED / branch | Desk geo-hint; jurisdiction for attest language |
| Hive crosswalk (15 frameworks) | `/crosswalk` | SHIPPED | Map each RWA card’s “framework touch” (MiCA, UCITS, '40 Act, VARA…) as REPORTED metadata — not a GSPC score |
| Measured AI board | `/api/gspc`, MCP `gspc_board` | MEASURED | RWA cards cite grammar; do not alter axis cells |
| Arena / Council Space | MEOK traces | PARTIAL | Optional ClaimGuard-row evidence |
| Human baseline | `/api/reported` | REPORTED | Never fuse into MEASURED; labour indices below |

**SOV Signal does not yet ingest RWA attestation corpus.** Upgrade: optional fifth leg = “public-artifact attestation density” (count of signed cards per instrument) once Stage 2 publishes — still **no fused score**.

---

## Financial axes (18–25) × RWA / tokenization

**Synthesis (Claude-aligned, this repo):** the public board is **GSPC 13 measured of 14** (slot 14 `jail` quotable but separation UNTESTED). Slots **18–25** are **financial/domain extension axes** — same Ed25519/JCS honesty grammar, deterministic grading, three-state MEASURED/UNMEASURED/… — **not** a fused “22-axis board.”

**RWA attestation is how the financial-extension track goes from canon → live public-artifact data** (adapters → engine → publishers → OS+DSH cards). Same spine as GSPC; different subject (tokenized *assets* / issuers, not AI *systems*).

| Slot / theme | Status | Cross-ref |
|--------------|--------|-----------|
| 18 Bond-crossing / Venturi | MEASURED / pilot | Bond-tokenization verification gap (IOSCO) — attestation as *input*, not issuance |
| 19 Insurance engine | PLANNED | Insurer underwriting / parametric *input* (pilots later) |
| 20 Stock market | PLANNED | Tokenized equity catalogs (Ondo Stocks / Backed) Stage 3+ |
| 21 East-West bridge | PLANNED | Already live as `/east-west` product; axis status trails the surface |
| 22 SME fractional | SPEC | Micro-issuance — out of Stage 1 |
| 23 Agent economy | PARTIAL | MCP + x402 metering of **cards**, not grades (HO.2) |
| 24 Data DAO | GAP | Arena traces marketplace — later |
| 25 EUNOMIA token | GAP | Protocol design — not company equity; not Stage 1 |

**Option B** (tokenize via Tokeny/Securitize/Archax) plugs here as partner distribution — never as us becoming the issuer (SEC taxonomy trap).

**HO.2:** verification free forever; **grades never sold**; no list prices on marketing surfaces — rates live at endpoints / after published ruling only.

---

## Human labour · humanoid labour · AI economy — honest gaps

| Ask | What exists today | What does **not** exist | Upgrade path |
|-----|-------------------|-------------------------|--------------|
| **Human labour index** | **`/indices/human-labour` UNMEASURED surface** · `/api/reported` human baselines; in-lane `human-vs-ai`; HLE/GPQA as **calibration context** | No labour-market / wage / displacement **MEASURED** score | ✅ Declared UNMEASURED; next = INDEX-METHOD-0.1 → REPORTED → MEASURED after freeze — cite AEI / ILO WP140 / WEF FoJ as **contextual signal only** |
| **Humanoid labour** | **`/indices/humanoid-labour` UNMEASURED** · `/humanoids-poc`; industry `humanoid`; MachBench / machinery-conformity | No “humanoid labour” MEASURED index | ✅ Declared UNMEASURED; Goldman/MS TAM = projections; robotics RWAs → machinery-conformity + context layer |
| **AI economy index** | **`/indices/ai-economy` UNMEASURED** · `ecosystem_index` (org catalog); narrative “AI economy” on globe | No scored economy MEASURED index | ✅ Declared UNMEASURED; Anthropic Economic Index / OECD = firewalled **context**; do not invent TVL/ARR as MEASURED |
| **RAS** | **`/products` catalog** · `/payg` · East-West packs · `/powered-by` | Metered RAS SKUs pending published ruling | Catalog live (HO.2 — no grade prices) |
| **Value Ledger** | Local East-West ledger | **publishedCount: 0** | Keep empty until real published receipts |

**Firewall rule (compass wf-5004bf4b):** labour/economy indices may appear as clearly-labeled contextual layers beside cards — **never** as inputs to SHA-256/Ed25519 deterministic grading. Hub: `/indices` · API: `GET /api/indices` · Playbook: `docs/EAT_PLAYBOOK.md` · Moves: `docs/NEXT_300_MOVES.md`.

---

## Claude Q&A — locked into architecture

| Question | Answer (binding) |
|----------|------------------|
| Data-generation **and** attestation body? | **Yes.** Generate signed corpus; we are the body that signs. Revenue = RAS + A2A/API + index + tooling/training; regulators free forever; never sell scores; never Stripe-as-grade; never issuer. |
| GPU to churn all contracts faster? | **Mostly no.** Churn = I/O (chain reads) + deterministic CPU grading. GPU only if an axis uses a model. Accelerator = **archive RPC + parallel workers**. |
| Two gates? | **Key custody** (Turnkey or AWS KMS both-curves; or YubiHSM/cb-mpc later) + **securities counsel** before any named-security verdict. Enforce `CSOAI_KEY_CUSTODY=hsm` (or equivalent) in publishers before `--publish`. |
| Moat language? | **Unsolicited + permissionless** — not merely “independent.” |
| CopilotKit rewrite? | **No.** Job-scoped only if ever; default = existing Lobby + controlled cards (`COUNCIL_OS_BUILD_PLAN.md`). |

---

## MCP / Lobby / DSH — where new work plugs in

| Existing | Plug RWA / Option A |
|----------|---------------------|
| MCP `gspc_board` … `instruments_catalog` (6 tools) | Future: `attest_facts` / `rwa_targets` tool → `MeasuredToolCard` |
| Lobby `measuredToolCards.ts` | Register new tool names → cards |
| DSH `/dashboard` | Same card hash (DSH into all) |
| `/powered-by` | Option A white-label |
| `/competitors` RWA EAT | Unsigned stubs + play class (clean / demo) |
| `/api/signal` | Later: attestation-density leg |
| Estate `/estate` | Inventory RWA publishers + custody status as crown-jewel rows |
| ClaimGuard (external) | `estate_tool` on RWA rows for public claims |

**Not in this repo (claimed elsewhere / still to land):** `EXECUTION-PLAN.md`, `CURSOR-JOBS.md`, `batch_signal_run.py`, `erc3643_attest_adapter.mjs` — treat as **job specs** to implement here under `adapters/` + `publishers/`, not as already-shipped on `instruments-catalog-7fb8`.

---

## Statistical rigor moat (GSPC board — also inherits to financial/RWA)

| Practice | Status | Competitor gap |
|----------|--------|----------------|
| Wilson score CIs on proportion metrics | In grammar / `separated_leaders` | Moody’s TIE, S&P SSA, Credora/RedStone, Particula, Chainlink ACE — **no public CI / separation disclosure** |
| Refuse “leader” on overlapping intervals (fleet-mean overlap → TIE) | Conservative anti-overclaim | MLPerf still point-estimates; we are ahead on honesty |
| Document as deliberately conservative | Needed | Overlapping CIs ≠ formal non-significance — add paired/McNemar for A-vs-B later |
| Cite Miller arXiv:2411.00640 + NIST AI RMF MEASURE | Methodology paper | Differentiator vs issuer-solicited brands |

RWA/financial cards inherit the **same** three-state honesty — UNMEASURED until graded; no invented AUM as MEASURED.

## Live regulation cross-ref (small-team path)

Already: `/api/signal` legs, East-West, hive crosswalk, geo-hint desks.  

**Upgrade:** free official feeds — EUR-Lex CELLAR SPARQL/REST, FederalRegister.gov API, eCFR API, IOSCO/BIS — with SHA-256 content-hash diffs (same primitive we already use). Paid RegTech only for gap jurisdictions. Each RWA card may carry regime tags (e.g. RLUSD→MiCA/NYDFS, Aviva→CBI UCITS, BUIDL→Reg D) as **REPORTED metadata**, not GSPC cells.

## Branch honesty — what Claude shipped elsewhere vs this tip

| Claim | On `instruments-catalog-7fb8` tip |
|-------|----------------------------------|
| ESTATE_CROSSWALK / EAT / Option A `/powered-by` page | Yes (docs + page) |
| RWA stubs + adapters seed | Partial (stubs yes; churn engine not here) |
| `/products` catalog LIVE | **Not on this tip** — treat as other-deploy until ported |
| `compliance/key-custody-decision.md` | Added under `compliance/` here |
| `batch_signal_run` / EXECUTION-PLAN / CURSOR-JOBS | **Absent** — job specs only |
| App.tsx full router | **Must restore** (tip had PLACEHOLDER stubs; split modules) |

### P0 — gates & honesty
1. Custody: AWS KMS **or** Turnkey (both secp256k1 + Ed25519); did:web key provenance; Shamir cold backup only.  
2. Counsel brief: SEC Jan 2026 staff statement + CRA unsolicited-disclaimer template + IOSCO; no named-security verdict until sign-off.  
3. Publisher `--publish` refuses without custody env.  
4. Pin `xrpl` ≥4.2.5 / 5.x + SCA.  
5. EU GPAI vendor map + CRA SBOM/ENISA for Option A engine.

### P1 — signal before outreach
6. `adapters/` per RWA slug; RWA.xyz API for address discovery; pending ≠ guessed.  
7. Parallel I/O churn (workers + archive RPC) — **not GPU**.  
8. ERC-3643/T-REX **read-only** public views → facts adapter (GPL isolation from private engine).  
9. Clean-play Stage 2: OUSG, BUIDL, Aviva/RLUSD; JMWH **demo-only** schema.

### P2 — standards & index
10. W3C VC 2.0 data model for cards; Bitstring Status List for revocation.  
11. EAS Indexing Service + XRPL Credential ingest → unified Postgres corpus (build item).  
12. Optional SOV Signal leg: attestation density (still no fused score).

### P3 — distribution (after wire + pricing)
13. AG-UI wire (`AGUI_WIRE_URL`) or refuse-closed.  
14. Option A embed + x402/PAYG metering of **cards**, not grades.  
15. Channels / Slack later — same AG-UI agent, not CopilotKit greenfield.  
16. Human labour / humanoid labour / AI economy: only as **new REPORTED** methods with doctrine — do not invent indexes.

### Explicitly defer
- Becoming regulated issuer  
- CopilotKit/Next rewrite  
- Invented Ledger rows, AUM as MEASURED, GPU-as-churn marketing  

---

## Sources

- Live: `functions/api/signal.ts`, `gspc.ts`, `finance/anatomy.ts`, `east-west.ts`, MCP well-known  
- Docs: `COUNCIL_OS_PRODUCT.md`, `COUNCIL_OS_BUILD_PLAN.md`, `EAT_DSH_ALIGNMENT.md`  
- Compass: freshness `wf-15a67236`, custody/legal `wf-7af46a56`, competitive/Wilson `wf-5004bf4b`, attestation≠tokenization `wf-b01660de`, target corpus `wf-67a7e7b4`  
- Claude strategic lock (data+attestation flywheel; GPU honesty; two gates; HO.2)  
- Gate docs: `compliance/key-custody-decision.md`, `compliance/attestation-language-template.md`
