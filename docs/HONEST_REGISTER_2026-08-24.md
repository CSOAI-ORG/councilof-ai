# HONEST REGISTER — what is real, lane-real, gated, theory (ClaimGuard-gated claim set)

**Doc ID:** `csoai-honest-register-v1` · **Revision:** 2026-08-25 (RALPH overnight)
**Purpose:** the "are we really building it" answer, row by row. Every claim is either a signed
artifact a stranger can check, or it says so. JL.5: no traction without a signed stranger-checkable
row. This register is itself a claim set — ClaimGuard-gated before external citation.

## The register (rows state exactly what is verifiable today)

| Claim | Register today | The row that flips it |
|---|---|---|
| Signed cards verify without us | **REAL** — stranger-executed VALID/INVALID verified (Stage 43); offline verifier LIVE | — (real; custody maintained) |
| Public signed board renders live `totals.public_count` | **REAL** — /api/gspc LIVE · **14 measured of 14 quotable**; jail MEASURED · separation TIE (2026-08-25) | — (real; cite live public_count — do not invent 22 axes) |
| Machine connection without asking (measure/verify/jail-probe/enter-arena) | **REAL** — handshake executed; registry entry LIVE; arena entry LIVE | — |
| Verified-dated regulation deadline API | **REAL** — /api/regulation LIVE | — |
| Corrections register as credibility reserve | **REAL** — entries appended-never-edited; /api/corrections LIVE | — |
| Signed per-axis signals + wave dashboard | **REAL** — 20 signed artifacts on master, verify PASS | — |
| Free Article 50 passport | **REAL** — POST issues ok:true, tier:free, honest stored:false | — |
| Trust root resolves for strangers (DID) | **GATED** — P0-1, owner action | INT-01: off-network resolution ×3 verified |
| Time-anchored cards ("even we can't backdate") | **LANE-REAL** — planned, disclosed | stranger-verified anchor receipt published |
| Transparency log (non-equivocation, inclusion proofs) | **THEORY** | log live + inclusion-proof API |
| Per-axis signed cards at scale | **LANE-REAL** — 14 quotable axes on live board; banks various states | identity purge + ledger exposed + key discipline; then per-axis first-row gates |
| The 8 signed financial registers | **LANE-REPORTED** — register endpoint 404 (CAT F6) | register endpoint ships serving the 8 axes |
| First-Fine Watch + Unfixed Watch | **THEORY** — routes dark | routes 200 + first dated watch rows signed |
| SOV Signal index ingesting rows | **LANE-REAL→REAL** — signal index built (9-axis ingest, signed) | index endpoint + methodology card signed + stranger-recompute |
| Expansion axes on the board (memory-poisoning, oversight) | **LANE-REAL** — repos exist, demo-scale cards signed | EXP 061/064 gates: frozen-bank hash-pin + environment disclosure + card verifies |
| Collusion/deception/sycophancy-adjacent axes | **THEORY** — scoping notes queued | each EXP gate; research note before board row |
| Signed settlement on agent-payment rails | **LANE-REAL** — repos exist; /api/receipts/latest honestly UNPUBLISHED | paid query e2e returning signed artifact + receipt |
| Insurance evidence packs moving money | **LANE-REAL** — insurer page LIVE; packs packaged | named underwriter acceptance ledgered; first premium moved |
| Marketplace trust fields adopted | **THEORY** — proposal + reference impl queued | fields live in the registry or rendered by one directory |
| MCP worker runtime matches registry 1.0.3 | **GATED** — registry LIVE 1.0.3; worker runtime still 1.0.0 | restore CF_API_TOKEN on csoai-static-deploy2 |
| csoai.org honesty/arenas Pages | **GATED** — tip 14/TIE; CDN stale since deploy-static fail 2026-08-16 | CF_API_TOKEN + Pages scope |

## The honest one-liner

The trust substrate, the board (**14 measured of 14**), the machine door, the deadline feed, and the corrections reserve are
REAL today. The axis-card mass-production is LANE-REAL pending identity purge + ledger exposure +
the gold-set gate. The economy layers (payments settled, premiums moved, fields adopted, credentials
held) are THEORY-to-LANE-REAL with named, dated flip rows — and every flip row is a signed artifact
a stranger can check, or it does not count (JL.5).

## Doctrine gates (every row)

- Measurement, not certification. Determination stays with authorities.
- Honest zeros, UNPUBLISHED, and LANE-REPORTED render; a status that cannot be checked cannot say
  LIVE.
- No internal codenames in any public row (this register uses none).
- Sim rows are labeled SYNTHETIC-SIM; the index counts measured rows, never a forecast.
- Cite live `totals.public_count`. Never remint DOIs. Never invent 22 axes.
