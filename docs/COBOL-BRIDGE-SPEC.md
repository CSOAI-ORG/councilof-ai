# COBOL Bridge — SPEC pointer

**Ours.** Canonical SPEC lives on the CSOAI-ORG sidecar (read-only; never writes core; no Starter pricing on measurement surfaces; domain 522 sits; do not attach):

https://github.com/CSOAI-ORG/cobol-bridge-mcp/blob/main/SPEC.md

COBOL Bridge is **Layer-0** on the **same public root as GSPC** (`surface=cobol.legacy`). It is **not** a competitor product. Kill any competitor framing.

`cobolbridge.ai` HTTP 522 on 31 Aug 2026. This pointer does not attach the domain. Not a GSPC cell and does not fill empty axes. Living board remains `GET https://councilof.ai/api/gspc` (**22 · 15 · 7**). Cobalt leave alone.
