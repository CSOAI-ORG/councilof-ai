# COBOL Bridge — SPEC pointer

**COBOL Bridge is ours** — [`CSOAI-ORG/cobol-bridge-mcp`](https://github.com/CSOAI-ORG/cobol-bridge-mcp).  
Kill any CSGA / competitor framing. This is a CSOAI sidecar, not a rival product.

Canonical SPEC (read-only sidecar, never writes core, no Starter pricing on measurement surfaces, domain 522 sits, do not attach):

https://github.com/CSOAI-ORG/cobol-bridge-mcp/blob/main/SPEC.md

`cobolbridge.ai` HTTP 522 on 31 Aug 2026. This pointer does not attach the domain. Not a GSPC cell. Living board remains `GET https://councilof.ai/api/gspc` (**22 · 15 · 7**). Surface `cobol.legacy` shares the same public root and does **not** fill empty axes or stamp MEASURED. Cobalt = leave alone (different product).
