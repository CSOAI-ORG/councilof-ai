# BACKLOG — TUI-6 visibility + standards (communications). 2026-09-05.

Every row carries the command that proves the row is real. A row with no proof command is not a
backlog item, it is an opinion. Rows are taken from the top; new rows are appended by the hunt.

## OPEN — found by hunting outward, 2026-09-05

| # | Row | PROOF | State |
|---|---|---|---|
| B-01 | **Three public Zenodo deposits carry RETIRED INTERNAL CODENAMES in their DOI-minted titles** — "SOVOS / SOV Space", "The SOVOS Flywheel", "The SOV Signal", all authored Templeman, Nicholas. `AGENTS.md` bans these from public output. A Zenodo title is permanent-ish: it is corrected by publishing a new version, never deleted. | `curl -sL "https://zenodo.org/api/records/?q=csoai&size=10" \| jq -r '.hits.hits[].metadata.title'` | **OWNER-ASK** — Zenodo credentials + a new version per record |
| B-02 | **The agent card does not reference the A2A signed-receipts extension.** The extension serves 200 at its canonical URL, but an agent discovering us via `/.well-known/agent-card.json` cannot find it — `ext: 0`. | `curl -s https://councilof.ai/.well-known/agent-card.json \| jq '[..\|strings\|select(test("signed-receipts"))] \| length'` → 0 | OPEN — repo change, takeable |
| B-03 | **`/api/feed.xml` is hand-typed and freezes counts.** Its top item is titled "22 axis · 22 measured"; its own comment says items are appended by hand. Superseded by the derived feeds in this lane, but the old feed still serves the frozen titles. | `curl -s https://councilof.ai/feed.xml \| grep -c "<item>"` and `grep -n "const ITEMS" functions/api/feed.xml.ts` | OPEN — retire or derive after the feeds land |
| B-04 | **Cloudflare 403s any UA prefixed `Python-urllib`, site-wide** — including `Python-urllib3`, the transport under `requests`. `urllib.request` is the zero-dependency way an agent reads a URL, against an estate whose offer is keyless reading. | `python3 -c "import urllib.request;urllib.request.urlopen('https://councilof.ai/api/gspc')"` → 403; same client with a GPTBot UA → 200 | **OWNER-ASK** — Cloudflare dashboard, not a repo change |
| B-05 | **`/honesty` hard-codes "335 signed measurement cards … all 335 verify"** in rendered page copy — a frozen count on the page that exists to state what we have not measured. | `curl -s https://councilof.ai/honesty/ \| grep -o "335 signed measurement cards"` | OPEN — outside this lane's FILE AREA, reported |
| B-06 | **No surface is confirmed live.** The spray log carries 24 `drafted` + 3 `queued`, every one owner-gated, and zero `live`. `/api/press.json` publishes `distribution_surfaces.live` as **null, not 0**, so the gap stays legible. | `jq '[.[].status]\|group_by(.)\|map({(.[0]):length})\|add' scripts/badger/_spray-log-v2.json` | **OWNER-ASK** — HF_TOKEN, kaggle.json |
| B-07 | **Wikidata Q141128616 exists with only 7 statements.** No web-feed URL, no source-code repository, no documentation URL, no official name — all of which we can source to a public artifact. | `curl -s "https://www.wikidata.org/w/api.php?action=wbgetentities&ids=Q141128616&format=json&props=claims" \| jq '.entities.Q141128616.claims\|keys'` | OPEN — statements DRAFTED, owner submits (self-editing is a conflict of interest) |
| B-08 | **`/.well-known/scitt-configuration` is 404** while `/.well-known/scitt.json` is 200. Anything probing the conventional discovery path finds nothing. | `curl -s -o /dev/null -w '%{http_code}\n' https://councilof.ai/.well-known/scitt-configuration` → 404 | OPEN — takeable |
| B-09 | **`settled_usdc` is UNMEASURED and fail-closed by design** — "null until X402_PAY_TO + a facilitator are provisioned and a receipt settles". Two doors answer 200 (`/api/x402`, `/mcp`); neither has produced a settled receipt. | `curl -s https://councilof.ai/api/revenue \| jq '.settled_usdc\|{count,status}'` | **OWNER-ASK** — X402_PAY_TO + facilitator *(both provisioned 2026-09-03; `/api/x402` reads `mode: live`; the remaining ask is a first settled receipt from a non-self payer)* |
| B-10 | **`/press` served "This legacy page is temporarily withdrawn."** Withdrawing is right for copy nobody can stand behind and wrong forever. | `curl -s https://councilof.ai/press/ \| grep -c "temporarily withdrawn"` | **TAKEN** — derived `/press/` + `/api/press.json` in this lane |
| B-11 | **C2PA "0.2 resubmission" has no evidenced first submission.** `claims-register.json` CR-012 states C2PA conformance is `planned` — "Contributor, conformance in progress. Artefacts today carry Ed25519 provenance, not C2PA conformance." Nothing in the repo records a submission at any version, so a pack calling itself a *re*-submission would assert a history that is not evidenced. | `curl -s https://councilof.ai/claims-register.json \| jq '.claims[]\|select(.id=="CR-012")'` | **OWNER-ASK** — confirm whether a submission exists and at what version; the pack is then written as a first submission or a resubmission accordingly |
| B-12 | **`/.well-known/c2pa.json` publishes no axis mapping.** It is a discovery pointer whose own note says "No axis mapping is published for this standard", so a reader arriving from the C2PA side learns nothing measurable. | `curl -s https://councilof.ai/.well-known/c2pa.json \| jq -r '.notes[0]'` | OPEN — needs a measured axis before it can say anything |
| B-13 | **No SCITT receipt exists and none can be invented.** A receipt is issued BY a transparency service on registration; we run none and are registered with none. `/.well-known/scitt.json` is `implementation_status: PLANNED`. The unsigned signed-statement construction is now derived and checkable. | `node scripts/scitt-statement.mjs --check` and `curl -s https://councilof.ai/root.json \| shasum -a 256` | **PARTLY TAKEN** — statement derived; registration is owner-gated (key + a service) |
| B-14 | **I-D -01 not yet warranted.** `draft-templeman-scitt-framing-space-00` is published and unannounced; no review feedback has arrived to incorporate, and revising a draft nobody has commented on adds a version without adding information. | `curl -s -o /dev/null -w '%{http_code}\n' https://www.ietf.org/archive/id/draft-templeman-scitt-framing-space-00.txt` → 200 | HOLD — revise on first substantive review, not on a schedule |
| B-15 | **The agent card's signing input described a card nobody serves.** `agent-card-jws-input.json` is `JCS(card minus signatures)` prepared for GHA OIDC. The card was edited and the generator never re-run: the served card said `csoai-gspc-mcp@0.2.1` / "seven free readers plus four x402-metered tools"; the signing input still said `0.1.0`. Signing it would have produced a signature over bytes nobody serves. | `python3 scripts/adapters/agent_card_jws.py` then `git diff --stat public/interop/agent-card-jws-input.json` | **FIXED** — regenerated + JCS gate (#1396) |
| B-16 | **`/.well-known/agent.json` is a byte-identical alias of the card and had diverged.** Two discovery paths serving different cards is worse than one: an agent resolving the alias verifies a signature computed over the other file's bytes. | `cmp public/.well-known/agent.json public/.well-known/agent-card.json` | **FIXED** — synced + asserted (#1396) |
| B-17 | **The derived feeds were undiscoverable.** After shipping four feeds: `robots.txt` 0 mentions, `llms.txt` 0, `sitemap.xml` 0, and the homepage's only `<link rel="alternate">` pointed at the hand-maintained `/feed.xml`. Exactly the defect `functions/feed.xml.ts` documents: "a return path that cannot be discovered is the same as no return path." | `curl -s https://councilof.ai/ \| grep -c 'rel="alternate"'` and `curl -s https://councilof.ai/llms.txt \| grep -c feeds/` | **FIXED** (#1400) |
| B-18 | **`functions/api/_authCrypto.test.ts:74` is flaky in CI.** Failed a `gates` run on an unrelated docs/HTML PR, expecting a decoded `{name, sub}` and receiving `null`. Passes locally against master's own copy, and passed on re-run with **no code change** — so it is timing/clock-dependent, not a real regression. A flaky gate teaches lanes to re-run instead of read. | `npx vitest run functions/api/_authCrypto.test.ts` (green locally); `gh run rerun <id> --failed` on a red one goes green | OPEN — not this lane's file, reported |
| B-19 | **The git stash stack is shared across every worktree and is a cross-lane hazard.** A `git stash pop` in one lane's worktree popped and dropped another lane's entry (`WIP on hermes/h15-next100`); it was restored with `git stash store <sha>` from the drop message. Nothing warns, and the reflog line is the only recovery path. | `git stash list` — entries from other branches are visible from every worktree | OPEN — convention: read `git stash list` before any stash operation, or never stash in a shared repo |

## Rules this backlog obeys

- **Absent is not zero.** `surfaces live` is null while nothing is confirmed live; it does not
  become 0, because 0 reads as a measured result.
- **An owner-ask is a row, not a blocker.** It gets one line naming the credential and what it
  unlocks, then the next item is taken.
- **Nothing is posted under CSOAI's name on anyone else's repo, list or registry.** Wikidata
  statements, the OLP review and the I-D announcement are drafts; the owner sends.
