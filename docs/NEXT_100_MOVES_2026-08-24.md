# CSOAI — NEXT 100 MOVES (2026-08-24, post pod-reboot recovery)

**Doc ID:** `next-100-2026-08-24` · **Revision:** 2026-08-24
**Status:** grounded on the live verified state. The signed per-axis Elo leaderboard engine
is deployed and the verify path is proven working (`?verify=1` → match:true, cross-runtime
byte-exact). This batch's focus: resilience (pods reboot, SSH ports change), the learn-more
loop (competitive gap is still open), and revenue rails.

**Learnings to bake in (this batch):**
- Pods reboot + RunPod reassigns SSH ports (A100 23166→14954, sink 25804→33982) — scripts
  must auto-discover ports or be updated; /tmp state is ephemeral (scoreboard → /workspace).
- Latent bug: `publish_scoreboard.py` used its own `ensure_ascii=True` canon while the verify
  path uses `ensure_ascii=False` — content_id never matched. Fixed (PR #414).
- Sibling lane can push committed merge conflicts (PR #415 App.tsx) that break every deploy —
  the deploy timeout guard (PR #395) + a conflict-guard are essential.

**Doctrine:** measurement-not-certification · nobody-ranked-pays · check-never-assume ·
corrections-appended-not-edited · no banned codenames public · signing key never travels ·
pod-canonical (engine + monorepo on runpods, Oracle backup).

---

## PHASE A — Resilience + self-healing (1-25) [priority this batch]
1. Add a project CI guard that FAILS on any committed `<<<<<<< / >>>>>>>` marker in
   src/ (so a conflict push can never break the build again).
2. Auto-discover RunPod SSH ports (query GraphQL runtime ports) instead of hardcoding —
   scripts survive any pod restart.
3. Make the A100 boot.sh a launch-supervised/cloud-init auto-start (engine + auto-loop
   relaunch on every restart, idempotent).
4. Keep the signed scoreboard in /workspace (persists across reboot), not /tmp.
5. Persist pynacl requirement — a bootstrap that pip-installs on start if missing.
6. Health monitor cron (every 10 min) + alert (already GREEN: axis RUN, loop RUN, verify TRUE).
7. Weekly pod-restart smoke test — simulate a reboot, confirm engine + loop auto-return.
8. Add mfs-write-degradation guard: if /workspace write fails, fail loudly + alert (the
   infra quirk from 2026-08-23 — reboot fixed it, but guard against recurrence).
9. Cross-runtime canon self-test in CI (Python cjson == JS canonize on a fixture) so a
   canon regression is caught at build, not at runtime verify.
10. Backup-integrity test: restore from Oracle, recompute content_id, confirm match.
11. Failover: if A100 dies, 3090 (23243) or Oracle has the corpus + monorepo has the ledger.
12. Round-store redundancy: mirror arena_rounds.jsonl to sink RAG + Oracle each cycle.
13. Pod-canonical check: assert no critical artifact exists only on the Mac.
14. Scoreboard archival: keep per-generation snapshots so drift is reconstructable.
15. Engine health dashboard: throughput, clean-rate, errors, signed-cards per cycle.
16. Alerting: pager/webhook on engine stall or verify fail.
17. Daily signed summary: new rounds, per-axis Elo deltas, verify status (webhook report).
18. Deterministic EOF -> measure->package->sign->publish->deploy->verify->backup all on-pod.
19. Continuous parity test: recompute reference Elo from recorded rounds, assert published
    score matches (integrity gate).
20. Boot-time fail-fast: assert key present + pynacl importable before publishing.
21. Model-library refresh loop: pull new open weights, re-measure, append.
22. Per-generation drift report (the unique longitudinal product).
23. Cost/telemetry: track A100+3090 spend vs leaderboard signups ($/hr discipline).
24. Open-source the harness (MIT) so methodology is auditable (moat = verifiable).
25. Phase-A exit: a resilient, self-healing, pod-canonical engine that survives any reboot.

## PHASE B — Learn-more loop (competitive gap) (26-45)
26. Weekly OpenRouter model-list re-pull; diff for new models/providers/pricing.
27. Weekly LMArena board-state re-pull; diff.
28. New-capability detection (alert on new feature).
29. Gap auto-detect (new benchmark axis we don't measure).
30. Competitive memo refresh monthly (arena-competitive-watch).
31. Design-arena/intelligence-index cross-check vs our per-axis scores where comparable.
32. Adoption-vs-quality note (OpenRouter rank = adoption, not measured capability).
33. Style-bias study (quantify crowd style bias vs our length-control).
34. New-axis research (agentic autonomy, tool-use safety) + add measurement banks.
35. Contamination watch (published score vs training-data contamination).
36. Groundedness/retrieval axis (hallucination-resistance).
37. Human-baseline axis (reported aggregates alongside measured; DPIA-gated).
38. Cross-lab run (3090 runs same bank for consistency).
39. Benchmark-registry listing (mcp.so / Glama / Kaggle; owner-gated).
40. Quarterly parity report (our Elo vs LMArena general Elo, domain resolution).
41. Confidence audit (every signed surface scans clean).
42. Crowd-verification beta (public "verify a model here" submit flow).
43. Institutional outreach (academic eval labs; owner-gated).
44. One peer-style result (per-domain signed Elo resolves what general Elo collapses).
45. Phase-B exit: automatic learn-more loop with weekly intel + gap alerts.

## PHASE C — Revenue rails (doctrine-safe legs) (46-70)
46. Enterprise data license (signed per-axis Elo corpus; buyer licenses data, not ranked).
47. Data-licensing tiers/pricing on /legal/licensing.
48. Researcher subscription (signed, per-axis, longitudinal dataset + replay harness).
49. Regulator attestation package (signed evidence, NOT certification).
50. Insurer evidence pack (PR #324 wired to live leaderboard; 4 classes).
51. Domain packs (finance, healthcare, defense) signed per-axis scoreboards.
52. "Your model here" tool (pick provider/model -> signed per-axis scorecard).
53. LMArena cross-reference page (our scores next to LMArena general Elo).
54. Vendor/enterprise pilot (1 design-partner; owner-gated legal).
55. 2 case studies (published ROI stories with signed receipts).
56. Free public tier (leadership + verify demo stays free).
57. Reputation flywheel (linkable signed scores; "prove it yourself" demo).
58. Crossref/DOI for the dataset (citeable identifier).
59. Benchmark-lab outreach (owner-gated).
60. One research note showing per-domain resolution.
61. SEO for "verified AI leaderboard" / "per-domain Elo".
62. Revenue telemetry (signups vs verify-demo traffic funnel).
63. GRC reseller channel (integrations resell per-domain scoreboard).
64. OpenRouter pricing counter-tool (workflow cost vs gateway margin).
65. Cross-site embed widget (verified scoreboard + live verify link).
66. Public trust ledger (append-only list of verifications issued).
67. Confidence labeling (every score: n, CI, design-vs-measured).
68. Signed dataset v0.2.0 (per-axis Elo + CIs, re-signed).
69. Community open-source engine (MIT per estate precedent).
70. Phase-C exit: paid data-license + researcher + regulator package on doctrine-safe legs.

## PHASE D — Continuous improvement (71-100)
71. Auto-batch cycle: RUN->TEST->AUDIT->IMPROVE->RUN (daily, from pods).
72. Weekly full audit: text, canon, banned tokens, dead links, 370-page sweep.
73. Monthly signature-chain audit (all 335 cards + ledger + board).
74. Pod health + RAG volume integrity; fleet model roster refresh.
75. Standing cadence: AISI #4413 daily, Unitree A3, arXiv G6Y9SY (HARD Aug 27), insurer 30 Sep.
76. Growth Lab submit (27 Sep) — owner.
77. BSI ART/1 seat pack (owner-gated, £0).
78. IETF scitt/agent2agent engagement post (send).
79. SCITT registration: pod-side scitt-ccf-ledger on a pod; register /api/gspc +
    /api/regulation snapshots.
80. A2A registry verify (DNS TXT a2a-registry-verify) — owner.
81. Board membership doctrine check (C2PA, OpenSSF, OWASP, AI Verify).
82. Endorser email to Moon; right-of-reply (counsel 11 Sep).
83. SEIS/EIS H-5 draft (counsel-timed).
84. EIC NO-GO decision (2 Sep).
85. AI Assurance Innovation Fund monitor (NOT OPEN; NPL Centre alignment).
86. MLab/HPCA seed grant screen.
87. DOI/zenodo DOI manifest + MCP listing (D70, sibling shipped).
88. Live attestation register refresh (board_living, 335 cards).
89. Brand-gate sweep (banned codenames) after any content change.
90. Benchmark-quality register refresh.
91. MCP registry sync (mcp.so listing).
92. x402 payments rail check (owner-gated).
93. SMITHERY gate (owner: Stripe keys, npm 2FA).
94. Growth Lab + insurer pitch prep (30 Sep).
95. Counter-canon enforcement (measurement not certification) audit.
96. Firewall 2 + EZ firewall re-verify after any new surface.
97. Live attestation register refresh + corrections ledger on new errors.
98. New OOWM weights pull + re-measure (append-only).
99. Full estate integrity re-verify (canon, banned tokens, dead links).
100. Phase-D exit: a self-improving, pod-canonical, resilient verified-leaderboard engine.

**Near-term priority (this batch):** PHASE A items 1-25 (resilience + self-healing — the
pod reboot + port change + conflict guard are the immediate lessons), then PHASE B.
