# CSOAI independent run — Vaara SEP-2828 conformance corpus
**Runner:** Council of AI (CSOAI Ltd, UK 16939677) · run executed 2026-08-25 ~05:20 UTC
**Run kind (Iman's vocabulary):** reproduction of author-supplied checkers
**Source:** github.com/vaaraio/vaara @ commit `9fefe51a61f16dc13cd64ca8ca4b8792e48fb64b` (depth-1 clone)
**Corpus:** conformance/sep2828 VERSION 1.0.0
**Environment:** macOS Darwin 25.5.0 arm64 · Python 3.14.6 · stdlib only (corpus imports no Vaara code)

## Results
1. **Bytes verification:** MANIFEST OK — 32 files match; corpusDigest
   `sha256:0baa437da95d6ffb6bda3185d657385b7738c79daba77819acc0c6280ac0ed7a`
2. **record_conformance_v0:** 10/10 cases matched expected (incl. all negative vectors:
   malformed backlink digest, missing signature, not-an-object, unsupported alg)
3. **record_set_v0:** 7/7 sets matched expected (clean, decision-without-outcome,
   duplicate-call, executed-gap, mixed-nonconforming, outcome-without-decision, proper-pair)
4. **Exit code:** 0 — PASS: all 2 suites matched expected.

No divergence found. This is a reproduction run, not an independent implementation;
we state its kind rather than let a table row imply more.

## Addendum — canonical runner (same commit, same machine)
`scripts/conformance_runner.py` (deps: cryptography, rfc8785, in a clean venv):
**43 suites passed, 0 failed, 3 skipped (75 cases) across 46 suites** — skips are the two
optional-dependency suites (pq_hybrid_v0, qualified_time_v0) plus one, stated by the runner
itself. Suites exercised include `x402_settlement_v0` and `sep2787_attestation_v0`.
PASS: every suite that ran matched its expected verdicts.

## About the runner (for context, not for the row)
Council of AI is an independent AI measurement body (legal entity CSOAI Ltd, UK Companies
House 16939677). We publish a signed measurement rail in the same dialect this corpus uses:

- **Ed25519 over RFC 8785 (JCS) canonical JSON**, content-addressed, append-only — a
  correction is a new record, never an edit.
- **Three-state verdicts**: a slot with no completed measurement is served as UNMEASURED —
  an answer, never a zero and never interpolated. Live board counts are always read from
  the API, never quoted from prose: https://councilof.ai/api/gspc
- **Verification is free and loginless**, and runs client-side:
  https://councilof.ai/gspc-verify
- Deterministic grading only; no model judges another model anywhere in a scoring path.

What we do **not** claim: certification, accreditation, or enforcement of anything —
measurement only, and this run is a reproduction of author-supplied checkers, stated as such.
