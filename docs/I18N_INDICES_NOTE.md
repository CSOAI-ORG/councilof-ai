# i18n · indices / products (NEXT_300 #218)

**N/A for OpenGridWorks Dict** — `client/src/i18n` / `client/src/lib/i18n.ts` is scoped to the **OpenGridWorks** map chrome (framework atlas). Honesty surfaces (`/indices`, `/products`, `/powered-by`) ship **English-first** with `document.title` + `setMetaDescription` / `setOgMeta`.

No separate i18n keys added for labour MEASURED scores (there are none — UNMEASURED). Future locale packs must preserve UNMEASURED / scores-never-sold wording.
