# Show, don't tell — the 2-minute script (x402 family, tag @coinbaseDev)

**Hook (0:00–0:15):** "The agent economy has a payment protocol. Nobody measured whether it pays."

**The one finding (0:15–1:00):** On screen: the live numbers from the deterministic census.
"316 x402 hosts. A correctly signed payment went to every one. **100 delivered.** 213 refused
the payment. **13 took the money and delivered nothing.** The protocol is young — and the
conformance rate is the thing a protocol needs to see. So we measured it, signed it, and put it
on chain: every settlement, every refusal, every take-and-refuse row."

**What we are (1:00–1:40):** "We are Council of AI — a neutral measurement house. Signed cards
over 22 axes, Merkle-rooted, witnessed on the public record; nine paid doors, all x402, USDC on
Base. One of them is the receipts endpoint the audit world has never had. When you `circle
services inspect councilof.ai`, the payment requirements come back clean — the protocol works,
and our doors work with it."

**Ask (1:40–2:00):** "If you build a service with x402, the market you're joining is thin right
now. We publish the time series every week. Follow the measurement."

**Format:** ≤120 s · landscape · captions · the census table as text (legible at 240p) ·
end-finisher: the URL + "@councilofai · 60 seconds to verify".

**Assets used:** raw census table (scripts/grants/x402-settlement-census.py output) · one card
merkle root · the `circle services inspect` terminal screenshot (already captured, payable/$0.1
USDC/Base/our payTo) · the one_number page.

**Doctrine:** no forecasts, no "token will go up", no prices beyond the census spend (1.34 USDC
spent, stated as cost), self-settlements labelled "self" if shown. Measurement, not endorsement.

— JEEVES draft, 06 Sep 2026. Owner OAuth/X post is the only gate; the assets are all captured.
