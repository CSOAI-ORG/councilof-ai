# HF method paper card — DRAFT (NEXT_300 #365)

**Status:** SPEC · **not published** until INDEX-METHOD freeze (#362 gate).

Use this as the dataset-card body when promoting `docs/SOVOS/INDEX-METHOD-0.1.md` to a frozen method — never before.

```yaml
---
license: mit
tags:
  - council-of-ai
  - index-method
  - unmeasured
  - measurement-not-certification
pretty_name: CSOAI INDEX-METHOD (labour & AI-economy)
---

# INDEX-METHOD — labour & AI-economy indices

**Register today:** UNMEASURED on councilof.ai (`GET /api/indices`).

This card describes the **method** for future REPORTED → MEASURED promotion. It is not a score product.

- Live surfaces: `/indices` · `/indices/ai-economy` · `/indices/human-labour` · `/indices/humanoid-labour`
- Firewall: contextual only — never GSPC SHA-256 / Ed25519 cell inputs
- `measured_score`: null until bank freeze + usable n

Method source: `docs/SOVOS/INDEX-METHOD-0.1.md` (branch until master merge).

Do not read Hub `MEASURED-INDEX-v0.1` experiment files as permission to invent OS product scores — see `docs/HF_LABOUR_INDEX_HONESTY.md` and correction `C-2026-0826-01`.
```

Publish target repo (after freeze): `csoai/index-method-labour-economy` or section inside `csoai/labour-economy-unmeasured`.
