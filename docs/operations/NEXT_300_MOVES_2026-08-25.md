# NEXT-300 MOVES — refreshed 2026-08-25 (supersedes 2026-08-22, state-marked)

Same 7 phases as v2026-08-22; STATE = what changed since (DONE / OPEN / OWNER /
NEW). Full per-move text lives in the 08-22 doc + OWNERSHIP-100 + NEXT-100.

## Phase 1 · MAC SORT + RAG VOLUMES (1-40)
OPEN (owner-adjacent — heavy-data migration to RunPod sink pod 213.173.105.83,
currently unreachable at write time). Pods endpoint-drift fixed this week (resolve
via runpodctl); sink pod still dark → migration queued until it returns.

## Phase 2 · VERIFICATION THRESHOLD (41-70)
- DONE this week: jail separation TIE → **14-of-14** ; board re-sign (stale-sig
  repair) ; stranger-recomputable control-facts v2 (Wilson + signed).
- OPEN: portable verifier (`verify_signature.py`), transparent-log anchor
  (RFC 6962), SCITT registration (RFC 9943 §registration), determinism re-run gate.

## Phase 3 · REVENUE RAILS (71-110)
- DONE: six revenue legs live-verified (claimguard, data license, corpus 10,226
  records, Article 50 passport, PayPI dataset 200 + Zenodo, checkout path).
- OPEN: Stripe price IDs (owner — "unknown product_id"), pricing ruling Move 211,
  enterprise pilot outreach, foundation donation EXP 211.
- NEW (strategy pass): white-label engine CRA-readiness (SBOM ✅ shipped; ENISA
  workflow duty-holder before 2026-09-01).

## Phase 4 · SCITT + STANDARDS (111-150)
- OPEN: RECEIPT-SPEC I-D submission, application/agent-measurement+json media type,
  MCP #426 fix PR, AIUC-1 (Sep 30) + C2PA + A2A + OSFF engagement, PQC note
  (ML-DSA-65), cross-jurisdiction crosswalk (ISO 42001 / Art 17 / Decree 142 / SB 315).

## Phase 5 · PRODUCT + ESTATE (151-200)
- DONE this week: RWA attest line scaled (corpus 18 entries + intel flags, control-
  facts v2 signed, core data on HF `csoai/rwa-attest`), CRA/GPAI/SBOM compliance docs,
  EAT playbook (this doc set), measurement signal index (15 rows).
- OPEN: financial-axes 0.2 published ✅ but the 3 index axes (AI-economy, human-
  labour, humanoid-labour) — **declared UNMEASURED with rubric + bank NONE**; the
  next campaign = assemble their public input banks → measure → sign → live.
- OPEN: locate remaining XRPL issuer addresses (Aviva, DCP, EURCV, JMWH currently
  "not-located") via explorer search → then measure (pipeline is ready).
- OPEN: EVM control-facts rubric (BUIDL/BENJI/ACRED contracts).

## Phase 6 · MINE + LEARN (201-250)
- DONE: 335-card chain adjudicated from bytes (manifest-consistent; publish rule
  = owner) ; honey/arena loops live.
- OPEN: 19-model board-fleet jail run (7-model fleet honest note in place);
  gold-bank dataset → HF (frozen on pod).

## Phase 7 · CONTINUOUS IMPROVE-TEST-AUDIT (251-300)
- OPEN (from OWNERSHIP-100): all 16 axes × cap-markets/insurance/sovereign/health
  (domain packs), data licensing lines, correction-receipt runbook, kill-switch +
  cold-restore drills, annual transparency report, external reviewers on card spec.
- NEW rules in play: JMWH demo-only flag vocabulary; "represented ≠ distributed"
  first-class field; CRA/GPAI procurement checks; portability rule + agent cost caps.

## Top-15 next moves (now, agent-doable)
1. Assemble AI-economy index input bank (public series → signed first index).
2. Locate Aviva/DCP/EURCV/JMWH XRPL issuer addresses → extend control-facts (→ "8 xrpl").
3. EVM control-facts rubric + measure BUIDL/BENJI/ACRED.
4. Wire `/attest` + `/financial-axes` + `/indices` (declared UNMEASURED cards) in the
   front-end (Cursor, from os-production).
5. Product catalogue: licence/evidence/attestation products linked + priced DRAFT.
6. Homepage + header nav alignment sweep (grammar 14-of-14, emerald brand).
7. Portable verifier + `/verify` stranger-checkable links on every signed artifact.
8. Gold-bank dataset → HF (unblocks jail dataset field).
9. Standards: RECEIPT-SPEC I-D + media type + MCP #426 (AIUC-1 cadence).
10. Rekor anchor + SCITT registration.
11. Dependabot/SCA on all runtime deps (CRA prep) + npm 2FA automation tokens.
12. Correction-receipt supersede chain + incident runbook (before 2026-09-01).
13. 19-model fleet jail run (when pods free) → separation SEPARATED/TIE re-test.
14. Kill-switch + cold-restore drills (key revocation → clients fail closed).
15. Domain-coverage matrix publish (who measures what — the SEO anchor).
