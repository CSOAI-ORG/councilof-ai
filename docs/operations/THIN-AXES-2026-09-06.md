# Thin axes (n<30) — written reasons, 6 Sep 2026

Live source: `GET https://councilof.ai/api/gspc` captured tonight. These are **deterministic-facts** axes (issuer accounts / public series), not model-comparison banks. Breadth mill of HF2200 models does not feed them. n<30 remains **unquotable** as a comparison result.

| axis | n | why n is this size | why not a model mill |
|---|---|---|---|
| provenance-controls | 6 | `len(measured)` issuers in the signed financial run (`harness/rwa-attest/financial_four_measure.py` ISSUERS). | Facts from public XRPL issuer accounts. No fleet, no leader, no accuracy. |
| reserve-attestation | 16 | top-level `n` on the content-addressed run | Same family. Unsigned content-addressed artefact. |
| regulatory-framework | 16 | top-level `n` | Same family. |
| distribution-integrity | 16 | top-level `n` | Same family. |
| custody-disclosure | 16 | top-level `n` | Same family. |
| ai-adoption-components | 2 | two public statistical series in the component-facts run (not a restored index sticker; C-2026-0826-05) | Component facts. Adding HF slugs would be a category error. |
| labour-components | 2 | two public series | Same. |
| humanoid-labour-index | 8 | eight component facts | Same. |

Recomputability of n and `content_id` is declared in `docs/operations/FINANCIAL-AXIS-RECOMPUTABILITY.md`. Expanding n means adding **issuers or series** to that producer, then a supersede pass (signed bytes are never edited).

HF2200 overnight mill (`writes_board: false`, `enters_board_means: false`) must not be cited as feeding these axes.
