# BODY-RUNBOOK — the estate that runs itself (derived from .github/workflows)

> Generated by scripts/badger/generate-body-runbook.py on 2026-09-05T12:31:18Z.
> Row-by-row: workflow file (HEAD), cadence read from the file, the pipeline stage it serves.
> Nothing here is typed from memory. Rerun the generator after any workflow change.

| stage | workflow | cadence | trigger | file |
|---|---|---|---|---|
| measure | Reg-watch — regulation-change detector (living loop) | cron 45 6 * * * | schedule | `.github/workflows/reg-watch.yml` |
| measure | auto-eat-sign | cron 17 * * * * | workflow_dispatch | `.github/workflows/auto-eat-sign.yml` |
| measure | census-delta | cron 30 4 * * * | schedule | `.github/workflows/census-delta.yml` |
| measure | eat-overnight | cron 47 */2 * * * | schedule | `.github/workflows/eat-overnight.yml` |
| measure | hf-fin-shells | cron 27 */2 * * * | schedule | `.github/workflows/hf-fin-shells.yml` |
| measure | hf-fin-shells-measure | on push / manual | workflow_dispatch | `.github/workflows/hf-fin-shells-measure.yml` |
| measure | hf-inference-mill | cron 17 * * * * | schedule | `.github/workflows/hf-inference-mill.yml` |
| measure | hf-token-scope-probe | on push / manual | workflow_dispatch | `.github/workflows/hf-token-scope-probe.yml` |
| measure | hub-queue-mill | cron 41 * * * * | schedule | `.github/workflows/hub-queue-mill.yml` |
| measure | mine harness (repo smoke) | on push / manual | push | `.github/workflows/mine-harness.yml` |
| measure | provider-watch — hash-only provider document change watcher | cron 20 5 * * * | schedule | `.github/workflows/provider-watch.yml` |
| measure | public-root-watcher | on push / manual | push | `.github/workflows/public-root-watcher.yml` |
| sign | Claims E2E (live truth check) | cron 0 6 * * * | push | `.github/workflows/claims-e2e.yml` |
| sign | Sov Stack E2E (agentic globe/sim/hive) | cron 30 6 * * * | push | `.github/workflows/sov-stack-e2e.yml` |
| sign | Verify signed measurement card | on push / manual | push | `.github/workflows/verify-card.yml` |
| sign | reject-335-board | on push / manual | push | `.github/workflows/reject-335-board.yml` |
| flip | hub-queue-flip | on push / manual | push | `.github/workflows/hub-queue-flip.yml` |
| flip | hub-queue-land | on push / manual | push | `.github/workflows/hub-queue-land.yml` |
| flip | sticky335-land-atomic | on push / manual | workflow_dispatch | `.github/workflows/sticky335-land-atomic.yml` |
| anchor | public-root | cron 7 * * * * | schedule | `.github/workflows/public-root.yml` |
| deliver | Build + deploy site (prerender + fields + tour → both domains) | cron 20 */3 * * * | push | `.github/workflows/deploy.yml` |
| deliver | csoai-site deploy — full static site from csoai-static-deploy2 main | cron 15 7 * * * | workflow_dispatch | `.github/workflows/csoai-site-deploy.yml` |
| ledger | Drift guard — live site vs ruled canon | cron */30 * * * * | schedule | `.github/workflows/drift-guard.yml` |
| ledger | evidence-smoke | cron 17 */6 * * * | push | `.github/workflows/evidence-smoke.yml` |
| ledger | honest-board-floor | on push / manual | push | `.github/workflows/honest-board-floor.yml` |
| ledger | hub-index-drift | cron 40 5 * * * | schedule | `.github/workflows/hub-index-drift.yml` |
| ledger | inspect adoption gate | on push / manual | pull_request | `.github/workflows/inspect-adoption-gate.yml` |
| ledger | protect-verified-335 | on push / manual | workflow_dispatch | `.github/workflows/protect-verified-335.yml` |
| ledger | protect-verified-board | on push / manual | workflow_dispatch | `.github/workflows/protect-verified-board.yml` |
| report | Board attestation key — one-time setup | on push / manual | workflow_dispatch | `.github/workflows/board-key-setup.yml` |
| report | gspc-nightly-board | on push / manual | workflow_dispatch | `.github/workflows/nightly_board.yml` |
| report | regulator axis map (false-UNMEASURED guard) | cron 17 6 * * * | push | `.github/workflows/regulator-axis-map.yml` |
| guard | One-door guard — AG UI is Council OS | on push / manual | pull_request | `.github/workflows/one-door-guard.yml` |
| guard | conflict-guard | on push / manual | pull_request | `.github/workflows/conflict-guard.yml` |
| guard | crawler-view-gate | cron 17 6 * * * | push | `.github/workflows/crawler-view.yml` |
| guard | lane-guard | on push / manual | pull_request | `.github/workflows/lane-guard.yml` |
| guard | pr-gates | on push / manual | pull_request | `.github/workflows/pr-gates.yml` |
| other | Outsider walk — D13 stranger verification + machine contracts | cron 15 7 * * * | schedule | `.github/workflows/outsider-walk.yml` |
| other | Persona gauntlet — 8 personas vs live canon | cron 15 */2 * * * | schedule | `.github/workflows/persona-gauntlet.yml` |
| other | Wave11 assemble card_index | on push / manual | workflow_dispatch | `.github/workflows/wave11-assemble.yml` |
| other | app-router-floor | on push / manual | push | `.github/workflows/app-router-floor.yml` |
| other | assemble-align-app | on push / manual | push | `.github/workflows/assemble-align-app.yml` |
| other | assemble-align-rm | on push / manual | workflow_dispatch | `.github/workflows/assemble-align-rm.yml` |
| other | eval-ci | on push / manual | push | `.github/workflows/eval-ci.yml` |
| other | patch-app-jad2 | on push / manual | workflow_dispatch | `.github/workflows/patch-app-jad2.yml` |

## Pipeline the workflows serve

request (free door/402) → measure → sign → flip → anchor → deliver → ledger → report

- **measure** — the mills and censuses (HF inference mill, hub-queue mill, mine-harness, auto-eat-sign, census-delta, provider-watch, reg-watch).
- **sign** — OIDC signer + claims/e2e + reject-335 (serious: protects verified corpus).
- **flip** — hub-queue-flip/land: promote staged leaves into the published queue.
- **anchor** — public-root (+ watcher): 4×/day root ceremony; witness integrity fails closed.
- **deliver** — deploy (Build + deploy site, both domains; scheduled cron 20 */3 * * *) + csoai-site-deploy.
- **ledger** — evidence-smoke, drift-guard, protect-verified-*, inspect-adoption, honest-board-floor.
- **report** — nightly_board, regulator-axis-map, reg-watch, printer-board, provider-watch.
- **guard** — lane-guard, conflict-guard, one-door-guard, crawler-view, claims-e2e, eval-ci, selftest.

## How to read a row

1. `cadence` is read from the workflow's own `schedule:`/`periodic:` block — if a stage shows
   'on push / manual' it was never scheduled; that is honest, not a gap.
2. `trigger` is the `on:` key. `schedule` workflows only run at their cron.
3. Live status per workflow: `curl https://councilof.ai/api/body-state` (last run, last success, last failure).

## Stage truth

- The 402 rail answers at `/api/x402` (free door `/.well-known/x402`). Paid artefacts unlock on settle.
- `measurement, not certification` — every stage publishes evidence, never conclusions.