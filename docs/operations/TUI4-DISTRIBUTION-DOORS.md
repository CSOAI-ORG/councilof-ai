# TUI 4 — distribution doors (not more MCPs)

29 Aug 2026. Card never changes. Lifestyle MCPs are not the product.

## Brief vs bytes

| Claim | Bytes |
|---|---|
| Official MCP Registry is empty for GSPC | **False.** `io.github.CSOAI-ORG/gspc` **1.0.3** is `isLatest` on [registry.modelcontextprotocol.io](https://registry.modelcontextprotocol.io/). |
| That entry is the right door | **False.** Remotes are `https://councilof.ai/api/assess` (classifier, not the four tools) and `csoai-gspc-mcp.nicholastempleman.workers.dev/mcp`. Copy still says **14 of 14**. Repo is `csoai-static-deploy2`, not `councilof-ai`. Live four-tool server is **`https://councilof.ai/mcp`**. |
| `npx csoai-gspc-mcp` is a lie | **False.** npm `csoai-gspc-mcp@0.1.0` exists. Pin 0.1.1 still needs Owner OTP. Do not republish as if unpublished. |
| Glama lists lifestyle, not GSPC | **Partial.** Glama has `CSOAI-ORG/councilof-ai` (stale 13-axis / schema 0.3). `csoai-gspc-mcp` 404. Fishkeeper-class servers are also listed. Own-goal is **wrong remote + stale count**, not “no listing”. |
| marketplace.json GitHub source | **True miss.** `.claude-plugin/marketplace.json` and `.cursor-plugin/marketplace.json` have `"source": "./"`. Claude catalog add wants `{ "source": "github", "repo": "CSOAI-ORG/council-of-ai-grok" }`. TUI 3. |
| Codex dir missing | **Partial.** `.codex-plugin/` exists. `.codex` does not. |
| Homepage hides inspect/claimguard/corpus-watch | **Partial.** Footer already links PyPI `inspect-signed-receipt`. `claimguard` / `corpus-watch` not on the home tools strip. TUI 2. |
| `/methodology` 308 | **308 → 200** at `/methodology/`. Trailing-slash, not a dead page. |
| Seven Art 50 MCPs | True duplication. **Do not silently switch canonical.** Prior TUI 4 keep-one: `csoai-c2pa-durable-mcp`. This brief says `c2pa-watermark-mcp`. Owner picks **one**; TUI 4 will not list two as “the” path. HMAC MEOK is not card verify. |
| OpenShell cage | **Not in this monorepo.** `pins.json` says “Hermes inside OpenShell”. Do not bid a second cage. Membrane/Nono are not extra products this weekend. |
| Key custody / mainnet | Held. No `board-attestation-2`. No mainnet verdict. |

## Permissionless holes that remain (ranked)

1. **Republish registry 1.0.4** — remote **only** `https://councilof.ai/mcp`; description 22·15·7; four tools; drop `/api/assess` from GSPC namespace. DNS TXT / GitHub namespace if the publisher requires it. TUI 3 + Owner DNS.
2. **marketplace.json** GitHub `source` + Claude directory form. Do not claim catalog install while **xai-org/plugin-marketplace#398** is OPEN. TUI 3.
3. **npm 0.1.1** if stdio client must pin a newer tarball — OTP is Owner. 0.1.0 is already the `npx` target.
4. **Homepage four OSS cards** — inspect-signed-receipt, claimguard, corpus-watch, signed-receipts. TUI 2. Not fishkeeper.
5. **HF MCP badge** on the personal Gradio space. Org Space 402 until Team. TUI 3.
6. **Emilia** consume `e507acdf` with **our** verifier. No IETF session. TUI 3.
7. Scorecard / provenance CI on the **four** load-bearing repos only. Not 500 MCPs. TUI 1/3.

## Pin, do not merge (catalogue only)

New names (Agent Governance Toolkit, ConductAI, mcp-guardian, agentgateway, AI Provenance Protocol, FROST) go in **TUI 3 `pins.json`**, never in card-v1, never as bindings.json v0 row 13. Bindings **stay 12** until a versioned v0.2 is explicit.

C2PA text gap: C2PA is not JSON/text. Provenance Protocol may be pinned as `kind` attachment, not a glass-card field.

## Refuse (this pane)

- 600-repo monorepo  
- Lifestyle MCP listings as Council OS  
- Filling UNMEASURED (ai-economy, human-labour, humanoid-labour)  
- Tokens, bonds, mainnet GSPC credentials  
- Plugin auto-sign  
- SOVOS / sovereign strings on public surfaces  
- Extra scanners as the growth move  
- Expanding the four-tool plugin  
- Treating `/api/assess` as GSPC  

Growth is the **one** four-tool verify server at the doors agents already crawl: registry remote = `/mcp`, npm 0.1.0 (honest), Claude marketplace GitHub source, HF MCP badge. Everything else is wasted until that door is the **correct** URL.
