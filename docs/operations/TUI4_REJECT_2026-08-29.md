# TUI 4 reject — 29 Aug 2026

Brake. No card-v1 schema change. No mainnet mint. No token.

## XRPL hashes (labelled from JSON-RPC `tx`, 2026-08-29)

RPC: `https://s.devnet.rippletest.net:51234/`. Same hashes `txnNotFound` on mainnet (`xrplcluster.com`) and testnet (`s.altnet.rippletest.net`).

| Label in spine / run JSON | Hash | TransactionType | Network | Ledger | Result |
|---|---|---|---|---|---|
| `memo_attach_tx` | `BC767FEF6497832908B2D208101E361C58A6C0B617C5D94419F9274826A77464` | **Payment** (memo type `csoai/attest`) | **xrpl-devnet** | 4763652 | tesSUCCESS |
| `credential_attach_tx` | `958BA25801A068AEA1507FC1649A862C33D59A1D715924794D98D2C66254DC4B` | **CredentialCreate** (XLS-70) | **xrpl-devnet** | 4763655 | tesSUCCESS |

CredentialCreate fields (decoded):

- `CredentialType` ASCII = `CSOAI.GSPC.CARD/0.1` — a **pointer type name**, not a measured score
- `URI` = `https://councilof.ai/signed/card_index.json`
- Subject `rBkftm76…`, issuer `rpeqYS…`
- Unaccepted → authorizes nothing

No new CredentialCreate. No mainnet mint this pass.

## OTS

**Left `err`.** `harness/mine/cards/kernel-anchor.json` already records `tsa.status: "err"` (freetsa.org PKIStatus 2, no token). This machine: `ots_anchor.py` calendar submissions **0**, serialize of an empty timestamp failed. No `.ots` file written. Do not present an external time anchor.

## Reject (will not ship)

| Attempt | Why rejected |
|---|---|
| **C2PA-inside-card** | Card-v1 preimage is GSPC body only. Art 50 disclosure is a separate MCP/page. Mixing C2PA into the signed JSON breaks `#card-attestation-1` verify. |
| **XLS-70-as-grade** | CredentialCreate on **devnet** is a pointer. The type string `CSOAI.GSPC.CARD/0.1` is not a score. Mainnet Credentials exist (Sep 2025); we do not issue grades there. |
| **“Art 50 certified”** | Measurement, never certification. One C2PA MCP may **scan/disclose**. It does not certify. |
| **token / cut / bond** | Firewall: measurement independent of selling the score. XRPL memo is not an instrument. |
| **six watermark SKUs** | One C2PA path. Canonical: `csoai-c2pa-durable-mcp`. Ignore/fold: `csoai-watermark-attest-mcp`, `csoai-eu-aigc-icon-mcp`, `watermarking-authenticity-mcp`, `c2pa-watermark-mcp`, `agent-content-watermark-mcp`. |
| **DepositPreauth as CSOAI KYC** | XLS-70 + DepositPreauth = who may pay whom. Not a GSPC cell. Not our KYC product. |

## Also not this pane

- Glass card (package 1) = existing `/gspc-verify` + embed. TUI 2 tightens chrome only.
- Embed-nav / HomeWorlds / no iframe: PRs **#903** / **#907**. TUI 2.
- Emilia vectors, VRO one-pager, COSE wrap fixture, plugin pin `cb016901`: TUI 3.
- 7 UNMEASURED financial axes stay empty.

Plugin pin observed this pass: `cb016901e4291dbeb3f510d82f2158da31dcccc2`.
