# ACTIVE GOAL — all TUIs · Cursor · EAT (2026-08-28)

Status: **ACTIVE**. EAT loop. Do not mark complete without independent evidence.

**EAT = Execute → Attest → Test.** One loop, every lane, every day.

## Objective (all planned, all ready)

Execute `docs/operations/NEXT_100_MOVES_2026-08-28.md`.

Immediate, in order:

1. **#876** — 335 verifying chain. Structural GHA (floor, reject, lane-guard, conflict-guard, crawler-view) **green** after `7ab8b075`. Vercel 402 is dead (ignore). Wait Cursor Approval Agent, then merge. Live must read `n_cards == n_cells == 335`.
2. **Inspect 10-fixture gate** — separate PR. Throwaway keys. No new jail number.
3. **Living-door aliases** + `/api/arena/scoreboard` HEAD (Grok lane).
4. **Cursor** — OS doors / AG-UI E2E; quote live `GET /api/gspc` **22 axis · 22 measured · 0 UNMEASURED**. Do not restore 14-of-14, 15·7, or 150.
5. **Claude / other TUIs** — products, MCP, lobby, **single dist builder**. Close stale 150-floor PRs. Do not counter-push #876.
6. **EAT attest** — `signed-json-guard` is the only index gate. Floor/reject bots no longer restore 150.

## Alignment (binding for every TUI)

| Lane | Owner | Touches | Does not touch |
|---|---|---|---|
| harness-measurement | **Grok** | `harness/`, signed cards, verify, inspect gate, scoreboard HEAD, living-door aliases | `client/` OS, `dist/` |
| Council OS / AG-UI | **Cursor** | OS pages, doors, e2e, AG-UI | `public/signed/card_index.json` |
| products / MCP / lobby / dist | **Claude** | products, mcp, lobby/play, `dist/` (single builder) | card index, harness |
| EAT measurement+sign | **K3/DSH** | measure, sign, interop | front-end OS |
| Owner | **Nick** | merge-to-master if required, insurance, NLnet, secrets, I-D | — |

Live grammar (cite the field, never type a constant):
`GET /api/gspc` → **22 axis · 22 measured · 0 UNMEASURED**.
Card chain after #876 → **335/335 VALID** under `#card-attestation-1`. The 150-row board is a **subset**, not a second set.

## Constraints

- Measure; never certify. UNMEASURED is first-class.
- No public $ prices. No index clamp (150 / 313 / 335 as a constant).
- GHA on master only. One lane = one writer. Pull-and-reconcile; never counter-revert.
- Do not merge #876 on red structural gates. Ignore Vercel 402.
- Do not write `dist/` unless you are the single builder. Direct wrangler = owner only.

## Done when

The 100-move list is re-derived from bytes on **2026-09-11**, every claim a live field or honest UNMEASURED.

## Owner-only (agents do not fake these)

Insurance · NLnet Sept window · secrets (receipt pubkey, HF, Stripe, Etherscan) · NIST CAISI + IETF I-D · pricing ruling 211.
