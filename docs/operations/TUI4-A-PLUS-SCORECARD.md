# A+++ scorecard (moves 61–70) — 29 Aug 2026

100/100 is these ten **true**. Not 400 more ideas. Not filed as DONE: five are false.

| # | Bar | T/F | Bytes |
|---|---|---|---|
| 61 | Measurement: stranger verifies card + board stamp | **T** | Card `00a5218048…` VERIFIED Rule A. Live `GET /api/gspc` stamp `#board-attestation-1`. `/gspc-verify` 200. |
| 62 | OS: no sitemap in the iframe | **F** | PR **#903** OPEN, not merged. Origin still has Lobby iframe contract. |
| 63 | Plugin: four hosts, one SHA, fail-closed | **T** | Pin `cb016901e4291dbeb3f510d82f2158da31dcccc2`. Tools: `board_totals` `get_axis` `verify_card` `list_cards`. Catalog install is **not** this bar — see 76 (#398 OPEN). |
| 64 | Join: bindings pins, not a monorepo | **T** | `council-os` `registry/bindings.json` = 12 rows. No emilia-protocol / c2pa-rs / scitt-ccf-ledger trees. Lane unmerged; method is pins. |
| 65 | Art 50: one C2PA path + honest CoP language | **F** | Keep-one list exists (`csoai-c2pa-durable-mcp`). Live fleet still names extra watermark MCPs. `c2pa-python` not wired. HMAC still in the story. |
| 66 | Time: OTS PENDING/VALID explicit | **F** | `tsa.status: err`. No `.ots`. Adapter refuses to call it VALID. Honesty is right; the stamp does not exist. |
| 67 | Ledger: devnet named; URI optional | **F** | Live `/xrpl-attest` still old “permissionless attach” chrome. Lane `xrpl-hash-labels` names Devnet Payment vs CredentialCreate; **not deployed**. |
| 68 | Standards: maps and labelled runs | **F** | No VRO map filed. Emilia pin only (`e507acdf…`); no labelled run numbers. |
| 69 | Enterprise: licence + embed, invoice | **T** | `/embed` `/badge` `/licensing-agreement` 200. White-label = those + invoice, not a token. |
| 70 | AEO: llms.txt = API | **T** | Live `llms.txt` says do not freeze counts; fetch `GET /api/gspc`; quote `totals.public_count`. API **22 · 15 · 7**. |

**5 true · 5 false.** Do not write `TUI4-DONE-NEXT.md` until 62, 65, 66, 67, 68 are true.

Not A+++ (71–79), still held: Lighthouse-as-goal, more axes, token, Town, 10k accounts, marketplace green while **#398 OPEN**, VRO widget, financial MEASURED (7 empty), “waves” copy.
