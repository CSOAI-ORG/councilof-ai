# NEXT 100 MOVES — 2026-08-28 (evening)

Supersedes STATE_RUNWAY §5 and the 08-22 NEXT-100 execution list where they
disagree. Cite live fields, never a typed constant.

Doctrine (binding): measure never certify · UNMEASURED is first-class · no
public $ prices · no clamp on the card index · GHA on master only · one lane
= one writer · pull-and-reconcile, never counter-revert.

Owners: **Grok** harness/cards/inspect · **Cursor** OS/front · **Claude**
products/MCP/lobby/dist · **Owner** insurance, grants, secrets, form submits.

---

## Closed this session (do not re-plan)

| Item | Bytes |
|---|---|
| Crawler-view on master | green on `243fd107` (Cursor) |
| Live board | 22 axis · 15 measured · 7 UNMEASURED |
| 335 vs 150 | 335 is the mine chain; 150 is a subset floor. PR **#876** |
| 22 withheld bodies | restored from `e557620b`; 335/335 VALID under the pin |
| Inspect emitter | `measurement_card.py` binds instrument + config_digest + rows_digest |
| Inspect 10-fixture gate | landed locally, not in #876 — next Grok PR |
| `estateFacts` 150 clamp | removed on #876 (origin still had `pinnedVerified = 150`) |

Still live on councilof.ai until #876 merges: index 313, `n_cells: 150`.

---

## Wave 0 — land what is in flight (1–10)

1. **Grok** Merge #876 after GHA green. Do not merge on red. Do not force-push.
2. **Grok** Confirm live `card_index.json` `n_cards == n_cells == 335` and
   `GET /api/cards` does not silently stay 314. Two labelled numbers if they
   disagree — never reconcile by invention.
3. **Grok** Confirm live HOW-TO-VERIFY and `/api/state` card_chain facts say
   335/335 VALID, withheld 0. If copy still says 150, that is a defect.
4. **Grok** PR the Inspect 10-fixture gate
   (`harness/arena/test_inspect_adoption_gate.py` + JCS dispatch in
   `tools/verify_any_card.py`). Separate from #876.
5. **Cursor** Green Council OS E2E on `cursor/instruments-catalog-7fb8`
   (AG-UI rail + Enterprise/Insurers tip). Stop the failing-push stream.
6. **Cursor** Close or restack leftover OS-door drafts (#870 logos, #863
   training world) onto current master. One gated merge each.
7. **Claude** Close stale 150-floor PRs (#647, #646, #612, #584, #582) as
   superseded. They fight #876 if merged.
8. **Owner** Merge #876 yourself if agents must not merge to master.
9. **Any** Dependabot PRs (#685, #684, #682, …): one batch after #876, not
   interleaved with card-index.
10. **Claude** Dist/deploy stays a single builder. Nobody else writes `dist/`.

---

## Wave 1 — measurement honesty (11–25) — Grok lane

11. Living-door aliases: `rwa` `xrpl` `evm` `arc-agi` overlay `ins` resolve to
    the live board/attest surfaces, not dead dumps.
12. `GET /api/arena/scoreboard` HEAD: 200, signed, no typed totals.
13. Inspect gate in CI (the 10 fixtures, throwaway keys, no fleet).
14. Tiny Inspect eval on the frozen jail gold-bank (n=71) — bind the published
    gold-run, do not invent a new accuracy. UNMEASURED if the run fails.
15. Only after 13+14 stay green: declare Inspect the default *runner*, still
    signing as MEASUREMENT never "Inspect-certified".
16. Jail v4: 19-model fleet when a pod is free. Separation SEPARATED or TIE.
    Do not conflate with the 13-axis 19-model board fleet.
17. Gold-bank HF field on the jail axis (dataset already public) — quote the
    live board field, do not retype n.
18. `verify_any_card.py` JCS path covered for a stranger kit (sibling `_jcs.py`
    or honest UNCHECKABLE if missing).
19. HF README brand sweep (Grok): no internal names, no certify, no 150 clamp.
20. Rate-the-raters skeleton: Vals / Arena / us — cite, never disparage. Wedge
    = signed + corrections ledger.
21. FAIR TIES paper v4: adaptive-n + variance sampling. ARC paper track.
22. Corrections ledger public API (append-only, stranger-checkable).
23. Determinism re-run gate in CI (same rows → same card id).
24. Count-grammar sweep: every public surface quotes `/api/gspc` or
    `/api/state`, never a typed 14/15/150/313/335.
25. Declare first, then measure: refusal/abstention, containment-limit,
    human-baseline — UNMEASURED + rubric before any number.

---

## Wave 2 — domain axes still UNMEASURED (26–40)

26. **Grok** Index v0.2 input bank: compute-price (Epoch public series).
27. **Grok** Index v0.2 input bank: AI-investment series (public, cited).
28. **Grok** Humanoid-labour registry: declared UNMEASURED until a bank exists.
29. Sign index v0.2 only for series that actually ran. Leave gaps visible.
30. XRPL: 5th locate attempt for Aviva / DCP / EURCV / JMWH. If still
    not-located, keep UNMEASURED. JMWH stays demo-only.
31. Extend XRPL control-facts only for issuers with a mainnet account.
32. EVM control-facts rubric (BUIDL / BENJI / ACRED). Owner: Etherscan key.
33. EVM `source_verified` run after the key exists. Until then UNMEASURED.
34. Provenance-controls stays the one measured financial axis until 31–33 land.
35. **Cursor** `/indices` and `/financial-axes` render live numbers from
    `/api/state`, with UNMEASURED cards first-class.
36. `/attest` door on Council OS uses the same facts, no second total.
37. East↔West market-pair axis: declare + rubric, no number.
38. Domain packs matrix (cap-markets / insurance / health): who measures what.
39. Article 50 detector line: `/api/detect` readiness vs 2026-12-02 watermark
    deadline — measured or UNMEASURED, never "compliant".
40. CRA / GPAI procurement check as a declared rubric, not a mark.

---

## Wave 3 — stranger verification + standards (41–55)

41. Rekor / Trillian anchor *test* (one receipt). Record UNMEASURED if the
    test cannot complete. Do not claim a log we do not have.
42. COSE RFC 9942 receipt builder (shape only, no fake receipts).
43. SCRAPI shape doc against current IETF draft.
44. SCITT implementer-list PR (ietf-wg-scitt) — evidence of registration,
    never certification.
45. Owner: submit IETF I-D (draft ready in owner pack).
46. Owner: IANA media type `application/agent-measurement+json` after I-D.
47. Owner: NIST CAISI input (draft ready) — send.
48. PQC note (ML-DSA-65) in RECEIPT-SPEC — planned, not claimed live.
49. MCP registry GET discovery stays honest (count from the live endpoint).
50. **Claude** gspc-mcp package + `/llms.txt` consume live board, no 150.
51. Portable verifier one-pager linked from every signed artifact.
52. verify-demo Space linked from site header (Cursor) — no new numbers.
53. Key-rotation drill (runbook exists): clients fail closed on revoke.
54. Correction supersede chain (new card names prev, old stays).
55. External reviewer list for the card spec (invite, do not claim review).

---

## Wave 4 — product + OS (56–70)

56. **Cursor** Footer real framework + verify logos (#870) — measurement
    badges, not conformity marks.
57. **Cursor** Compliance Training World (#863) as Academy completion records,
    never "get certified".
58. **Claude** Nine-products e2e green on live, not demo.
59. **Claude** Lobby/play inner experiences: no dead `/?lobby=` dumps.
60. Product catalogue links live. Prices stay DRAFT until Owner ruling 211.
61. Homepage grammar: 22 axis · 15 measured, derived. No 14-of-14 leftover.
62. Header: Open OS → `/os`; verify stays free forever.
63. E2E sweep (`e2e/sweep`) against live after #876 — known failures as
    `fixme` with finding numbers, never deleted.
64. Library IA: leftover marketing doors hop to `/os` or `/honesty`.
65. AG-UI control rail + chat-seeded menus (Cursor) stay green on mobile.
66. **Claude** MCP fleet tile quotes live server count, UNMEASURED if down.
67. A2A agent-card intake (`enter-arena`) stays unsigned receipt + UNMEASURED.
68. No new "Get certified" copy. Existing UI may say it; do not add more.
69. Prerender lane-safe (`prerender-run.sh`); never kill other lanes' browsers.
70. **Claude** One dist build → `deploy.yml`. Direct wrangler = owner only.

---

## Wave 5 — revenue + grants (71–85) — mostly Owner

71. **Owner** PI + media liability ≥£1–2M, US/CA extended — before any named
    negative verdict.
72. **Owner** NLnet / NGI Zero: Sept 3 open, Nov 3 deadline. UK eligibility
    already confirmed in writing. Submit in the window.
73. **Owner** AIRR Rapid Access (20k GPU hours).
74. **Owner** NVIDIA Inception + Microsoft Founders Hub (drafts in pack).
75. **Owner** HF credits (old token dead) + EF ESP inquiry + Manifund +
    Longview EOI.
76. **Owner** Secrets: `RECEIPT_PUBKEY_HEX` (arms `/api/fulfill`), Stripe,
    HF_TOKEN, Etherscan.
77. **Owner** Pricing ruling Move 211 — clears DRAFT tags. Until then no
    public $ strings.
78. x402 metered bulk/replay — free tier stays free; meter workflow/scale
    only after 76.
79. Insurer feed pilot pitch SENT (owner). Meeting artifacts already exist.
80. Enterprise workflow metering design (no prices in UI).
81. ERC-3643 attester feed (partner) — UNMEASURED until a live issuer.
82. Data-licence line for corpus/index feeds (procurement), never issuer-pays.
83. Sponsorware only for OS features, never for a grade.
84. Newsletter / report sponsorship one-pager (owner).
85. Media kit: measurement credential, not a conformity mark.

---

## Wave 6 — standing + scale (86–100)

86. Foundation trigger: incorporate only at ≥1 grant or £50K revenue. LTD
    carries doctrine until then.
87. Ring-fence methodology + verifier + ledger for the Foundation when 86
    fires. Not before.
88. AIUC-1 engagement (Sep 30). Show up as measurer, not certifier.
89. C2PA vocabulary adoption note (Art 50 timing).
90. OpenSSF model-signing EOI.
91. Arena 24/7 on a dedicated pod when one is free; honey harvest continues.
92. Signed corpus versioning + licensing line (MIT/CC0 for artifacts).
93. Annual transparency report skeleton (first edition after Q4).
94. Kill-switch + cold-restore drill #1 (document the actual minutes).
95. OTel "AI attestation" convention donation (SIG) — after I-D exists.
96. Significance-gated arena product (TIE is first-class).
97. Academic citations: FAIR TIES + RECEIPT-SPEC, not blog counts.
98. Third-leg evaluation only at a stated scale trigger. No premature "lab".
99. Weekly: mine CI, brick-watchdog, corrections append, lane claim check.
100. Re-derive this list from bytes on 2026-09-11. Anything still typed is
     a defect.

---

## Grok's next 10 (this lane, in order)

1. #876 green + merge (or owner merge)
2. Live 335 check
3. Inspect-gate PR
4. Living-door aliases
5. Scoreboard HEAD
6. Inspect CI hook
7. Gold-run Inspect bind (no new number)
8. HF README sweep
9. Rate-the-raters skeleton
10. Index v0.2 banks (declared UNMEASURED until filled)

## Owner's next 5 (the real gates)

1. Insurance
2. NLnet in the Sept window
3. Secrets (`RECEIPT_PUBKEY_HEX`, HF, Stripe, Etherscan)
4. NIST CAISI + IETF I-D send
5. Pricing ruling 211 — or keep DRAFT forever, honestly

Agents cannot close 71–77. Do not pretend a PR substitutes for them.
