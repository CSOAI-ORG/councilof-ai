# x402 Self-Test: request_attestation (0.01 USDC Base)

**Date:** 2026-09-11T14:00:00Z
**Classification:** INTERNAL_SELF_FUNDED
**Rail:** x402 / Base eip155:8453 / USDC
**Subject:** `tui4-self-test-0.01usdc`
**Tool:** `commission_card` → `GET /api/request-attestation?subject=tui4-self-test-0.01usdc`

---

## Step 1 — Discovery (passive, no payment)

**Request:**
```
GET https://councilof.ai/.well-known/x402.json
```

**Result:** HTTP 200, x402 discovery manifest returned.
- `mode`: live
- `network`: eip155:8453
- `asset`: 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913 (USDC)
- `payTo`: 0x212686404A7D1E1fD88F35eD6200c3aF7A78ae31
- `extensions.offer-receipt.supported`: true
- `extensions.offer-receipt.emission`: conditional
- `extensions.offer-receipt.board_signing_key_configured`: true

**Status:** ✅ Discovery works. Agent can find the payment rail.

---

## Step 2 — Challenge (the 402 response)

**Request:**
```
GET https://councilof.ai/api/request-attestation?subject=tui4-self-test-0.01usdc
```

**Result:** HTTP 402 Payment Required

**Challenge body (verbatim structure):**
```json
{
  "x402Version": 2,
  "error": "Payment required",
  "resource": {
    "url": "https://councilof.ai/api/request-attestation",
    "description": "A signed card-v0 commission receipt for one named subject, plus every already-signed measurement card on file for it. Payment never mints a MEASURED cell.",
    "mimeType": "application/json",
    "serviceName": "CSOAI Request Attest"
  },
  "accepts": [
    {
      "scheme": "exact",
      "network": "eip155:8453",
      "amount": "10000",
      "maxAmountRequired": "10000",
      "asset": "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
      "payTo": "0x212686404A7D1E1fD88F35eD6200c3aF7A78ae31",
      "maxTimeoutSeconds": 300,
      "extra": {
        "name": "USD Coin",
        "version": "2",
        "decimals": 6,
        "symbol": "USDC"
      },
      "csoai_pricing": {
        "product_id": "csoai.product.request_attestation",
        "sku_id": "request_attestation",
        "tier": "per_request",
        "pricing_basis": "PROMO_EXISTING_DATA",
        "campaign_id": "csoai-launch-30d-20260911",
        "normal_amount_atomic": "20000",
        "offered_amount_atomic": "10000",
        "starts_at": "2026-09-11T00:00:00Z",
        "ends_at": "2026-10-11T00:00:00Z",
        "fresh_compute_excluded": true
      },
      "resource": "https://councilof.ai/api/request-attestation",
      "description": "A signed card-v0 commission receipt for one named subject, plus every already-signed measurement card on file for it. Payment never mints a MEASURED cell.",
      "mimeType": "application/json"
    }
  ]
}
```

**Price:**
- `amount`: 10000 atomic (0.010000 USDC, 6 decimals)
- `normal_amount_atomic`: 20000 (0.02 USDC — full price)
- `offered_amount_atomic`: 10000 (0.01 USDC — 50% launch promo)
- `campaign_id`: `csoai-launch-30d-20260911` (active 2026-09-11 to 2026-10-11)

**Status:** ✅ Challenge returned. Amounts are consistent with self-test target.

---

## Step 3 — Signed Offer Receipt (JWS)

The challenge includes a signed offer receipt in `extensions.offer-receipt.info.offers[0]`.

**JWS Header (decoded):**
```json
{
  "alg": "EdDSA",
  "kid": "did:web:csoai.org#board-attestation-1"
}
```

**JWS Payload (decoded):**
```json
{
  "amount": "10000",
  "asset": "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
  "network": "eip155:8453",
  "payTo": "0x212686404A7D1E1fD88F35eD6200c3aF7A78ae31",
  "resourceUrl": "https://councilof.ai/api/request-attestation",
  "scheme": "exact",
  "validUntil": 1789129104,
  "version": 1
}
```

**Signature:** Present (first 40 chars: `w1yRmp4tFTiFe4BTIx7PPuie_LPH2a9_frkZxonj...`)

**Verification path:**
```bash
# 1. Resolve DID document
curl https://csoai.org/.well-known/did.json | jq '.verificationMethod[] | select(.id=="did:web:csoai.org#board-attestation-1") | .publicKeyJwk.x'
# → "k2fPWb6ctyu8l5at8FYgHsHFit_qoT-DssW3VNbCAXA"

# 2. Decode JWS (split on '.')
# header.payload.signature
# Base64url-decode header → {"alg":"EdDSA","kid":"did:web:csoai.org#board-attestation-1"}
# Base64url-decode payload → {"amount":"10000",...,"validUntil":...}
# Verify Ed25519 signature using the public key from DID
```

**Status:** ✅ Signed offer receipt IS being emitted. The DRAFT declaration in agent-card.json extensions is now understating reality — offers are live when the board key is provisioned.

---

## Step 4 — Payment (NOT EXECUTED — owner approval required)

To execute the actual payment, an agent would:

1. **Construct X-PAYMENT payload** per x402 exact scheme:
   ```json
   {
     "x402Version": 2,
     "scheme": "exact",
     "network": "eip155:8453",
     "payload": {
       "authorization": {
         "from": "<payer-address>",
         "to": "0x212686404A7D1E1fD88F35eD6200c3aF7A78ae31",
         "value": "10000",
         "validAfter": "<unix-timestamp>",
         "validBefore": "<unix-timestamp>",
         "nonce": "<random-bytes-hex>"
       },
       "signature": "<EIP-3009-transferWithAuthorization-signature>"
     }
   }
   ```

2. **Base64-encode** the payload → `X-PAYMENT` header value

3. **Call the tool with payment:**
   ```
   GET https://councilof.ai/api/request-attestation?subject=tui4-self-test-0.01usdc
   Header: X-PAYMENT: <base64-encoded-payload>
   ```

4. **Expected response:** HTTP 200 with:
   - The requested card-v0 leaf
   - `X-PAYMENT-RESPONSE` header containing `SettlementResponse` with:
     - Transaction hash (on-chain verifiable)
     - Signed receipt (when board key + facilitator confirm payer)

**Status:** ⏸️ NOT EXECUTED. Requires owner wallet/private key. Steps documented for future automation.

---

## Step 5 — Settlement Verification (post-payment, documented)

After settlement, the agent would verify:

1. **On-chain:** `https://basescan.org/tx/<transaction-hash>` — USDC transfer from payer to payTo
2. **Receipt:** `POST https://councilof.ai/api/receipts/verify` with the signed receipt
3. **DID resolution:** Verify signature against `did:web:csoai.org#board-attestation-1`
4. **Artefact delivery:** Confirm the card-v0 leaf exists at `/api/request-attestation?subject=...`

---

## Summary

| Step | Status | Notes |
|------|--------|-------|
| Discovery | ✅ PASS | x402.json returns correct rail config |
| Challenge | ✅ PASS | HTTP 402 with correct amount (0.01 USDC promo) |
| Signed Offer | ✅ PASS | JWS EdDSA offer receipt emitted (board key provisioned) |
| Payment | ⏸️ PENDING | Owner approval required |
| Settlement | ⏸️ PENDING | Depends on payment |
| Receipt verification | ⏸️ PENDING | Depends on settlement |

**Key finding:** The x402 offer-receipt extension is no longer DRAFT-only — signed offers ARE being emitted when `BOARD_SIGN_KEY_PKCS8_B64` is provisioned. The agent-card.json extension description says "DRAFT WE PUBLISH AND DO NOT YET EMIT" for signed-receipts, but the x402 offer-receipt is working. This is a documentation accuracy issue: the x402 offer-receipt emission is live, while the A2A signed-receipts extension (task-outcome receipts) remains a draft.
