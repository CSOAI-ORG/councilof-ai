# x402 demand map — what agents pay for, and what we can honestly sell them (2026-09-02)

**Lane:** x402-demand-map (research; this document only — no code, nothing posted, no keys, no accounts).
**Question asked:** the x402 network reports ~69,000 active agents / 165M transactions. What do those
agents actively want, which asks can we service today over x402 or with a signed attestation, and when
they request "basic" data (an RPC read, a TOML check, model metadata, a hash) can we serve the read
plus the signed, witnessed card of the read so they learn the signed form?
**Doctrine honoured:** measurement, never certification · verification free forever · a rank/verdict is
never sold · buyer-led · no prices in prose (amounts live only in a 402 `accepts[]`) · no token, credit
or index · open source · nothing posted by an agent, no keys, no accounts created.

**Binding input (owner-supplied stress-test brief, 2026-09-02):** `~/Downloads/compass_artifact_wf-b4603903…_text_markdown.md`
("Stress-Test & Rebuild: The Black-Swan Permissionless Revenue Thesis"), Parts 1–3 and 7. Its rulings
are applied throughout and cited as **[brief §n]**: (a) BUIDL and FOBXX/BENJI are **not** on XRPL — only a
sliver of Ondo USDY/OUSG is [brief Claim 1]; (b) nobody pays per-call for signed *public* data — what sells
is per-request evidence packs, commissioned attestations and a signed historical time-series ("provable
archive": live flags free, provable history sold) [brief Claim 2, Part 3 Rank 2]; (c) an AI-provider
"anti-benchmarking safe harbor" does not exist as law [brief Claim 5, Part 2 §3]; (d) "not a benchmark"
needs T&C safeguards — prohibit use in financial instruments, no composite/continuous rate, point-in-time
only — and the words "oracle" / "risk" must not frame our outputs [brief Claim 3, Part 2 §1–2, Part 7 §4];
(e) rank 1 by time-to-first-pound is the EU AI Act Article 50 marking-evidence pack invoiced in GBP before
2 Dec 2026; x402 is distribution and optionality at ~£0 today [brief Part 3 Rank 1 and Rank 5].

Everything in §0 was read from bytes (curl + code) on 2026-09-02 08:30–09:00Z. Every claim in §1–§6
cites a URL and a date. Anything I could not confirm is marked **UNVERIFIED**.

---

## 0. Bytes first — what exists, live vs master

| Surface | Live probe (08:40Z) | On master `103ec792` | Verdict |
|---|---|---|---|
| `GET /api/x402` (catalogue, no amounts) | **404** | `functions/api/x402.ts` (3 tiers, `free_forever[]`, invariants) | prod behind master (same state the 08:04Z probe in `REVENUE-RESEARCH-2026-09-02.md` saw — the deploy has not landed; treat as the first owner action in §5) |
| `GET /.well-known/x402.json` | 200 **static stub** (`mode:"mock"`, points at `pack.councilof.ai`) | `functions/.well-known/x402.json.ts` v0.2: 4 resources on this origin, mode derived from env | prod behind master |
| `GET /api/request-attestation?subject=…` | **402**, x402 v2 body, `extensions.bazaar {info, schema}` present, `payTo: null` | `payTo` = `ESTATE_PAY_TO` (`_x402_config.ts`), EIP-712 domain `USD Coin`/`2` | challenge is real; live one is unpayable until deploy |
| `GET /api/evidence-bundle` | **404** | `functions/api/evidence-bundle.ts` — OSCAL 1.1.0 assembly of signed leaves for article-50 / article-53 / dora / cra | prod behind master |
| `GET /api/rwa/evidence?asset=` | **404** | **PR #1158** (open, `lane/rwa-evidence`): per-request signed XRPL asset card — AccountRoot flags, `gateway_balances`, two-way `xrp-ledger.toml` check, every raw fetch sha256'd | not merged |
| `GET /api/eunomia-data` | 402 (v1-shaped, `payTo: null`, `price_usd` typed in body — the old shape) | v2 challenge + assembled signed feed | prod behind master |
| `GET /api/proof?sha=<64hex>` | 200/404 honest (`unmeasured:["inclusion"]`, names the root) — **free** | same | REAL, free forever |
| `GET /root.json` | 200 — `card_count: 50`, `merkle_root 8025ee10…6909`, `as_of 2026-09-02T07:13:27Z` | one writer: `scripts/publish_public_root.py`, hourly cron `7 * * * *` (`public-root.yml`) | REAL |
| `GET /interop/root-witness-latest.json` | 200 — `csoai.root-witness/v1`, as_of 07:13:32Z, Rekor `rekord` + OTS status per root | `scripts/witness_public_root.py` (Rekor rekord over the signed preimage; OTS when the client is present; EAS/XRPL memo `NOT_YET`) | REAL — the ONE root is witnessed, never N leaves |
| `GET /mcp` | 200 discovery doc; POST = 7 tools (`board_totals get_axis verify_card list_cards get_root get_card verify_inclusion`), all free | `functions/mcp/[[path]].ts`, `mcp/gspc-server` (npm `csoai-gspc-mcp`) | REAL, no paid tool yet |
| `GET /api/gspc`, `/api/xrpl` (n=16), `/api/swift` (n=26), `/api/fines` | 200 | readers, `writes_board:false` | REAL, free forever |
| Signed corpus | `/signed/card_index.json` 335 positions (313 verify, 22 withheld per verifier README) | — | REAL |
| Settlement | — | `_x402.ts verifyX402Payment`: fail-closed until `X402_FACILITATOR_URL`; `/verify` then `/settle`; v1/v2 dialect matched | REAL, switched off |
| **Provable archive** (hourly signed root + leaves) | `public/cards/` holds **131** leaves: 88 `xrpl.asset.state`, 41 `public.notice`, 2 `benji.onchain.supply`; `public/root.json` has **27 commits since it was first added 2026-08-31 08:52 BST**; 4 dated `root-witness-*` files | one writer, hourly; every leaf carries its inclusion proof; Rekor/OTS on each root | the archive the brief's Rank 2 asks for **already started compounding on 31 Aug** — for XRPL issued assets only, at hourly cadence. Retention of superseded roots (not just leaves) is **UNVERIFIED** — check `public/interop/` and git history before selling history |
| XRPL reader scope | `/api/xrpl` n=16 xrpl.fi identity-verified issued assets (RLUSD, …); `benji.py` reads Franklin's **Stellar** TOML + dated notices and records "XRPL toml ABSENT" | `writes_board:false`, DISCOVERED, never clients | per **[brief Claim 1]**: BUIDL and FOBXX/BENJI are **not** on XRPL; only part of Ondo USDY/OUSG is. Never describe the XRPL reader as institutional-RWA coverage |
| Article 50 assets | `/packs/eu-article-50/`: `article50_oscal.json` + `.sig.json` (ProvBench — C2PA marking durability under transforms, OSCAL 1.1.0, signed 2026-08-13) and `provbench.json` + `.sig.json`; `POST /api/article50` free HMAC "passport" (`stored:false`, Ed25519/OTS tiers return **501** until a signing backend is bound); `_obligations.ts` article-50 record `counsel_confirmed: true` with verbatim fine ceiling | `functions/api/article50.ts`, `functions/article-50-kit/`, `scripts/evidence-pack-generate.mjs` | the Rank-1 product's evidence half exists; the **per-output marking measurement of a buyer's file** does not (§3) |
| Staged leaves | — | `scripts/adapters/staged_leaves.py`: unsigned atoms in `STAGED_DIRS` → `public.notice` leaves in the next root; `hub_cite.py` deliberately emits **no** card-v0 for Hub listings (DISCOVERED, never MEASURED) | the pattern `witness-my-hash` and `hub-card` reuse |

Reading: the x402 rail described in `docs/REVENUE-LOOPS.md` is on master and still not on prod; nothing
is *agent-payable* until the deploy lands and one env var is set (§5 step 0). The Rank-1 product does not
wait for that: the Article 50 pack can be assembled from master today and invoiced in GBP by CSOAI LTD
(§3 A, §7). The free surfaces that make the "stack on top" credible — `/root.json`, `/api/proof`, the
Rekor/OTS witness, `/mcp` — are live now.

---

## 1. Claim-by-claim verdicts on the pasted brief

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 1 | "~69,000 active agents / 165M transactions (late Apr 2026)" | **REPORTED BY COINBASE, not independently reproducible.** The numbers are Coinbase's, released with the Agentic.Market launch (2026-04-20). Third-party counts differ: the same launch coverage says "more than 480,000 transacting agents"; on-chain trackers count **179.4M cumulative tx / $41.6M** as of 2026-09-02 and warn raw counts "include tests, infrastructure traffic, repeated service calls". A peer-reviewed dataset (119M tx, May–Dec 2025) shows Coinbase's facilitator alone at 77.17M tx / $26.85M and >93% of servers bound to one facilitator. Quote the 69k/165M only as "Coinbase reported". | [cryptobriefing, 2026-04-20](https://cryptobriefing.com/agentic-market-ai-agents-hub/) · [Eco explainer](https://eco.com/support/en/articles/14839402-x402-protocol-explained) · [agenteconomy.to stats, 2026-09-02](https://agenteconomy.to/stats/x402-transactions) · [arXiv 2607.19545, 2026-07-21](https://arxiv.org/html/2607.19545v1) · [Chainalysis, 2026-06-03](https://www.chainalysis.com/blog/x402-agentic-payments-adoption/) |
| 2a | "x402 Bazaar discovery registry at `api.cdp.coinbase.com/platform/v2/x402/discovery/…`" | **CONFIRMED, probed.** `GET …/discovery/resources?limit=100&offset=N` answers **HTTP 200 without any key**; `pagination.total = 15,083` on 2026-09-02 08:37Z; items carry `accepts[]`, `description`, `extensions.bazaar`, `lastUpdated` (range 2026-06-08 → 2026-09-02), `quality`. `…/discovery/merchants` and `…/x402/supported` answer **401** (key needed). The MCP variant `…/discovery/mcp` answers 405 to GET (POST-only MCP). councilof.ai is **not** in the catalogue. | probe 2026-09-02 · [CDP Bazaar docs](https://docs.cdp.coinbase.com/x402/bazaar) ("Bazaar discovery is public. You do not need a CDP API key to use the discovery APIs") · [AWS AgentCore → Bazaar MCP](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/payments-connect-bazaar.html) |
| 2b | "`/.well-known/x402` is the Bazaar registry" | **PARTLY WRONG.** `/.well-known/x402(.json)` is a *seller-side manifest* convention (IETF draft "Discovering x402 Payment Capability via DNS and a Well-Known URI"); many sellers publish one and third-party crawlers read it, but the **Bazaar does not index from it**. We already serve one (`functions/.well-known/x402.json.ts` on master). | [x402 gitbook Bazaar](https://x402.gitbook.io/x402/core-concepts/bazaar-discovery-layer) · awesome-x402 entries linking `/.well-known/x402` ([README](https://github.com/xpaysh/awesome-x402/blob/main/README.md)) |
| 2c | "listing needs the CDP facilitator" | **CONFIRMED.** "The x402 Bazaar is a catalog of payment-gated services discovered by the CDP Facilitator"; the catalogue "builds itself from payments it has already settled" — a settle through CDP carrying a conformant `extensions.bazaar` block is the only documented listing path; there is no registration form. Whether the `payTo` wallet must also belong to a CDP developer account is **UNVERIFIED** (issue #2112: 8 CDP settles, never indexed, closed without a maintainer answer). Sellers in the wild say "All settlement routed through Coinbase CDP facilitator for Bazaar indexing." PayAI settles do not index. | [CDP Bazaar docs](https://docs.cdp.coinbase.com/x402/bazaar) · [x402#2112](https://github.com/x402-foundation/x402/issues/2112) · [awesome-x402](https://github.com/xpaysh/awesome-x402/blob/main/README.md) · `docs/X402_BAZAAR_DISCOVERABLE_2026-09-02.md` |
| 3 | "Cloudflare offers an edge x402 payment gateway" | **ANNOUNCED, NOT GA.** *Monetization Gateway* (2026-07-01): "charge for any resource behind Cloudflare via x402" incl. "web pages, datasets, APIs, or MCP tool calls", stablecoin settlement (USDC + "Open USD"), **waitlist only**. *Cloudflare Wallets* (2026-08-04): `cloudflare.pay` handle reservation is live; Account/Virtual Wallets and spend controls are "coming months". Cloudflare's own x402 examples use Coinbase's facilitator — Cloudflare is not a facilitator. For us: irrelevant as infrastructure (our 402s already run at the edge in Pages Functions); relevant as *buyer supply* once Virtual Wallets ship. | [Cloudflare blog, 2026-07-01](https://blog.cloudflare.com/monetization-gateway/) · [InfoQ, 2026-07](https://www.infoq.com/news/2026/07/cloudflare-aws-x402-micropayment/) · [wavect verification, 2026-08-05](https://wavect.io/blog/cloudflare-wallets-ai-agents/) |
| 4 | "Apify accepts x402 for Actor runs" | **CONFIRMED.** Blog 2026-06-30: ~20,000 pay-per-event Actors answer a direct 402 from the Actor endpoint; schemes `upto` (v2, charge on actual usage) and `exact`; USDC on Base (`eip155:8453`); recommended client is Coinbase's Agentic Wallet CLI (`awal`). The docs page also describes a prepaid-token path (`agi.apify.com/protocols/x402/prepaid-tokens`, $1 minimum, 14-day expiry). Actor developers must pass KYC. | [Apify blog, 2026-06-30](https://blog.apify.com/introducing-x402-agentic-payments/) · [Apify docs](https://docs.apify.com/platform/integrations/x402) · [changelog](https://apify.com/change-log/pay-for-apify-actors-with-x402) |
| 5 | "Nodit is an x402 facilitator" | **REFUTED AS STATED — Nodit is a SELLER.** Nodit (Lambda256) *accepts* x402 for its own RPC and Web3 Data APIs on Base + Solana, in a pre-funded credit mode and a pay-per-use mode ("instant wallet-authenticated access to blockchain endpoints across 20+ major chains"). No public `/verify` `/settle` facilitator by Nodit was found. It is a demand signal (agents paying for RPC reads), not a settlement option. | [Nodit blog, 2026-03-24](https://blog.nodit.io/introducing-nodit-x402-payment-protocol/) · [Nodit explainer](https://blog.nodit.io/ai-agents-that-pay-for-themselves-how-the-x402-protocol-works/) |
| 6 | "Nevermined is an x402 facilitator" | **CONFIRMED, non-standard paths.** Public production facilitator `https://facilitator.live.nevermined.app` with `POST /api/v1/x402/verify` and `POST /api/v1/x402/settle` (sandbox at `facilitator.sandbox.nevermined.app`); settles ERC-20 transfers and its own credit/subscription contracts (`nvm:erc4337` appears once in the Bazaar sample). Probed: `/supported` and `/api/v1/x402/supported` → 404, so it is **not a drop-in** for `_x402.ts` (which calls `${facilitator}/verify`). Account/key requirement and fees **UNVERIFIED**. | probe 2026-09-02 · [Nevermined docs](https://nevermined.ai/docs/products/x402-facilitator/overview) · [Nevermined blog](https://nevermined.ai/blog/the-payment-layer-ai-agents-actually-need-introducing-the-nevermined-x402-facilitator) |
| 7 | "Coinbase AgentKit agent wallets with spending limits" | **CONFIRMED with a naming correction.** The wallet product is *CDP Agentic Wallets* (announced 2026-02-11; `npx awal` CLI + MCP server; per-transaction and session caps; gasless on Base; x402 client built in). *AgentKit* is the framework layer (LangChain/CrewAI/Vercel AI SDK actions incl. `make_http_request_with_x402`). The CDP TypeScript SDK exposes `applySpendControls`, `SpendTracker`, `SpendControlError`, `getSpendControlsRegistry`. The stronger claim that limits are "enforced at the smart-contract level" appears only in third-party explainers — **UNVERIFIED** against CDP docs. | [docs.cdp.coinbase.com/llms.txt](https://docs.cdp.coinbase.com/llms.txt) (SpendControls symbols) · [CDP Spend Permissions](https://docs.cdp.coinbase.com/server-wallets/v2/evm-features/spend-permissions) · [Eco explainer](https://eco.com/support/en/articles/14845485-coinbase-agentic-wallets-explained) · [AgentKit README](https://github.com/coinbase/agentkit/blob/main/typescript/agentkit/README.md) |
| 8 | (implicit) "MetaMask facilitator" | **CONFIRMED URL, different scheme.** `https://tx-sentinel-base-mainnet.dev-api.cx.metamask.io/platform/v2/x402` (Base 8453; also Base Sepolia, Monad 143) — pays via ERC-7710 delegations from MetaMask *smart accounts*, not a plain EOA `exact`/EIP-3009 flow. Not a drop-in for our rail. | [MetaMask SKILL.md](https://github.com/MetaMask/smart-accounts-kit/blob/main/skills/x402-payments/SKILL.md) · [MetaMask docs](https://docs.metamask.io/smart-accounts-kit/1.5.0/guides/x402/overview/) |
| 9 | Facilitator stats (PayAI, CDP) | PayAI: keyless, `GET /supported` lists v1 `exact/base` and v2 `exact/eip155:8453` (probed 200); claims ">90% of all real x402 transactions on Solana", peak >3M tx/week Dec 2025, Solana weekly count down >90% by 2026-02-09. CDP: largest by count (1.8M+ in 30 days on x402scan, May 2026), third by volume. Dashboards: Daydreams 8.1M+, Coinbase 7.6M+, X402rs 411K+ (2026-04-14). | probe · [PayAI blog, 2026-05-29](https://blog.payai.network/largest-x402-facilitator-solana/) · [note.com x402inc, 2026-05-30](https://note.com/x402inc/n/nfd6227f13b55?hl=en-US) · [web3trackers, 2026-04-14](https://www.web3trackers.com/x402-dashboard) |

---

## 2. The demand map — what agents actually pay for

### 2.1 The evidence base (three independent lenses)

**Lens A — the Bazaar catalogue (supply that CDP has settled at least once).** Sample: the first
3,000 of 15,083 items (`offset 0–2900`, 2026-09-02 08:37Z). 705 distinct hosts, 484 distinct `payTo`
wallets (one wallet owns 516 items — a reseller). Networks: 95.5% `eip155:8453`, 3% Solana mainnet,
<1% Base Sepolia/BNB/X Layer. Schemes: `exact` 11,309 entries, `upto` 120, `batch-settlement` 36.
Price per call (USDC, first `accepts` entry): min 0.001, **p25 0.005, median 0.01, p75 0.05**; 34% of
items under 0.01, 45% in 0.01–0.10, 18% in 0.10–1.00, 2% above. Keyword classification (mine — a
rough single-label pass over description + URL, so treat shares as ±5 points):

| Category | Share of sample | Median USDC | Typical hosts |
|---|---|---|---|
| market data / trading signals | 16.8% | 0.015 | kronossignals, defillama.use.x402atlas, signalpulse |
| web search / scrape / extract | 12.9% | 0.012 | api.kadec0 (papers), osf-master-server, vaaya |
| on-chain reads / RPC | 9.5% | 0.010 | pro-api.coingecko, api.onesource.io, rpc.nodeflare |
| LLM inference / agent gateways | 6.2% | 0.010 | farouter, gate402, blockrun |
| identity / compliance / **attested facts** | 5.9% | 0.015 | rubric-protocol, api.sirenic.eu, lionx402 |
| geo / weather / travel | 4.3% | 0.010 | vibesprings, stabletravel |
| media generation | 4.1% | 0.010 | loonie-toll, x402-seller |
| enrichment / leads | 3.6% | 0.040 | aeml-x402, solenrich, stableenrich |
| news / research | 2.7% | 0.030 | scholarpulse |
| sports / betting / social / fun / storage | 6.7% | ≤0.07 | twitter.use.x402atlas, penny402 |
| other (long-tail vertical "pulse" feeds, utilities) | 27.8% | 0.010 | ozmium, theaslangroupllc.* |

**Lens B — what actually settled (7-day on-chain scan, 1,009 sellers, third-party index).**
`agent402.tools/api/leaderboard` (spec `x402-leaderboard/1`, asOf 2026-09-02T07:51Z, 302,400 Base
blocks, calls ≤ $0.75) — a seller-run index, so cite as "one on-chain scan", not truth:

| Rank | Seller | Calls settled (7d) | USDC (7d) | Unique buyers | What it is |
|---|---|---|---|---|---|
| 1 | StableEnrich | 35,953 | 780 | 142 | people/company enrichment + Exa/Firecrawl/Serper resale |
| 2 | x402.twit.sh | 29,639 | 178 | 62 | X/Twitter data |
| 3 | agents.chain.link | 13,084 | 139 | **2** | Chainlink data — two buyers, machine loop |
| 4 | BlockRun.AI | 7,072 | 127 | 186 | 55+ LLM gateway + tools |
| 5 | api.strale.io | 1,457 | 78 | 12 | — |
| 7 | Academic Paper Search (kadec0) | 2,213 | 70 | 13 | scholarly search |
| 13 | api.loyalspark.online | 4,460 | 33 | 80 | on-chain reads |
| 12 | DNS lookup (agent402) | 2,790 | 38 | 32 | deterministic utilities |
| 17 | Nansen | 1,562 | 26 | 36 | labelled on-chain research |
| 25 | **api.onesource.io** | 3,614 | 15 | **451** | plain Ethereum RPC reads (`erc20-balance`, `tx/:hash`, `block/:number`) — the **widest buyer base in the top 25** at $0.003–0.008 a call |

**Lens C — 30-day explorer totals (x402scan via x402 Inc, 2026-05-30):** 3.69M tx, $1.11M, 189,900
buyers, 43,000 sellers, ~$0.30 mean. Top by volume: StableEnrich (108k tx), BlockRun (85k), HYRE DeFi
intel (32k), twit.sh (22k), weather.hugen.tokyo (21.8k, 242 buyers), Nansen (6,950). Chainalysis
(2026-06-03): "Transactions of $1+ now represent 95% of total volume", use cases named: "premium data
feed access, sanctions screening services, credit bureau queries".

### 2.2 Top 15 things agents pay for today (evidence-ranked)

| # | Ask | Evidence (lens) | Verdict for us |
|---|---|---|---|
| 1 | **People / company enrichment** (email, firmographics, LinkedIn-shaped data) | A 3.6% at the highest median price (0.04); B #1 StableEnrich 36k calls/7d; C #1 | NOT-US |
| 2 | **Social data** (X posts, profiles, Farcaster) | B #2 twit.sh 30k calls; A 2% | NOT-US |
| 3 | **Web search / scrape / extract / academic search** | A 12.9%; B #7 kadec0; Exa/Tavily/Firecrawl resold everywhere | NOT-US |
| 4 | **LLM inference gateways** (per-token resale, model routing) | A 6.2%; B #4 BlockRun 186 buyers; C #2 | NOT-US |
| 5 | **Plain on-chain reads / RPC** (`balanceOf`, tx by hash, block header, ENS, gas) | A 9.5%; B onesource **451 buyers** in 7d, loyalspark 80; Nodit selling RPC on x402 (§1 #5); Chainlink loop | **NOT-US as a per-call toll** [brief Claim 2] — the live read stays free (`/api/xrpl`, `?preview=1`). The ask maps to the **provable archive**: "what was `lsfGlobalFreeze` on this issuer at time T, with the root it was in" — sold as a historical evidence pack (§3 B) |
| 6 | **Market data / trading signals / DeFi intel** | A 16.8% (largest category); B Nansen, Kronos; C HYRE | NOT-US (signals = advice-shaped; §6) |
| 7 | **Sanctions / wallet screening / KYA pre-pay gates** (OFAC SDN, EU/UN lists, PASS/BLOCK) | A: lionx402 (0.001–0.05), x402atlas wallet-sanctions, rubric attested-screening, eagle.quickai, GlobalAPI, relaystation, StillOS `agent-clearance`, payperbyte "Know-Your-Agent"; Chainalysis names "sanctions screening" | NOT-US (verdict-shaped; counsel gate) |
| 8 | **Trust / reliability checks of x402 counterparties before paying** (uptime, demand class REAL/SOLO/PROBE/WASH, wallet history, prepay trust) | A: x402.fuchss "trust score 0-100, grade A-F", orcpin endpoint-reliability, aeml `trust/precheck`, AI Growth `verification` ("timestamped proof-of-execution receipts"), x402oracle trade-check, gpt55 prepay checks, agentdata-nl counterparty check, rubric `wallet-record` | NOT-US as a score. The *shape* is our shape (signed, recomputable receipts) — see #10 |
| 9 | **Attested / signed facts** — the seller signs the response so the agent can carry it | A: rubric "Attested Hedera network facts… ML-DSA-65 attestation" (0.001), lionx402 "Ed25519-attested" reads, Touchstone "every answer is signed; same input → same bytes" (0.001–0.003), Tollbooth "reproducible SHA-256 hash + free golden-vector self-check", x402-endpoints "offline-verifiable signed receipt", eagle "signed audit receipts", StillOS "Every response Ed25519-signed… verify_receipt… always free" | Split: the *re-serve of already-public signed cards* per call is **NOT-US as a toll** [brief Claim 2]; the **commissioned attestation** (a named subject, a named question, point-in-time, T&C-bound) is **CAN-SERVE-TODAY** via `request-attestation` (§3 C). The 0.001–0.05 market price tells you the toll is worth ~£0 — it is distribution, not revenue |
| 10 | **Hash anchoring / notarisation / timestamp of the agent's own output** | A: anchor-x402 "Anchor a 32-byte hash to Base + Solana mainnet — $0.005"; ForgeMesh "Notary — digital attestation & timestamping"; StillOS `commit_claim` "SHA256-chaining and Ed25519-signing for permanently verifiable receipts"; Touchstone `/timestamp` and `/sealed`; arXiv "Notarized Agents: receiver-attested confidential receipts" (2026-06); plus cheap plain hash compute (agent402 `/api/hash`, keyronne `/hash`, hash-hmac at 0.001–0.01) | **CAN-SERVE-WITH-~6-HOURS** — `witness-my-hash` (§3). Nobody in the sample offers a Sigstore-Rekor + OpenTimestamps witnessed Merkle root; every competitor anchors to a chain or self-signs. Tokenless, cheap, our exact shape |
| 11 | **Official-registry reference facts** (GLEIF LEI, VIES VAT, Companies House, gazetted central-bank rates, Japan corporate numbers) with citations | A: compliancepulse LEI, afriref (406 series "every value cited to its official gazette"), Kaisha, agentdata-nl, GlobalAPI, x402-endpoints | **NOT-US as a per-call toll**; the facts we hold (Swift census, XRPL issuer directory, fines) stay free readers. Sellable only when assembled into a **per-request evidence pack** for one obligation (§3 A) or served as **provable history** (§3 B) |
| 12 | **Package / supply-chain checks** (npm/PyPI existence, malicious-release scan, dependency health) | A: attester.dev, Lazaretto, stride20k, Signal Oracle DepWatch | NOT-US |
| 13 | **Site / API audits** (security headers, SEO, AI-readiness, `llms.txt` grading) | A: x402atlas headers, minifetch, ozdreamtools `agent-ready/assess`, kadec0 website-audit, Tinstop | NOT-US (we publish our own `/api/clarity` record; we do not grade others' sites) |
| 14 | **Regulatory-change feeds and compliance briefs** ("RegWatch 55 verticals", "regulatory changes affecting AI systems", $2 AI-compliance briefs) | A: rubric `regwatch-feed` (0.05), Signal Oracle, Sentinel Intelligence ($2/$10) | **CAN-SERVE-TODAY, and it is Rank 1** [brief Part 3 Rank 1]: the Article 50 marking-evidence pack (§3 A), GBP-invoiced, before the 2 Dec 2026 marking deadline; `evidence-bundle` for article-53 / dora / cra ship with the honesty note; `/api/fines` stays free |
| 15 | **Model / provider metadata for routing** (which model, licence, gated?, last-modified, config hash; "benchmarks" for endpoint liveness) | A: AI Growth `benchmarks`, Locus/farouter model routers, cheaptokens; Hub-metadata reads are what routers need before they spend | **NOT-US as a per-call toll** of Hub metadata [brief Claim 2]. Two honest forms only: a **commissioned point-in-time attestation** of one model listing at a named revision (§3 C, ~5 h), or the **provable archive** of licence/gated/revision changes over time (§3 B). DISCOVERED, never MEASURED |

What is **absent** from all three lenses: nobody pays for a *grade*, a *rank* or a *certificate* of an
AI model. The demand that touches us is narrower and better: **signed, recomputable, third-party
receipts of public facts, and witnessed proof that a given byte-string existed at a given time.**
Agents buy those at 0.001–0.05 USDC a call, thousands of times a week, from sellers who self-sign —
which is the brief's point [brief Claim 2, Rank 5]: at those prices a per-call toll on public facts is
~£0 of revenue and a distribution channel at best. What survives is the **shape** — non-repudiable,
timestamped, methodology-backed evidence a buyer can show a regulator, auditor, insurer or court — sold
as per-request packs, commissioned attestations and provable history. Our differentiator there is the
witness chain (root → Rekor → OTS), the published methodology, and a public verifier that is free.

---

## 3. Serviceability — fitted to the four sellable shapes

Per **[brief Claim 2, Part 3 Rank 1–2, Part 7 §3]** only four things are sold: **A** per-request evidence
packs, **B** the provable archive (live flags free, provable history sold), **C** commissioned
attestations, **D** `witness-my-hash`. Per-call tolls on public data are NOT-US (§6). Legend: **TODAY** =
code on master (or the open PR named) needing only deploy + facilitator env; **N-HOURS** = smallest honest
addition, an engineering estimate; the *rail* (x402 USDC or a CSOAI LTD GBP invoice) is the buyer's
choice — `_skus.ts` already marks packs `x402-or-invoice`.

| Shape | Agent asks it absorbs (§2.2) | Status | Endpoint / smallest honest addition | Free forever | What is sold (never a verdict) |
|---|---|---|---|---|---|
| **A. Per-request evidence pack — Article 50 marking evidence** (Rank 1) | #14, and the buyer-supplied-output form of #9 | **TODAY for the assembled pack; ~1 DAY for the per-output measurement** | Today: `GET /api/evidence-bundle?obligation=article-50&subject=<s>&bundle=1` returns OSCAL 1.1.0 observations from already-signed leaves + the signed ProvBench pack (`/packs/eu-article-50/*.sig.json`, C2PA-marking durability under transforms) + the verbatim Art 50(2) obligation text and fine ceiling (`counsel_confirmed: true`). Addition (~1 day): `POST /api/article50/measure` — buyer uploads (or names by URL) one generative output; we run the ProvBench probe set (C2PA manifest present / signature valid / binding intact / `digitalSourceType`; SynthID-style detectors where public) and issue ONE signed card-v0 recording the **measured fact at time T beside the verbatim Art 50(2) text**, hashes of the input and of each probe result, `unmeasured[]` for anything not run. Reuses `scripts/evidence-pack-generate.mjs` and `functions/api/article50.ts` (replace the HMAC free tier's 501 with the Ed25519 path already used by the metered endpoints). | the obligation record, the existing pack, `/api/fines`, `verify` | the assembled, signed, point-in-time evidence artefact; **never** "compliant". Watermarking is spoofable, so the card says point-in-time measurement, not a guarantee [brief Rank 1 caveat] |
| **A. Per-request evidence pack — other obligations** | #14 | **TODAY** | same endpoint, `obligation=article-53 \| dora \| cra` — ships `counsel_confirmed:false` + honesty note until counsel widens the gate (legal gates widen only via counsel) | preview without `bundle=1` | assembly |
| **B. Provable archive — XRPL issued-asset permission flags** (Rank 2) | #5, #11 | **COMPOUNDING since 2026-08-31; ~1 DAY to sell a slice** | The hourly root already stages `xrpl.asset.state` leaves (88 on disk) and witnesses each root. Addition: `GET /api/archive?asset=<symbol>&from=<iso>&to=<iso>` → 402 → ONE signed manifest card + the ordered list of historical leaves for that asset in the window, each with its root, inclusion proof, Rekor log index and OTS status; **live state stays free** (`/api/xrpl`, `?preview=1`). Prerequisite check: superseded roots must be retained (git history is, dated witness files are; a root index file is the ~2 h fix if not). Scope honesty: XRPL-native issued assets only (RLUSD, Ondo USDY/OUSG slice); **not BUIDL, not BENJI** [brief Claim 1]. The brief's Ethereum-first archive is a separate N-day lane, not this document. | current state, one inclusion proof, `root.json`, the witness file | provable history: existence + integrity of each flag reading at each hour, with a third-party witness |
| **B. Provable archive — Hub model listings** | #15 | **~1 DAY (recorder) + reuse of the slice endpoint** | new adapter `scripts/adapters/hub_listing.py` staging one `public.notice` leaf per watched model per hour (licence, `gated`, `pipeline_tag`, HF revision sha, `config.json` sha256, `lastModified`) — DISCOVERED counters labelled as such, never a board cell (`hub_cite.py` rule stands). Sold through the same `/api/archive` slice. | the Hub API; the latest leaf | provable history of a listing's terms |
| **C. Commissioned attestation — named subject, named question, point-in-time** | #9, #15, the RPC/TOML ask of #5 when a buyer commissions it | **TODAY** (`request-attestation`); **TODAY after PR #1158 merges** for the XRPL form | `GET /api/request-attestation?subject=<id>[&axis=]` → `ras.commission` card-v0 citing the settle tx/invoice, the reserve pointers, `fresh_run: UNMEASURED` (a payment never mints a MEASURED cell). `GET /api/rwa/evidence?asset=<symbol\|issuer>` (PR #1158) → ONE `public.notice` card of one asset's AccountRoot flags, obligations and the **two-way `xrp-ledger.toml` check** (the owner's "TOML check"), every raw fetch sha256'd, `?preview=1` free. Reframe both in the 402 description as *commissioned, point-in-time, on request* — never a continuously published figure [brief Part 2 §1 safeguard 3]. Private commissions for an AI company are the GBP-invoice form of the same artefact [brief Rank 4]. | `/api/gspc`, `/signed/card-matrix.json`, `/api/xrpl`, `?preview=1`, `/gspc-verify` | issuance + independent signature |
| **D. `witness-my-hash`** | #10 | **~6 HOURS** | `GET /api/witness?sha=<64hex>` → 402 (`extensions.bazaar` declared) → on settle: `REVENUE_KV` `witness:<sha>` = `{sha256, settle_tx \| invoice_ref, requested_at}`; new adapter `scripts/adapters/witness_requests.py` reads the free KV-backed queue (`GET /api/witness/queue`) in the hourly `public-root.yml` run and stages each as a `public.notice` leaf (kind `csoai.witness.request/0.1`, ≤3072 canonical bytes, signed by the one writer) → next `root.json` → `witness_public_root.py` puts the root in Rekor (`rekord`) and OTS. Buyer gets a receipt card now (queue position, next-root ETA) and, after the hour, `/api/proof?sha=<leaf>` free forever + `/interop/root-witness-latest.json`. Fail closed: no settle/invoice → nothing queued; no `REVENUE_KV` → 402 names the missing store. Comparator: eIDAS qualified timestamping sells from ~€500/month [brief Part 3 anchors] — so the honest commercial form is **per request or a monthly retainer invoiced**, with the x402 door as the agent-facing entry. Nobody in the Bazaar sample offers a Rekor + OTS witnessed Merkle root; competitors anchor per-leaf to a chain (anchor-x402) or self-sign (StillOS, ForgeMesh). | `/api/proof`, `/root.json`, Rekor, OTS, the verifier | inclusion of the buyer's hash in the next witnessed root — existence/time of bytes, **nothing about their meaning** |
| paid MCP tools | any of A–D | **~3 HOURS after the above** | `witness_hash`, `commission_attestation`, `archive_slice` as *paid* tools on `/mcp` with the same 402 body (Vercel `x402-mcp` `paidTool` pattern, 2025-09-12); the seven free tools stay free | `verify_inclusion`, `get_root`, `get_card`, … | — |
| #8 counterparty trust score, #7 sanctions verdicts, #1–#4, #6, #12, #13 | — | NOT-US | — | — | §6 |

Every artefact above is **discrete, point-in-time, per request**: no composite, no ranking, no continuous
reference rate, no aggregation across assets into one number [brief Part 2 §1 safeguards 2–4]. The T&C block
in §4.4 travels with each of them.

**Hours are engineering estimates for one lane** (Function + adapter + tests + docs), assuming `REVENUE_KV`
is bound (§5) and `BOARD_SIGN_KEY_PKCS8_B64` is present on Pages. Without them the endpoints must answer
402/501 naming the missing piece — never a receipt for work that cannot happen.

---

## 4. The "stack on top" rule — copy for `llms.txt` and the 402 descriptions

The plain read is free. What is sold is the signed, witnessed **evidence** of a read — a per-request pack, a
commissioned point-in-time attestation, a slice of provable history, or the witnessing of the buyer's own
hash. Never a rank, never a verdict, never a certificate, never a per-call toll on a public fact, never a
continuously published figure. Every paid artefact is recomputable for free by anyone with the public key.

### 4.1 `public/llms.txt` — new section (drop in under "Verify and connect")

```
## Paid — what is sold and what is never sold

Every read here is free, unauthenticated, forever: GET /api/gspc, /api/xrpl, /api/swift, /api/fines,
/root.json, /api/proof?sha=<64hex>, /mcp (seven tools), the latest state of anything we record.

What can be commissioned (HTTP 402 → USDC on Base, or a CSOAI LTD invoice in GBP; amounts live only
inside a 402 accepts[] — never on a page, never in this file):
- a per-request EVIDENCE PACK mapped to one obligation (EU AI Act Article 50 marking evidence;
  Article 53, DORA, CRA with the counsel note) — signed observations beside the verbatim obligation
  text; determination: NONE;
- a COMMISSIONED ATTESTATION: one named subject, one named question, point-in-time, on request —
  Ed25519 under did:web:csoai.org#board-attestation-1, ≤3KB canonical bytes, a leaf of the next
  public root, the ONE root witnessed in Sigstore Rekor and OpenTimestamps;
- PROVABLE HISTORY: the signed hourly record of a public fact (e.g. an XRPL issuer's permission
  flags) with each reading's root, inclusion proof and witness — the live flag is free, the
  history is sold;
- WITNESS-MY-HASH: inclusion of YOUR sha256 as a public.notice leaf in the next witnessed root —
  existence/time of bytes, nothing about their meaning.

Never sold: a score, a rank, a pass/fail, a certificate, a place on the board, a "safe" or
"compliant" verdict, a per-call toll on a public fact, a continuously published reference figure,
verification (free forever). A payment never mints a MEASURED cell. Catalogue: /api/x402.
Verify anything: /gspc-verify · /api/proof?sha= · /interop/root-witness-latest.json.
Terms: outputs are point-in-time measurements provided for information; not for use in determining
the value or payout of any financial instrument or financial contract; not a benchmark; not a
creditworthiness opinion.
```

### 4.2 402 `resource.description` (≤500 chars; the Bazaar shows this string)

- **evidence-bundle (article-50):** "Per-request Article 50 marking-evidence pack: OSCAL 1.1.0 observations from already-signed CSOAI cards and the signed ProvBench marking-durability pack, beside the verbatim Art 50(2) text and fine ceiling. Point-in-time measurement, never a compliance opinion; determination: NONE. Preview free without bundle=1. Also available as a GBP invoice."
- **request-attestation:** "Commissioned, point-in-time attestation of one named subject, on request. Issues an Ed25519-signed receipt card naming your settle tx and every signed measurement card already on file; the board read is free at /api/gspc. Never a score, rank or certificate; a payment never mints a MEASURED cell. Not a benchmark; not for financial-instrument valuation."
- **rwa/evidence:** "Commissioned point-in-time card of one XRPL issued asset's on-ledger state (AccountRoot flags, obligations, two-way xrp-ledger.toml check), every raw fetch sha256'd, included in the next witnessed public root. The live read is free at /api/xrpl and ?preview=1. Historical state at fetched_at — not a rating, not a guarantee, not for use in valuing any financial instrument."
- **archive (proposed):** "Provable history: the signed hourly readings of one public fact over a window, each with its root, inclusion proof, Rekor log index and OpenTimestamps status. The live reading is free. Point-in-time facts, no composite, no rate; not a benchmark."
- **witness (proposed):** "Puts your sha256 into the next signed public root as a public.notice leaf; the root is witnessed in Sigstore Rekor and OpenTimestamps. Proves existence/time of bytes, nothing about their meaning. Inclusion proof free forever at /api/proof?sha=."

### 4.3 The three sentences every paid response carries (`note` field)
1. "You could have recomputed this for free; you paid for our signature, our citation of the raw bytes, and inclusion in a witnessed public root."
2. "This is measurement/witness, not certification. It carries no verdict, no grade, no opinion on creditworthiness or safety."
3. "Verify: /gspc-verify · /api/proof?sha= · /interop/root-witness-latest.json."

### 4.4 T&C block — `terms` field on every paid response, and one paragraph on `/pricing-free` [brief Part 2 §1–2]
- Provided for information as a point-in-time measurement; **not for use in or for the determination of the value or payout of any financial instrument or financial contract; not a benchmark** (UK BMR / Reg 2016/1011 sense) and not offered as one.
- No composite score, ranking, weighting, aggregation across subjects, or continuously updated reference figure is published or implied; each artefact is discrete and on request.
- Not a credit rating, creditworthiness opinion, or assessment of whether anything is "safe" or "risky"; the buyer's auditor, regulator or counsel keeps every determination.
- Recomputable for free; the signature attests issuance by CSOAI LTD (UK 16939677) at the stated time, nothing more.
- Words that must not appear in our framing of any output: "oracle", "risk score", "rating", "safe", "compliant", "certified" [brief Part 7 §4]. (Third-party sellers' product names quoted in §2 are quotations, not our framing.)
- Counsel questions the brief lists for a UK FCA-perimeter solicitor stay open; this document widens no gate.

---

## 5. Listing steps — who can do what, without a paid account

**Step 0 (owner, blocks everything):** get master onto prod. `curl -s https://councilof.ai/api/x402 | head`
must return `csoai.x402-catalog/0.1`. If still 404: `gh auth refresh -h github.com` then
`gh workflow run deploy.yml --ref master` (never `wrangler pages deploy` from a laptop — CLAUDE.md).

| # | Registry / channel | Account needed? | Mechanism | Who | Status |
|---|---|---|---|---|---|
| 1 | **`/.well-known/x402.json`** on councilof.ai | No | already on master (`functions/.well-known/x402.json.ts`, 4 resources, mode from env). Add `/api/rwa/evidence` (PR #1158) and `/api/witness` when they land. Crawlers that read it: agent402.tools index, x402scan (mechanism **UNVERIFIED**), wavect/awesome lists | deploy | waiting on step 0 |
| 2 | **CDP Bazaar** (`discovery/resources`) | **Yes — CDP account + API key (owner)** | set Pages env `X402_FACILITATOR_URL=https://api.cdp.coinbase.com/platform/v2/x402` + `X402_FACILITATOR_TOKEN=<CDP key>`; the first CDP-settled payment whose 402 carried `extensions.bazaar` indexes the route (Binance's compatible B402 says ~30 s; CDP lag unstated; #2112 shows it can silently fail). Our routes already declare the block and omit the invalid `discoverable:true`. If the owner prefers keyless PayAI (`https://facilitator.payai.network`, 1,000 free settlements) the rail is live but **not** Bazaar-indexed — the two are mutually exclusive per route unless we run two challenges. Discovery *reads* need no key (probed). | owner (key, env) | not started |
| 3 | **Agentic.Market** (Coinbase directory, 2026-04-20) | derived from Bazaar; no self-serve found (**UNVERIFIED**) | nothing beyond #2 | — | follows #2 |
| 4 | **awesome-x402** (`xpaysh/awesome-x402`, community README) | No — a GitHub PR | one README line under the compliance/trust section. Draft (owner opens the PR; an agent posts nothing): `- [Council of AI — signed public-root cards](https://councilof.ai) - Independent measurement body (CSOAI Ltd, UK 16939677). Free readers (/api/gspc, /api/xrpl, /api/swift, /root.json, /api/proof); paid signed Ed25519 cards of public reads and OSCAL evidence bundles over x402 USDC on Base, every root witnessed in Sigstore Rekor + OpenTimestamps. Never a score, rank or certificate; verify free. ([Discovery](https://councilof.ai/.well-known/x402.json) | [MCP](https://councilof.ai/mcp) | [llms.txt](https://councilof.ai/llms.txt))` | owner | drafted here |
| 5 | **Official MCP Registry** (`io.github.CSOAI-ORG/gspc` 1.1.1, already listed) | GitHub OAuth (owner) | a paid `witness_hash` tool = new `server.json` version via `mcp-publisher` after §3 ships; the seven free tools are unchanged | owner | after §3 |
| 6 | **Smithery / Glama / mcp.so / PulseMCP** | already staged in `mcp/gspc-server/REGISTRY-SUBMISSIONS.md` | unchanged; paid tool appears on re-scan | owner | as before |
| 7 | **agent402.tools open index** (`/api/find`, `/api/leaderboard`) | No | crawls the Bazaar (`bazaarTotal: 15080`) and on-chain settles; we appear once #2 indexes us or once any settle lands in its scan window | none | automatic |
| 8 | **x402scan** | **UNVERIFIED** — server pages exist (`x402scan.com/server/<uuid>`); the JS app exposed no public API to probe; indexing appears to follow facilitator/on-chain settles | none known | automatic if #2 |
| 9 | **A2A registries** (a2aregistry.org one-POST; a2a-registry.org PR) | No | already staged in `REGISTRY-SUBMISSIONS.md` (agent card is live at `/.well-known/agent-card.json`); add the paid skills to the card when §3 ships | owner runs the curl | staged |
| 10 | Binance B402 Bazaar | Binance OnchainPay account | separate ecosystem, BNB Chain — **NOT now** | — | skip |

Also required from the owner for any of the above to produce a settled receipt: `BOARD_SIGN_KEY_PKCS8_B64`
present on Pages (else paid cards ship `sig_ed25519:null`, declared); `REVENUE_KV` bound (else counts stay
null and `witness-my-hash` cannot queue); the `pack.councilof.ai` mock retired so no crawler follows a
stale door.

---

## 6. NOT-US — one line each

| Do not sell | Why (one line) |
|---|---|
| Gating `/api/gspc`, `/root.json`, `/api/proof?sha=`, `/mcp` read tools | verification is free forever; the free read is what makes the paid signature worth anything |
| **A per-call toll on any public fact** (RPC read, TOML check, Hub metadata, registry lookup, "signed read of X") | nobody pays retail per-call for signed public data [brief Claim 2]; the door may exist for distribution, the revenue thesis may not rest on it |
| A continuously published figure, composite, ranking or "reference rate" of anything on-chain | that is the line into benchmark administration [brief Part 2 §1]; ours are discrete, point-in-time, on request |
| Anything framed as an "oracle", a "risk score", or a statement of what is "risky" | CRA/BMR perimeter self-harm [brief Claim 4, Part 7 §4]; we publish facts beside verbatim regulation text |
| Claims that BUIDL, FOBXX/BENJI or any flagship RWA is read on XRPL | false [brief Claim 1]; the XRPL reader sees issued assets on XRPL (RLUSD, part of Ondo USDY/OUSG) and says so |
| Scraping or benchmarking an AI provider against its ToS on the strength of a "governance safe harbor" | no such safe harbor exists in law [brief Claim 5]; measure public artefacts and published APIs only |
| A rating, trust score, "grade A–F", 0–100 for any model, wallet, endpoint or agent | a rank is never sold; the moment money correlates with position the measurement claim is gone |
| A "safe" / "compliant" / PASS-BLOCK verdict (sanctions screening, KYA, DeFi protocol verdicts) | verdict-shaped and counsel-gated; we issue observations with `determination: NONE` |
| Custody, escrow, prepaid balances, credits, tokens | financial-instrument firewall; EIP-3009 moves buyer → estate directly, we hold nothing of the buyer's |
| Trading signals, market data resale, DeFi yield feeds | advice-shaped; also the most crowded category in the Bazaar (16.8%) with nothing of ours in it |
| LLM inference resale, enrichment, scraping, social data | commodity resale, no measurement in it, KYC/ToS exposure |
| "Pay to be measured", "pay to be listed higher", fresh MEASURED cells minted by payment | a payment never mints a MEASURED cell (`request-attestation` returns `fresh_run: UNMEASURED`) |
| Selling the Rekor/OTS witness of *our own* root | the ONE root is witnessed for free as public infrastructure; only the buyer's *own* hash is a paid leaf |
| Anchoring N leaves to a chain per request (anchor-x402's shape) | ONE root, never N leaves; chain anchoring is a token-adjacent surface we do not open |
| Grading other people's sites/APIs (AEO, headers, SEO) | we publish our own `/api/clarity`; grading others is a rank |
| Certificates of conformity, seals, badges that assert compliance | measurement, never certification (`assertNoSaleOfGrade()` throws at import) |

---

## 7. What to do first — fitted to time-to-first-pound [brief Part 3, Part 7]

**Rank 1 — Article 50 marking-evidence pack, GBP-invoiced, before 2 Dec 2026.** Assembled pack is
**TODAY** (`/api/evidence-bundle?obligation=article-50&bundle=1` + the signed ProvBench pack); the per-output
marking measurement is ~1 day (§3 A). One EU-facing generative-AI vendor, one invoice from CSOAI LTD. The
x402 door on the same endpoint is the agent-facing entry, not the plan.

**Rank 2 — Provable archive.** Already compounding hourly since 2026-08-31 for XRPL issued assets (88
`xrpl.asset.state` leaves, every root witnessed). Keep it running; add the `/api/archive` slice (~1 day,
§3 B); the Ethereum-first recorder the brief asks for is a separate lane and must not be dressed up as
existing.

**Rank 3 — Commissioned attestations** (`request-attestation` today; `/api/rwa/evidence` after PR #1158;
private GBP-invoiced measurement for an AI company is the same artefact, undistributed).

**Rank 4 — `witness-my-hash`** (~6 h) — the one agent ask in the sample nobody serves with a Rekor + OTS
witnessed root; sell per request or as a monthly retainer, with the x402 door for agents.

**x402 itself: distribution and optionality, ~£0 this quarter** [brief Rank 5]. Do step 0 of §5 (deploy),
set the facilitator, list on the Bazaar for discovery, and watch Agentic.Market volume by category; escalate
only if an agent buyer pays repeat fees for evidence packs.

### Top-5 CAN-SERVE-TODAY, one line each
1. **Article 50 evidence pack** — `GET /api/evidence-bundle?obligation=article-50&subject=<s>&bundle=1` (master) + `/packs/eu-article-50/*.sig.json`; invoice in GBP or x402.
2. **Commissioned attestation of a named subject** — `GET /api/request-attestation?subject=<id>` (master; the live 402 answers now, unpayable until deploy + facilitator).
3. **Other obligation packs with the counsel note** — `obligation=article-53 | dora | cra` (master).
4. **XRPL asset point-in-time card incl. the two-way TOML check** — `GET /api/rwa/evidence?asset=<symbol>` (PR #1158: merge, then deploy); live state free.
5. **Inclusion-proof bundle of the last witnessed root** — `GET /api/proof?bundle=1` (master; single proofs free forever) — the free half of the provable archive, until `/api/archive` ships.

Smallest additions, in order: `/api/article50/measure` (~1 day), `/api/archive` slice (~1 day),
`witness-my-hash` (~6 h), hub-listing recorder (~1 day), paid MCP tools (~3 h).

---

## 8. Sources (fetched/probed 2026-09-02 unless dated)

Binding brief: `~/Downloads/compass_artifact_wf-b4603903-db3d-5d8b-8f56-6e69ba760f97_text_markdown.md` (owner-supplied, 2026-09-02; Parts 1–3, 7). Estate bytes: `https://councilof.ai/api/x402` (404) · `/.well-known/x402.json` · `/api/request-attestation?subject=gpt-4o` (402) · `/api/evidence-bundle` (404) · `/api/rwa/evidence` (404) · `/api/eunomia-data` (402) · `/api/proof?sha=00…00` (404 honest) · `/root.json` · `/interop/root-witness-latest.json` · `/mcp` · `/api/xrpl` · `/api/swift` · `/signed/card_index.json`; repo `functions/api/_x402.ts`, `_x402_config.ts`, `_skus.ts`, `x402.ts`, `request-attestation.ts`, `evidence-bundle.ts`, `proof.ts`, `functions/.well-known/x402.json.ts`, `functions/mcp/[[path]].ts`, `scripts/witness_public_root.py`, `scripts/publish_public_root.py`, `scripts/adapters/staged_leaves.py`, `scripts/adapters/hub_cite.py`, `scripts/adapters/benji.py`, `scripts/adapters/xrpl.py`, `functions/api/_obligations.ts`, `functions/api/article50.ts`, `public/packs/eu-article-50/`, `public/cards/` (131 leaves), `.github/workflows/public-root.yml`, `mcp/gspc-server/{README,REGISTRY-SUBMISSIONS}.md`, `public/llms.txt`, `docs/REVENUE-LOOPS.md`, `docs/REVENUE-RESEARCH-2026-09-02.md`, `docs/X402_BAZAAR_DISCOVERABLE_2026-09-02.md`; PR #1158 `functions/api/rwa/evidence.ts` (worktree `/private/tmp/coai-revenue`).

Network probes: `https://api.cdp.coinbase.com/platform/v2/x402/discovery/resources?limit=100&offset=0…2900` (200, total 15,083) · `…/discovery/merchants` (401) · `…/x402/supported` (401) · `…/discovery/mcp` (405 GET) · `https://facilitator.payai.network/supported` (200) · `https://x402.org/facilitator/supported` (200, EVM testnet only) · `https://facilitator.live.nevermined.app/supported` and `/api/v1/x402/supported` (404) · `https://agent402.tools/api/leaderboard` (200, asOf 2026-09-02T07:51Z) · `https://docs.cdp.coinbase.com/llms.txt` · `https://raw.githubusercontent.com/MetaMask/smart-accounts-kit/main/skills/x402-payments/SKILL.md` · `https://raw.githubusercontent.com/xpaysh/awesome-x402/main/README.md`.

Web: [CDP Bazaar](https://docs.cdp.coinbase.com/x402/bazaar) · [x402 gitbook Bazaar](https://x402.gitbook.io/x402/core-concepts/bazaar-discovery-layer) · [x402#2112](https://github.com/x402-foundation/x402/issues/2112) · [AWS AgentCore Bazaar](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/payments-connect-bazaar.html) · [Binance B402 Bazaar](https://developers.binance.com/docs/onchainpay-x402/b402-bazaar) · [cryptobriefing Agentic.Market, 2026-04-20](https://cryptobriefing.com/agentic-market-ai-agents-hub/) · [Eco x402 explainer](https://eco.com/support/en/articles/14839402-x402-protocol-explained) · [Eco Agentic Wallets](https://eco.com/support/en/articles/14845485-coinbase-agentic-wallets-explained) · [agenteconomy.to](https://agenteconomy.to/stats/x402-transactions) · [web3trackers, 2026-04-14](https://www.web3trackers.com/x402-dashboard) · [x402 Inc on x402scan, 2026-05-30](https://note.com/x402inc/n/nfd6227f13b55?hl=en-US) · [Chainalysis, 2026-06-03](https://www.chainalysis.com/blog/x402-agentic-payments-adoption/) · [arXiv 2607.19545, 2026-07-21](https://arxiv.org/html/2607.19545v1) · [Cloudflare Monetization Gateway, 2026-07-01](https://blog.cloudflare.com/monetization-gateway/) · [InfoQ, 2026-07](https://www.infoq.com/news/2026/07/cloudflare-aws-x402-micropayment/) · [wavect Cloudflare Wallets, 2026-08-05](https://wavect.io/blog/cloudflare-wallets-ai-agents/) · [Apify blog, 2026-06-30](https://blog.apify.com/introducing-x402-agentic-payments/) · [Apify docs](https://docs.apify.com/platform/integrations/x402) · [Nodit, 2026-03-24](https://blog.nodit.io/introducing-nodit-x402-payment-protocol/) · [Nevermined facilitator docs](https://nevermined.ai/docs/products/x402-facilitator/overview) · [CDP Spend Permissions](https://docs.cdp.coinbase.com/server-wallets/v2/evm-features/spend-permissions) · [AgentKit README](https://github.com/coinbase/agentkit/blob/main/typescript/agentkit/README.md) · [MetaMask x402 docs](https://docs.metamask.io/smart-accounts-kit/1.5.0/guides/x402/overview/) · [PayAI, 2026-05-29](https://blog.payai.network/largest-x402-facilitator-solana/) · [Vercel x402-mcp, 2025-09-12](https://vercel.com/blog/introducing-x402-mcp-open-protocol-payments-for-mcp-tools) · [ForgeMesh](https://forgemesh.io/) · [ERC-8004 empirical study, arXiv 2606.26028](https://arxiv.org/html/2606.26028) (Validation Registry: no confirmed mainnet deployment through May 2026 — the third-party-validator demand is designed, not yet transacting) · [Notarized Agents, arXiv 2606.04193](https://arxiv.org/html/2606.04193).
