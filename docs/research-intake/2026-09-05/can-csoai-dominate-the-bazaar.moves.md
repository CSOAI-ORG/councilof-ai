# Can CSOAI Dominate the Bazaar? — extracted moves

**Source brief:** `~/Downloads/compass_artifact_wf-029e44dc-562a-5909-8b8a-553615ef0de8_text_markdown.md`
**Mined:** 2026-09-05, TUI-5 (research-intake lane)
**Rule:** the `already live?` column is a PROBE RESULT taken at mine time, never an assumption.
Probe commands are in the Evidence column so a stranger re-runs them.

| move | lane-doable? | owner-gated? | already live? (probed) | evidence |
|---|---|---|---|---|
| Retire **\"85% broken\"** from all copy; quote the real conformance finding verbatim | partly (docs) | **YES** (public copy) | not present in the doors index; copy surface not probed here | — |
| Ship C2PA per-object door as reader-style MCP tool + x402 route | no — functions/ | no | `c2pa` slug EXISTS | slug probe |
| Ship XRPL per-object door | no — functions/ | no | 12 `xrpl` slugs exist; `/api/xrpl` 200 | `curl` /api/xrpl |
| List resulting cards on **CDP Bazaar, x402scan, agentic.market/x402-list** | no — external | **YES** | no door records these listings | slug probe |
| Register as **ERC-8004 validator** — cleanest standing-to-revenue conversion | no — wallet | **YES** | validation registry unshipped | brief |
| **Do not** open unsolicited PRs, sell any score/badge/index, or emit an aggregated market figure | n/a — doctrine | n/a | doctrine already bans priced grades | `scripts/price-gate.mjs` |
| `poai-bench`, `omai-bench`, `mcp-scoreboard`, `registry-harvest-xrpl-mica-lei`, 2 x402 datasets are **UNVERIFIED** | partly — a door could record verification state | no | **UNMEASURED** — not confirmed on any live page | brief STEP 1 |

## Doctrine collision to respect

The brief recommends listing on discovery indexes and registering as an ERC-8004 validator.
Both are owner-gated (wallet + external accounts). The brief's own guardrail matches repo
doctrine exactly: **sell no score, badge or index, and emit no aggregated market figure.**
`price-gate.mjs` already enforces the price half of that mechanically.
