# Verification-and-Mapping Brief — extracted moves

**Source brief:** `~/Downloads/compass_artifact_wf-d05b0725-b24a-58c0-a6d9-0a2d058787ee_text_markdown.md`
**Mined:** 2026-09-05, TUI-5 (research-intake lane)
**Rule:** the `already live?` column is a PROBE RESULT taken at mine time, never an assumption.
Probe commands are in the Evidence column so a stranger re-runs them.

| move | lane-doable? | owner-gated? | already live? (probed) | evidence |
|---|---|---|---|---|
| Publish the signed **x402 conformance census card** | no — signing | **YES** | `x402-discovery-fact` exists; census card not found | slug probe |
| Publish the **NVIDIA–HF platform-neutrality card** (verbatim 8-K quote + jurisdictions) | no — signing | **YES** | not found in doors | slug probe |
| Ship the **FLT recompute card** (VALID / INVALID / UNCHECKABLE) | no — signing | **YES** | three-state verdict matches `/verify` doctrine | estate |
| Ship **GPT-6 Astra fleet-delta + ExploitBench saturation-flag** cards | no — signing | **YES** | not probed | — |
| Continue **ERC-8004 validator registration** as the standing-to-revenue path | no — wallet | **YES** | validation registry unshipped | brief |
| Keep both per-object doors live; **build nothing gated** | n/a — doctrine | no | verification is free forever | `CLAUDE.md` doctrine |
| NVIDIA–HF price reconciliation: press \"$12.93bn\" = ~$11.9bn to stockholders + ~$1.0bn retention | n/a — quote precisely when used | no | n/a | 8-K; CNBC framing |

## Three-state discipline

Every card this brief asks for is a **VALID / INVALID / UNCHECKABLE** verdict, which is the
same grammar `/verify` already uses. Nothing here is lane-doable — all of it needs the signing
key — but the mapping matters for the delta: these are *signing-blocked*, not *undesigned*.
