# DELTA — what is missing, 2026-09-05

Merged from the 13 research briefs mined in this directory. **Ranked by (signal, revenue,
contracts, XRPL)** as the lane doc specifies, then by how cheaply the gap closes.

Every `already live?` verdict in the source `.moves.md` files is a **probe result taken at mine
time**, not an assumption. Where a probe could not settle a brief's claim, the row says
**UNMEASURED** — absent is not zero.

**Rows: 41.**

## Two brief claims that did not survive re-probing

| Brief said | Re-probed 2026-09-05 |
|---|---|
| `GET /api/axis-register` — *fetch failed* | **200.** The brief is stale. |
| MCP Registry: *keep `io.github.CSOAI-ORG/gspc` v1.0.0* | `isLatest` is **v1.2.0**, correctly pointing at `councilof.ai/mcp` (200). The dead `workers.dev` worker survives only on the superseded v1.0.0. |

## Two moves the briefs list as TODO that are already DONE

`llms.txt` (#57) returns **200**, and the agent-card `.well-known` (#58) returns **200**.
Strike both from the owner list.

---


## SIGNAL

| # | move | why it matters / what the probe found | lane-doable? | evidence a stranger re-runs |
|---|---|---|---|---|
| 1 | **GLEIF LEI door** (L1 + L2 Who-Owns-Whom) — free, keyless, redistribution-friendly, cross-links BIC/ISIN/MIC | The brief names it *the single best harvest target not yet exploited*. Estate has no `gleif`/`lei` slug at all. | **YES** | slug probe over `/.well-known/index.json` (193 doors) + `/interop/index.json` (240 formats) → 0 matches |
| 2 | **`interop/mcp-directories.json`** — machine-readable listing state for MCP Registry / Smithery / Glama / PulseMCP / mcp.so / Cline / Docker | 330 servers are in the official registry but nothing records the other five. Claim becomes checkable instead of asserted. | **YES** | `curl -o /dev/null -w '%{http_code}' https://councilof.ai/interop/mcp-directories.json` → 404 |
| 3 | **Anchor-posture door** — what is signed, what is anchored, what is neither | Deployed `root.json` carries `sig_ed25519` and **no** `ots`/`anchor`/`rekor` field. The estate preaches 'signed, re-attested' without publishing its own posture. | **YES** | `curl -s https://councilof.ai/root.json` → no anchor key |
| 4 | **EU AI Act Article 71 database door** | Art 71(4) legally requires a public, machine-readable, easily-navigable DB from 2 Aug 2026. High value, low competition, directly on the E-section thesis. | **YES** (must open UNMEASURED until the endpoint is probed) | no `article-71`/`art71` slug |
| 5 | **ESMA MiCA register door** (CASPS.csv; 331 authorised CASPs @ 2 Sep 2026) | `esma` slug exists but no `mica`. Third parties already normalise it — occupied niche, so enter with the signed-diff angle only. | **YES** | slug probe → `mica` 0 matches |
| 6 | **OSCAL crosswalk door** | No `oscal` slug in 193 doors; `.well-known/oscal.json` 404. Workspace canon claimed a 97-component OSCAL package that does not exist. | **YES** | `curl -o /dev/null -w '%{http_code}' https://councilof.ai/.well-known/oscal.json` → 404 |
| 7 | **`agents.json` / `.well-known/agents.json`** | Agent-discovery convention; `agent-card.json` is live but `agents.json` is not. | **YES** | `/agents.json` 404, `/.well-known/agents.json` 404 |
| 8 | **SEC EDGAR + OFAC SDN doors** — free, keyless, machine-readable, frequent updates | Strong signed-census targets with no estate presence. | **YES** | no `edgar`/`ofac` slug |
| 9 | **eIDAS trusted lists / EU Digital Product Passport / EU Transparency Register doors** | All broadly public; each is a separate diligence item per the brief. | **YES** (one door each, UNMEASURED until probed) | no `eidas`/`product-passport`/`transparency-register` slug |
| 10 | **410-article EUR-Lex legal-article-to-test crosswalk corpus** | Named *high-effort, defensible, directly monetisable*; COMPL-AI proves demand and is **static**. Only `nist-airmf-gspc-crosswalk` exists today. | **YES** (corpus door; the 410 articles are a build) | slug probe |
| 11 | **Software Heritage archive + SWHID citation** for the eval harness | Code permanence; no `swh`/`software-heritage` slug. | no — external archive | slug probe |
| 12 | **Wikidata Q141128616** statements (DOI, official website, described-by) | No estate record of the item; discovery surface for AI search. | no — owner posts | slug probe |
| 13 | **MLCommons Affiliate membership** (free, silent participant) | Standing in the benchmark community at zero cost. | no — owner joins | no `mlcommons` slug |
| 14 | **EU AI Pact** voluntary pledge (≥3 core actions) | Open to any organisation; cheap standing. | no — owner signs | no `ai-pact` slug |
| 15 | Smithery / Glama / PulseMCP / mcp.so / Cline / Docker MCP submissions | Six directory listings, none held. | no — external forms | slug probe → 0 matches |
| 16 | **`/api/verify` is 404** | The brief flagged it; re-probed and confirmed still missing while `/gspc-verify` page exists. A verification body with no `/api/verify` is a discovery gap. | no — `functions/`, outside this lane's file area | `curl -o /dev/null -w '%{http_code}' https://councilof.ai/api/verify` → 404 |
| 17 | AI Incident DB / AIAAIC / MITRE ATLAS submissions | No `atlas` slug; three permissionless incident registries unused. | no — owner posts as CSOAI | slug probe |

## REVENUE

| # | move | why it matters / what the probe found | lane-doable? | evidence a stranger re-runs |
|---|---|---|---|---|
| 18 | **`npm publish --provenance` on csoai-gspc-mcp** | **Confirmed missing**: `dist` on 0.2.1 has no `attestations` key. Provenance is the cheapest credibility unit available and it is free. | no — CI + repo | `curl -s https://registry.npmjs.org/csoai-gspc-mcp/latest` |
| 19 | **PyPI Trusted Publishing + PEP 740 attestations** | `csoai` is live on PyPI (200) but attestation state unprobed — UNMEASURED, not zero. | no — CI | `curl -s https://pypi.org/pypi/csoai/json` |
| 20 | **OpenTimestamps the single Merkle root** (free Bitcoin calendars) | The one field that converts 'signed' into 'anchored'. Free. Named by three separate briefs as the binding constraint. | no — CI/keys | `curl -s https://councilof.ai/root.json` → no `ots` |
| 21 | **`actions/attest-build-provenance`** over cards + `root.json` + dataset snapshots | SLSA provenance on arbitrary files, free on GitHub-hosted runners. | no — workflows | — |
| 22 | **Cloudflare Monetization Gateway** — charge per MCP tool call / evidence-bundle fetch at the edge | Waitlist **submitted 2026-09-05** as nicholas@csoai.org. The estate is already 100% behind Cloudflare with a live MCP server. | no — owner account | blog.cloudflare.com/monetization-gateway (waitlist-only) |
| 23 | **CDP Bazaar / x402scan / agentic.market x402-list** discovery filings | Named 'day one' filings; the estate records none of them. | partly — a door recording filing state is lane-doable | slug probe → 0 matches |
| 24 | **Door A — C2PA per-object 'check-this-asset'** | `c2pa` slug exists but no per-object door. Price must live in the 402 challenge, never in static bytes. | no — `functions/` | `scripts/price-gate.mjs` blocks numeric `price*` keys under `public/**.json` |
| 25 | **Door B — token/issuer/contract-state 'check-this-thing'** | `/api/xrpl` is 200; the per-object door is the monetisable wrapper. | no — `functions/` | `curl -s https://councilof.ai/api/xrpl` |
| 26 | **39 registry entries advertise `api.meok.ai`, which has no DNS record** | Every agent discovering those servers by remote hits an unresolvable host. All 39 have a PyPI stdio fallback, so degraded not dead. | no — registry re-publish | `dig +short api.meok.ai` → empty; `dig +short meok.ai` → resolves |
| 27 | **Signed daily census diff feed** of open x402 endpoints | Named the *non-redundant crown jewel*; competitors sell scores, none publish a signed diff. | partly — the door; the fleet is owner-gated | `x402-discovery-fact` is the only x402 slug |
| 28 | **Deployer submission API** (deployers submit own systems for signed measurement) | Highest data-network-effect ceiling in the playbook; requires traction. | no — `functions/` | — |
| 29 | **HF datasets `x402-bazaar-census` / `-conformance` / `registry-harvest-xrpl-mica-lei` shipped at 0 downloads and unlinked from the homepage** | Three published artefacts with no inbound path. UNMEASURED this pass — not re-probed. | partly — an interop index could link them | brief §Key Findings |

## CONTRACTS

| # | move | why it matters / what the probe found | lane-doable? | evidence a stranger re-runs |
|---|---|---|---|---|
| 30 | **ERC-8004 canonical door** naming Identity `0x8004A169FB4a3325136EB29fA0ceB6D2e539a432` and Reputation `0x8004BAa17C55a88189AE136b182e5fdA19dE9b63` | Four `erc8004` ledger-card slugs exist but no door states the two mainnet registry addresses. | **YES** | slug probe |
| 31 | **ERC-8004 validator registration** | Named twice as *the cleanest standing-to-revenue conversion*, doctrine-compatible if it records measured fact only. Validation registry unshipped. | no — wallet/keys | brief PART A |
| 32 | **ERC-3643 / T-REX tokenised-treasury door** | No `trex`/`t-rex` slug anywhere in the estate. | **YES** | slug probe → 0 matches |
| 33 | **EAS on Base** — SchemaRegistry.register + EAS.attest (off-chain free, on-chain cents) | `facts.json` marks the rail `planned` and its code refuses to mint. A public badge claimed 104 models EAS-attested; corrected to `planned`/0 earlier today. | no — wallet | `client/src/data/facts.json` rail `eas` |
| 34 | **BMR-posture notice** — the board is a measurement record, NOT a benchmark for financial instruments | If a third party references the board inside a financial instrument the Benchmarks Regulation may attach. Cheap, honest, defensive. | **YES** (draft) | no `bmr`/`benchmark-regulation` slug |
| 35 | **GENIUS Act NPRM comment — due 19 Oct 2026** | Hard external date; the only dated regulatory window in the whole mine. | no — owner files | Federal Register, published 18 Aug 2026 |
| 36 | **Art 50 disclosure cards on hub-queue top-100 before 2 Dec 2026** | Second hard date. `eu-ai-act` door exists; the cards do not. | no — signing | slug probe |
| 37 | **GPAI Code of Practice — DO NOT SIGN** | Explicit negative recommendation: a pure measurement body is not a GPAI provider. Recorded so no lane signs it by initiative. | n/a — a do-not | brief §Recommendations |

## XRPL

| # | move | why it matters / what the probe found | lane-doable? | evidence a stranger re-runs |
|---|---|---|---|---|
| 38 | **Per-issuer XRPL failed-check cards** (SG-FORGE, Schuman, Palau) — `unmeasured=["strict_two_way_toml"]` | 12 `xrpl` slugs exist and `/api/xrpl` is 200, but per-issuer failed-check state is not published. | no — signing (goal §4 says draft only) | `curl -s https://councilof.ai/api/xrpl` |
| 39 | **Outward evidence-card path for the 4-of-16 two-way issuers** (domain + toml both present) | Goal §4 deliverable. Drafted in `LEDGER-AGENTS-XRPL-issuer-contract.draft.md`, not shipped. | **YES** (draft doc only) | `ledger-card-autoeat-xrpl-twoway` slug exists |
| 40 | **XRPL amendment-status census** (DID amendment; XLS-80 Permissioned Domains 4 Feb 2026; XLS-85 native escrow 12 Feb 2026; XLS-0096 Confidential Transfers in voting) | Amendment status is itself a signable census and the estate has no door for it. | **YES** | no amendment-status slug among the 12 `xrpl` matches |
| 41 | **26 SWIFT cards said to carry `sig_algo: SHA256-placeholder`** | **UNMEASURED** — not reproduced in `/interop/swift-registry.json`; the placeholder cards live elsewhere. Absent ≠ zero: unconfirmed is not refuted. | no — signed bytes; supersede or ledger only | `curl -s https://councilof.ai/interop/swift-registry.json` → no match |
