/**
 * Clean EAT playbook — attestation on the same rail as GSPC / DSH / OS.
 *
 * Binding doctrine (do not soften):
 * - Measurement, not certification
 * - Unsolicited + permissionless (not merely “independent”)
 * - Attestation ≠ tokenization ≠ ownership
 * - Scores never sold · regulators + verify free forever
 * - DSH = same evidence as OS (OWNERSHIP #80)
 * - Labour/economy indices = contextual firewall · UNMEASURED until method
 *
 * Canon also: docs/EAT_DSH_ALIGNMENT.md · docs/ESTATE_CROSSWALK.md ·
 * docs/COUNCIL_OS_BUILD_PLAN.md · compliance/*
 */

# EAT playbook — clean plays only

## Ready vs not ready (honest)

| Claim | Ready? |
|-------|--------|
| Declare AI-economy / human-labour / humanoid-labour as **UNMEASURED** public surfaces | **Yes** — `/indices/*` · `GET /api/indices` |
| Scale **all** public contacts × 8 RWA × XRPL × Wilson into signed mainnet MEASURED | **No** — custody + counsel gates open |
| “All legal done” | **No** — templates exist; counsel sign-off pending |
| “All HF living DB polished MEASURED” | **No** — ship REPORTED/UNMEASURED dumps first |
| Wire nav + products catalog + Option A page on this branch | **Yes** — this wave |
| Merge to `master` / Cloudflare Pages prod | Owner merge only |

## Messaging lock (one block)

Unsolicited + permissionless signed opinions/measurements — no issuer opt-in, no issuer payment. Attestations are not tokens, NFTs, or ownership claims. They do not tokenize anything and confer no title. Rivals who call themselves “independent” still require issuer cooperation; we do not.

## Clean vs demo plays

**Clean (public-artifact path may proceed to Stage 2 after gates):** Aviva (when on-ledger), RLUSD, BUIDL, BENJI, OUSG — see `client/src/data/rwaAttestationTargets.ts` `play: "clean"`.

**Demo-only:** JMWH and any announcement-without-artifact — `play: "demo"`. Never mainnet-attest demos as MEASURED.

## Three stages (unchanged)

1. **Doctrine / stubs / UNMEASURED surfaces** — adapters, pages, menus, legal templates (this wave).  
2. **Testnet publishers** — Memo / provisional Credential / EAS pointers; `CSOAI_KEY_CUSTODY` required before `--publish`.  
3. **Mainnet** — only after AWS KMS (both curves) or Turnkey **and** securities counsel on named-security language.

## Same spine as GSPC

```
public artifact → adapter → deterministic grade → Ed25519 card
       → OS Lobby card = DSH /dashboard card
       → /gspc-verify (or pack verify)
       → optional chain pointer (not ownership)
```

RWA does **not** invent a second board. Labour indices do **not** feed the grade.

## Wilson / confidence

Use Wilson intervals only on **frozen banks** with usable n. UNMEASURED cells stay empty — never scored as fail, never filled with invented AUM.

## DSH · RunPod · Oracle · Kaggle · Cursor

| Surface | Role in EAT |
|---------|-------------|
| **DSH** `/dashboard` | Same evidence pack as OS — never a softer grade |
| **RunPod / GPU** | Not the churn accelerator for contract attestation (I/O + CPU). Use for model axes only |
| **Oracle / Chainlink-shaped** | Countersign / independent verify metaphor for Option A — we remain opinion body |
| **Kaggle / HF** | Publish REPORTED corpora & methods; MEASURED dumps only after freeze + sign |
| **Cursor agents** | Execute NEXT_300 moves; no CopilotKit greenfield rewrite |

## HO.2

Verification free forever. Never put grade prices on marketing pages (`/products`, `/powered-by`, homepage). Meter access/runs/seats only.

## Owner gates before scale

1. Key custody decision signed (`compliance/key-custody-decision.md`).  
2. Counsel clears `compliance/attestation-language-template.md` for named securities.  
3. Publishers refuse `--publish` without custody env.  
4. Value Ledger `publishedCount` stays 0 until real receipts.

## What this repo must show visitors now

- `/indices` + three UNMEASURED pages  
- `/products` catalog (honest registers)  
- `/powered-by` Option A  
- `/competitors` RWA corpus  
- `/engine-axis` candidacy notes  
- Header Measure + Solutions links  
- `docs/NEXT_300_MOVES.md` execution register  
