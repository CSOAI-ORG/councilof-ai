# ERC-8004 — passport to live GSPC card + root — 1 Sep 2026

**Status:** DRAFT HOLD. Cite ERC-8004 public scan class only. **No endorsement.**  
**Locks:** Board **22 · 15 · 7**. Passport points at **evidence**, is **not** the evidence. **Never tokenize models.** Never MEASURED from listing. Never certify. Never sell an agent rank.

---

## Thesis

One on-chain agent **identity** (ERC-8004 registration class) should point at:

1. a **live GSPC / card-v0 card** (fetchable, stranger-recomputable), and  
2. the **living merkle root** that includes that card’s leaf sha256.

The passport is a **pointer**. The card + root are the evidence. Weights / checkpoints / model blobs are **never** tokens under this grammar.

Surface (existing grammar): `erc8004.callable` — registered-vs-callable coverage leaf; still not a board fill.

---

## Cite-only pane (re-pin before spray)

| Label | Fact (cite class) | Board rule |
|---|---|---|
| Registrations | ~531,269 across ~24 chains (public scan — re-fetch) | Labelled component only |
| Callable fraction | ~3–15% declare a live MCP / A2A-class endpoint | registered ≠ signed-and-callable |
| Role | Identity passport → card + root | Never tokenize weights |

**Do not** stamp `ai-economy-index` MEASURED from this pane.

---

## Passport discipline

| May | Must not |
|---|---|
| Point `evidence` / URI fields at card URL + root URL + digests | Embed model weights as the "asset" |
| Cite registration id + chain | Sell "ERC-8004 certified by Council of AI" |
| Use card-v0 `erc8004.callable` for coverage facts | Fill empty GSPC axes from registration count |
| Keep same public root as all other arms | Fork a second board for agents |

Dorado / escrow gates (elsewhere) open on **card + drift delta**, not on a model token.

---

## Hard stops

- Passport to evidence — **not** evidence.  
- **Never tokenize models.**  
- Never MEASURED-from-listing.  
- No endorsement claim.  
- One root. Chain witnesses **root hash only**.

Companions: fire-playbook `06-cite-only-panes.md` §A · denser-roots wedge · card-v0 grammar §passport.

*End. Pointer, not grade. Europe/London. 1 Sep 2026.*
