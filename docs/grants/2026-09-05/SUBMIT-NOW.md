# SUBMIT-NOW — funding doors, 2026-09-05

> **Measured facts, each naming the endpoint or file that returns it.** Re-fetch before sending.
>
> - **Buyer's-eye x402 census (measured artefact).** 316 conformant hosts paid for real: **100 DELIVERED**,
>   **213 REFUSED**, 2 NO_CHALLENGE, 1 MISMATCH. **13 hosts recorded an on-chain settlement and still
>   delivered nothing** (0.193 USDC), each row carrying its tx hash so a reader can check the chain.
>   Dataset: <https://huggingface.co/datasets/csoai/x402-settlement-census> — `summary-2026-09-06.json`.
>   *One purchase per host, one moment: a single refusal is not a pattern. 1.3398 USDC spent, all of it ours.*
> - **Revenue.** `/api/revenue` → `one_number.all_time` = **0** distinct non-self payers, status **MEASURED**.
>   Separately `settled_usdc.count` is **`null`, status UNMEASURED** — null is not zero, and neither is
>   revenue. Self-settlements (5) and zero-value settlements (4) are recorded and are never payers.
> - **Hub cells.** `/api/hub-cards` → `counts`. These are **third-party models on the Hub, never our own
>   coverage** — the endpoint says so in its own `population` field.

Every line was decided by bytes read on 2026-09-05 (see each target's file and `FACTS-2026-09-05.json`, regenerated by `bash scripts/grants/facts.sh`). "Owner" means a password-based account or money; OAuth sign-ins (GitHub/Google buttons) are authorised for agents in the browser — none of the five offers one.

```
READY    sovereign-tech-fund      native account on apply.sovereign.tech (email-confirmed; NOT OAuth → owner creates)   rolling, no deadline; ~10 weeks to first reply
READY    ccs-rm6200               password account + MFA on supplierregistration.cabinetoffice.gov.uk (owner) + DUNS + PI/PL insurance (money)   none — DPS open to 23/02/2029; Cyber Essentials only before first order
READY    circle-faucet-x402       no account (reCAPTCHA only) — anyone; governor already running it   none; 20 USDC / 2 h / address on Base Sepolia
NOT-FIT  gitcoin                  no open round: GG24 ended Mar 2026, GG25 never announced, DAO in "Transition Stewards" since Jul 2026, grants.gitcoin.co does not resolve; eligibility itself passes (51 commit days/90, MIT+Apache-2.0); wallet sign-in when a round opens
NOT-FIT  optimism-atlas           Season 9 ended 3 Jun 2026; Grants Council dissolution proposed 25 Jun; mission metric is DEX TVL and requires contracts deployed on an OP chain (we have none); Atlas sign-in is Privy email-OTP/Farcaster/wallet, no OAuth
```

| Target | File | Sign-in observed | Amount | What is done |
|---|---|---|---|---|
| Sovereign Tech Fund | `sovereign-tech-fund.md` | native account (Cloudflare JS-gated; no OAuth visible) | ≥ €50,000 cost of work; we propose €84,000 / 1,200 h / 12 months | full application in the PDF form's exact question order |
| CCS/GCA RM6200 AI DPS | `ccs-rm6200.md` | password + MFA (NQC portal) | free listing; levy on orders only | filter selections, standards/insurance/CE positions, capability statement |
| Circle faucet + testnet loop | `circle-faucet-x402-testnet.md` + `scripts/grants/x402-testnet-loop.sh` | none | 20 testnet USDC / 2 h | facilitator, network id, asset, payTo, exact curl; script with key placeholder |
| Gitcoin | `gitcoin.md` | wallet (SIWE) on Gardens/Giveth when a round runs | GG24 tooling: $1k–$13k per proposal | project profile in canonical field order; eligibility proven by bytes |
| Optimism Atlas | `optimism-atlas.md` | Privy: email / Farcaster / wallet | Season 9: 3.89M OP + 0.98M OP pools, in OP | Atlas project profile in field order; what would make it fit |

Findings that change other documents:
- The repository LICENSE file is **MIT** ("Copyright (c) 2024-2026 COAI - Council of AIs"); only the PyPI/npm packages are Apache-2.0. Any application or page that says "Apache-2.0" for the repo is wrong — say "MIT repo, Apache-2.0 packages, CC-BY-4.0 data".
- `/api/revenue` still serves `skus.issuance.note` = "No live settle path (x402 is fail-closed, mode:mock)" while `/api/x402` and `.well-known/x402.json` say `mode: live`. The producer is **`counters.json` at the repo root (line 123)** — `functions/api/revenue.ts` already derives its own `null_rule` from `railMode(env)`, but the SKU note is copied verbatim from the counter canon. Fix the producer, not the artefact. **Fixed in <https://github.com/CSOAI-ORG/councilof-ai/pull/1409> (pending merge).**
- The edge has no testnet switch: `_x402_config.ts` maps `base-sepolia` ↔ `eip155:84532` but nothing sets it, so the testnet loop copies the live 402 and re-targets it rather than exercising the edge.
