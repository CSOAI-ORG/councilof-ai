# Lifestyle MCP — archive / unbrand (recommendation)

Product MCP is **four tools** at `https://councilof.ai/mcp`. Lifestyle servers are **not** the product. Do not list them on `/products`, home, or Get measured.

Off the product list (examples still in `client/src/data/mcpRegistry.json` / scorecard dumps):

| Slug | Why it is not GSPC |
|---|---|
| `fishkeeper-ai-mcp` | Lifestyle |
| `fitness-ai-mcp` | Lifestyle |
| `csv-tools-ai-mcp` | Utility, not a measurement bank |

TUI 3 / owner: archive or unbrand those GitHub repos when convenient. TUI 4 does not merge 600 repos and does not spam awesome-mcp-servers #12480 (stale 13/14 correction already posted).

MEOK / UK Games Fund are not GSPC. Don’t mix.
